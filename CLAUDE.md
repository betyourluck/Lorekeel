# CLAUDE — Kataribe (語り部)

クラウド LLM をナレーター、Rust の決定論エンジンを正本とした TRPG-GM。

## 北極星

**「忘れない・矛盾しない GM」。** 売りは無限の自由ではなく一貫性。buzz/karma は反指標。
LLM-GM の死因は文章力でなく忘却と矛盾 → LLM に状態の真実を持たせないことで構造的に断つ。

## アーキテクチャ — 三権分立

> LLM は提案し、エンジンが裁き、Memoria が覚え、シナリオが縛る。

- **エンジン（正本 / Rust `crates/gm_core`）**: HP/所持品/ダイス/フラグ/位置の唯一の真実。
  - `adjudicate(state, scenario, delta) -> Verdict`: **state を一切変えない純粋関数**。却下理由は構造化 `RejectReason`（文字列でなくデータ）で返し、文面は提示層が `localize(lang)` で生成（Ja/En、i18n の土台）。検証ルールはエンジン不変、文字列だけ分離。
  - `apply(...)`: 受理時のみ**原子的**に適用。1つでも不正 op があれば全体却下・state 無傷。
  - ダイスは seeded RNG（`RngState`）で決定論・監査可能。LLM は出目を持てない（op 構造上不可能）。
- **LLM（提案 / Rust `crates/llm_client`）**: `StateDelta { narration, ops }` を返す。`narration` は検証しない（LLMの領分）、`ops` は全件検証。
  - OpenAI 互換 chat/completions を `base_url`+`api_key` で抽象化。**tool-use 強制**（単一ツール `emit_delta`）で構造化出力。
  - `emit_delta` の schema は schemars が `gm_core::StateDelta` から**機械生成**（規格=実装の単一真実源・手書き禁止）。
  - `tool_choice` 非尊重サーバ向けに content のフェンス JSON フォールバックあり。パース失敗は `raw` を保持し再生成の燃料にする。
  - **裁定はしない**。messages 構築と `adjudicate` は上位（harness）の責務。
- **ターンループ（結線 / Rust `crates/harness`）**: 三権を 1 ターンに繋ぐ。`run_turn`: 提案 → `adjudicate` → `Accept` なら `apply`／`Reject` なら理由を messages に積んで再生成（最大N回、LocalAI `_self_repair_loop` 同型）。ループは `DeltaProposer` trait に対して書き、実 LLM とテスト用 fake を差し替えられる。
- **シナリオ（拘束 / `scenarios/*.yaml`）**: `Gate` 条件つきの場所グラフ。即興が筋から外れすぎないよう縛る。
- **Memoria（記憶）**: 伏線・キャラ性格の semantic recall。**可変世界状態は絶対に置かない**（曖昧な recall は「忘れる GM」を再現する）。

## 掟（Mandate）

- **データ・ファースト**: コードの前に `data_contract.yaml`（名詞）を凍結する。
- **PoC 必須**: バグ修正・新機能は Red→Green をテストで実証してから完了。推測修正は不可。
- **リサーチ先行**: 実装前に三点測量（既存コード grep / 仕様 / 記憶）。
- **撤去したら grep**: 機構・enum 値・フィールド・op を**撤去**したら、その名前で全台帳（`CLAUDE.md` / `specs/*.md` / `data_contract.yaml` / doc comment / package_spec）を grep し、追従漏れを回収してから完了とする。**追加は書き足すので必ず目に入るが、撤去は既存節の読み直しを要求する**——この非対称が漏れの機序で、実測では回収を主題に据えたコミット（`89b9926`）ですら 3 箇所取りこぼした（2026-07-22 に `106b53c` で回収）。人の注意力でなく grep で発生させること。撤去した enum 値は**未知キー lint の射程外**（キーは既知で値が無効＝lint 到達前に serde が落ちる）なので、機械の網は代わりにならない。
- **失敗時に謝罪しない**: 観察 → 仮説棄却 → 次の検証ステップの三段で進む。失敗は接地を深める信号。
- **層分け**: 仕様・契約はこの file 台帳（`data_contract.yaml` / README / docs）に書く。Memoria には蒸留した教訓・判断だけを残す。

## 主要コマンド

```bash
cargo test --workspace            # PoC テスト（gm_core + llm_client、計 18）
cargo clippy --workspace --all-targets  # lint
```

## 現状

- ✅ `gm_core` 正本エンジン: 33/33 green。密室脱出（真偽の最小盤面）で不正遮断・原子性・敵対ターン・決定論ダイス、力の試練（数値の最小盤面）で**数値ステータス**を実証。
  - **数値ステータス** (`stats: BTreeMap<StatKey, i64>`): 四則演算をエンジンが代行。`AdjustStat`(＋/−) と `ScaleStat`(×/÷) で LLM は意図だけ提案・値は持てない。HP の 0 クランプ、**ゼロ除算は却下**、未宣言 stat（幻ステータス）の遮断、`StatAtLeast` 数値 gate を実証。式（ダイス＋能力修正の技能判定）は次の盤面。
  - **実 LLM 検証 (2026-06-23, claude-opus-4-8)**: 力の試練を通しプレイし、LLM が `adjust_stat`（鍛錬で str+2/hp-2 のトレードオフ）と `scale_stat`（「腕力を倍に」→ str 12×2=24）を**プロンプト変更ゼロ**で提案（schemars 自動露出）、`StatAtLeast` gate（str≥15）を越えて goal 到達。「LLM が意図を言い、エンジンが値を計算する」を端から端まで実証。LLM は現在値を読んで数値推論し（"14 では一歩足りない"）、不可能な手は自ら拒否。
  - **キャラ別ステータス** (`entities: BTreeMap<EntityId, BTreeMap<StatKey,i64>>`): 数値を entity（`"player"`/NPC/ヒロイン）別に保持。op/gate は `entity` を取る（省略時 `player`）。外部キャラ定義 `CharacterDef`（name/profile/stats[`StatDecl` initial・min・max]/taboos）を scenario が持つ。**境界クランプ**（好感度 max 100 等）と**未宣言 stat/entity の遮断**を実証（邂逅シナリオ）。`profile`（設定・背景・性向）は語りに供給。**禁忌は2種**: 硬い禁忌（豚肉を断つ＝`taboos` の Gate、✅Phase B 実装済）/ 柔らかい性向（同性を好む＝profile、語り）。Phase B: `check_taboos` が delta を state の clone に射影し taboo(Gate) が false→true 真化するなら却下（キャラは自分の禁忌を破れない＝「正本 > 文章力」のキャラ版）。禁忌(却下)とトリガー(発火, ✅Phase C 実装済)は同一機構の双対。
  - **反応ビート / トリガー** (`Scenario.triggers`, ✅Phase C): 禁忌の双対。`when`(Gate) が delta 適用後の実 state で真化した瞬間、authored な `effects`(StateOp 列・信頼済で検証せず)を `fire_triggers` が原子適用し `narration` を語りに注入する。`GameState.fired: Set<TriggerId>` で **edge-triggered once** に latch（セーブ対象、when が真のままでも再発火しない）。効果が次の trigger を真化させる**連鎖**は settle まで回す（各 trigger 高々1回＝必ず停止、authored 順で決定論）。**`repeatable: bool`（✅2026-06-26、既定 false）**: true なら永続 latch せず when 再真化で次ターン再発火（カウンタ閾値→効果→リセットのループ。リセットは authored 効果 `scale_stat num:0`/`adjust_stat` 負で書く）。停止性は二層管理（settle 内 `fired_this_settle` で高々1回＝効果が when を真のまま残しても無限ループしない／永続 latch は非 repeatable のみ）。PoC: `repeatable_trigger_loops_on_threshold_with_reset`/`repeatable_trigger_fires_once_per_apply_and_terminates`。`adjudicate`(純粋)は発火させず、発火は `apply` の戻り `ApplyOutcome.fired`(→harness `Accepted`)に載る。約束の想起シナリオで「好感度 30→想起→誓い直し→goal」の連鎖を実証（伏線の回収をエンジンが保証）。`memoria_bridge`（『思い出す』系→semantic recall→語り注入）は次。
  - **却下理由の多言語化**: `Verdict::Reject` は構造化 `RejectReason`（12 種）。`localize(Lang::{Ja,En})` で文面生成、bin は `KATARIBE_LANG=en`。検証ルールはエンジン不変、文面だけ言語層。
  - **外部キャラファイル**: `characters/*.yaml`（1キャラ1ファイル、ファイル名＝EntityId）を `harness::load_characters` が読む。**注入は `Scenario.cast` 宣言でスコープ**: `harness::inject_cast` が cast に挙げた entity だけを注入し、cast 空なら何も注入しない（inline 優先）。これで alice が全シナリオ（密室脱出等）に無差別混入する問題を断った（GUI 実機で発見、回帰テスト `no_cast_means_no_injection`）。邂逅は `cast: [alice]` で外部注入を宣言。**実 LLM 検証 (2026-06-23)**: 邂逅シナリオ（inline キャラ無し）で alice を外部ファイルから注入 → LLM が `adjust_stat entity=alice` で好感度 0→55、profile（人見知り・甘いもの・豚肉の誓い）を語りに自発反映、好感度 gate で goal 到達。
- ✅ `crates/llm_client` ナレーター脚: LocalAI `llm_client.py` を Rust 移植。OpenAI 互換 + tool-use 強制 + schemars 機械生成 schema + フェンス JSON フォールバック + 指数 backoff。罠は `failures.md`。
  - **統一ツール層 / spec 12 Phase A〜E (✅2026-07-15、Phase E live 検証完了)**: aituber-onair 上流 (2年運用の VTuber システム、`D:\Github\aituber-onair\docs\unified-tool-layer-design.md` が写経元) の運用知識を取り込む導管。**canonical + adapter seam** — `canonical::{ChatRequest, ChatResponse, ToolCall{args:object=D2}, ToolChoice::Specific, Finish, Usage}` を Driver (generate/generate_structured) が組み、`complete()` の provider match 一箇所から 3 adapter (openai_compat / anthropic / **gemini 新規** = generateContent + systemInstruction + ANY+allowedFunctionNames + x-goog-api-key、id は client 単位 AtomicU64 で `call_{seq}_{index}` 合成) の **encode/decode 純関数**へ。抽出 (`parse::extract`) は canonical への単一経路。`Provider` 三値化 (generativelanguage かつ `/openai` を含まない時のみ Gemini = 互換エンドポイント利用者を保護)。**`LLM_EFFORT`** (opt-in、不正値は Config エラー): Claude = `thinking:adaptive`+`output_config.effort` (未設定なら送らない。max_tokens は思考+本文の**合算上限** → effort 時 16000 以上推奨の非 fatal 警告 `LlmConfig::warnings()`) / **Grok = 対象モデル (grok-4.3/4.5) に既定送出 (opt-out)**: 4.3→`none`/4.5→`low` — 未送出だと xAI 既定の思考が max_tokens を食い潰し**空デルタ/タイムアウト** (grok-4.3 の真因、Phase E で確定=reasoning_effort=none 送出で解消・5ターン11秒/Empty ゼロ)。**empty-response 防御**: text 空∧tool_calls 空∧finish==length → EmptyResponse を一過性昇格し compat リトライで再抽選。資産保全 (cache_control #44 / conv_id #45 / raw 保持 #34 / CoT 除去 #30 / ops 救済 #40) は adapter 内へ移設・観測同一。**Phase E 実測完了 (2026-07-15)**: Anthropic cache_read=13607 / **Gemini C.5a Green** (`gemini::adapt_schema` の oneOf→anyOf で整数列 ops ゼロ・4バリアント正しく構築・attempt_challenge ダイス経路も通る、gemini-3.5-flash、failures #52) / **Grok cached は turn3 から ~88% 安定** (「128 頭打ち」は 2ターン計測アーティファクトだった=sticky routing のウォームアップに2ターン要・#45 の 89% と一致。**←2026-07-16 改訂・同日決着 (failures #57)**: これは台本駆動の高速ループ限定 — GUI 人間ペース実測 (12+40 リクエスト、`t=` 間隔計測) で cached は「静的全量/128」の二値交替。128=miss のフロア表示 (xAI は 128 ブロック粒度・部分ヒット 4096 も観測)。**miss は間隔非依存 (29〜68s で miss・118s で hit) → TTL 説棄却 = LB の best-effort 分散/確率的 eviction**。定常 miss ~11%・warm-up 3 リクエスト・人間ペースの累積 hit rate ~60% を上限見積もりとする) / **narration 量 tool-use 1.60× no-tools** (Grok、目安 1.5 クリア・Gemini ネイティブは use_tools 無視で A/B 不成立=K4)。Grok の「拒否癖/cast_vote latch」は n=4 で溶けた=確率的一発 (grok-4.3 は提案 noisy だが engine が全 spurious op を backstop・state 汚染ゼロ・outcome 到達=「正本 > 文章力」の positive 検証、failures #53=n≥3 で観察の方法論罠)。残 = Phase F (streaming、任意)。契約は data_contract `UnifiedToolLayer` 節、設計は `specs/12_unified_tool_layer.md`。
  - **プロバイダ別の送り分け 3 軸 / OpenAI・Meta 対応 (✅2026-08-08、実機 400 起点)**: OpenAI (gpt-5 系) と Meta (api.llama.com) がターン開始直後に必ず 400 で死んでいた。真因は `openai_compat::encode` が**モデル・ホストに依らず同じボディを組んでいた**こと — 互換を名乗るサーバは 3 つの軸で割れる。①**出力上限の欄名**: gpt-5 系/o 系は `max_tokens` を 400 `unsupported_parameter` で拒み `max_completion_tokens` を要求する一方、互換サーバ (llama.cpp/vLLM/gpt-oss/さくら) は新欄を知らない → `uses_max_completion_tokens(model)` で**常に片方だけ**送る (全面置換は不可)。②**`reasoning_effort` の明示**: gpt-5 系は `/v1/chat/completions` で function tools と思考の併用を拒否 (`Function tools with reasoning_effort are not supported`)。**キーを省いても回避できない** — 省くとサーバ既定の思考が効いて同じ 400 (**送っていないパラメータの名前で拒否される**)。`reasoning_effort(model, effort, sends_function_tools)` に一般化し、gpt-5 系は**tools を送る周だけ** `"none"` を固定 (送らない周は触らない＝制約は併用に掛かっており、一律に止めるとあらすじ要約の思考まで殺す)。o 系も射程に追加。③**`tool_choice` の実装範囲**: Meta は `only "auto" is supported for tool_choice. "none", "required", and named function choices are not currently supported` と名指しで 400。旧 `use_tools: bool` は「tool_choice が使えない」と「tools が使えない」を**同一視**しており、Meta を no-tools へ**過剰降格**させていた (実測 narration 量 tool-use = no-tools の 1.60× ＝品質ダウングレード) → **`ToolMode` 一軸三値** (`Forced > Auto > Off`) へ。判定は **明示 `LLM_TOOL_MODE` > `LLM_USE_TOOLS=false` > base_url 自動** で、旧キーの `true` は自動判定を上書きしない (既存 .env が `true` のまま Meta を指しても正しく `Auto` に落ちる)。**ホストで判定しモデル名では判定しない** (拒否はサーバの実装範囲でありモデルの性質ではない — 中継越しの同じ Meta モデルでは名前指定が通る)。加えて **400 からの自己降格 + latch**: `tool_choice` を名指しした 400 で `Forced→Auto→Off` と一段降格し `LlmClient.tool_mode: Mutex<ToolMode>` に覚える (中継・自前プロキシはホスト名から判定できないので実際の拒否から学ぶ。余計な往復はセッションで高々 2 回・名前指定が通るサーバでは一度も発火しない・底で名指しされたら降格せず 400 をそのまま返す)。**降格しても安全な根拠を先に数えた**: 提示ツールが `emit_delta` 1 本／GM_SYSTEM の提出行が mode 非依存／`parse::extract` のフェンス JSON フォールバック→`NoStructuredOutput`→既存 self-repair ループ。`tool_choice` はボディの値であって安定プレフィックスではないので **prompt caching (spec 13/14) に一切触れない**。**同時に `OutputTruncated { limit }` を `EmptyResponse` から分離** (非一過性＝上限は入力に対して決定的で再送しても同じ所で切れる。リトライはバックオフと課金だけ増やし画面には「空の応答」としか出なかった。次の一手が「`LLM_MAX_TOKENS` をいくつまで上げるか」なので現在値を文面に載せる)。**参考実装を意図的に写さなかった箇所が 1 つ**: 派生プロジェクト側は `Choice.message` を `serde(default)` で緩く受けるが、Kataribe では**必須のまま**にした — #34 は Gemini が 200 で `message` 無しを返した実観測で、真因 (`content_filter`) は Parse エラーが raw ごと本文を surface したから読めた。緩くすると同じ応答が「空の応答」に潰れて理由が消える（**緩さが安全なのは情報を捨てないときだけ**）。PoC 5 本 Red→Green (Red は実機の 400 と同じ形で落ちることを先に確認)。契約は data_contract `UnifiedToolLayer` の `tool_mode` / `tool_mode_downgrade` / `send_splitting` / `output_truncation` 節、罠は `failures.md #78`。**実機 1 周目 (2026-08-08 ユーザー実測)**: ワイヤは通った (降格が発火 → 再送 → 200・prompt 11053/completion 1847) が**次の失敗が出た** — Meta は `tool_choice` を強制できないので**ツールを使わず content に JSON を書き**、schema を知らないまま tool 定義から形を推測して `"ops": "
"` (配列であるべき場所に文字列) を出した。**#78 一般化 2 の実演**（400 → 次は抽出）。二段で修正: (a) **Auto でも schema を prompt に載せる** (`tool_or_json_treatment` ＝ Off の `json_instruction` とは別文面。あちらは「このサーバはツール呼び出しに対応していません」と言い切るので流用するとツール利用を自分で妨げる。`ops` は必ず配列を名指し＝救済に頼る前に起こさせない。**末尾に積むので安定プレフィックス不動＝キャッシュ影響ゼロ**) (b) **`parse` の lenient 二経路が救済の失敗理由を握り潰していた**のを修正 — 旧 `.map_err(|_| first)` は「一次症状を残す」意図だったが、**救済が効かなかった理由を恒久的に見えなくする**（画面には「ops が文字列」と出続けるのに実際の詰まりは別）。型付きパースは前から読むので後ろが壊れた応答でも先に見つかった型の不一致を報告する → **素の `Value` としても読めなければ詰まりは型でなく構文**なのでそちらを返す。PoC +2 (content 経路の救済を 4 包み方で / 途中切れは構文エラーを surface)。**✅実機 2 周目で Green (2026-08-09 ユーザー実測)**: Meta = 降格発火 → 再送 → 通し (prompt 11931/completion 2037)、**OpenAI (gpt-5 系) も通過**。3 軸とも実機で確認済み。**キャッシュの記述を 2026-08-09 に訂正**: 当初「Meta の互換経路は `cached_tokens` を返さないので `cached=0` が続く」と書いたが、これは**開幕 2 リクエストだけを見た n=2 からの過剰一般化**だった (どちらも初回付近＝書き込み側なので miss が正常な区間)。継続プレイでは**たまに効く** (ユーザー実測)。正しくは「Meta は cached_tokens を返しうるが命中は不安定」で、Grok の確率的 miss (#57) に近い側。GUI のキャッシュ健全性警告 (連続 miss 3 で ⚠) は Meta では鳴りうるが、#44 の注記どおり「キャッシュ非対応/不安定なプロバイダでは正常」の側。**方法論の反省は #53 と同型 — 観察 n が足りないまま「返さない」と機構の性質を断定した** (「今回は 0 だった」で止めるべきだった)。 **アグリゲータ対応 (2026-08-09)**: OpenRouter 等の中継は `vendor/model` 表記 (`openai/gpt-5.6-luna` / `x-ai/grok-4.3`) を使うため、**送り分け 3 軸のうちモデル名で判定する 2 軸が接頭辞で素通りし既定へ落ちていた** — 中継越しの gpt-5 系は旧欄 `max_tokens` を送って #76 の 400、思考も明示しないので #77 の 400、grok は思考が max_tokens を食い潰す＝**中継を挟むと直接なら踏まない罠を同じ順で踏み直す**。`bare_model()` (最後の `/` 以降) で正規化。接頭辞が無い既存動作は不変、パス風モデル名 (`models/foo.gguf`) も剥がした結果がどの族にも一致しないので無害。**`ToolMode::detect` には openrouter を足さない** — 中継の `tool_choice` 対応は**ルーティング先のモデル次第**でホスト名から決まらないので、400 からの自己降格 (latch) が正しい機構（そのために作った）。PoC 1 本。
  - **Perplexity / OpenAI Responses 形の第 4 adapter (✅2026-08-20、実機エラー起点)**: app の `.env` を `https://api.perplexity.ai/v1` に向けると即死 — **Perplexity には `/chat/completions` の口が無い** (404・本文なし。Fuseforks が 2026-08-19 に同じ形で踏んだ)。互換の口 `/router/v1/chat/completions` (Router API) は**有料クレジット要** (403)、鍵だけで通るのは Agent API の OpenAI Responses 互換別名 **`/v1/responses`**。`#78` の延長 = 「互換」を名乗る層はメッセージの形しか保証しないどころか、**その形を話す口が既定のパスに無い**プロバイダがある。**解**: `crates/llm_client/src/responses.rs`（encode/decode 純関数、写経元は Fuseforks `openai_responses.rs` Spec 34 — Kataribe が使う欄だけ: flat function tool / `store:false` 常送 / `max_output_tokens` / `reasoning{effort}` は LLM_EFFORT 明示時のみ、web 検索・思考要約・`include` は持ち込まない）+ `Provider::Responses`（**`api.perplexity.ai` かつ `/router/` を含まない base_url を自動判定** = Gemini の `/openai` 例外と同じ形で互換の口を選んだ利用者を壊さない。明示は `LLM_PROVIDER=responses|openai_responses|perplexity`）+ `responses_endpoint()`（ホスト直なら `/v1` を補う）+ client.rs の `complete()` に 1 腕 + retry（`reject_empty_reasoning` を共有 = `status:incomplete`→Length→OutputTruncated）。**probe で確定した癖 3 つ**: ①`tool_choice` の**名指しは 200 で黙殺**（message が返る・文書の request schema にも無い）、**`"required"` は効く**（3/3 vs 無指定 2/3）→ 提示ツールが `emit_delta` 1 本なので `Specific` を `required` へ畳む（「受理された≠効いた」= #52 の同族、出力形のテストでしか分からない） ②実 schema（oneOf 入り）がそのまま通り `ops` を正しい配列で返す ③usage は `input_tokens_details.cached_tokens`（文書は `cache_read_input_tokens`、両受け）+ `cost` USD。**ToolMode の降格は持たない**（tool_choice で 400 を返す口ではない）。**GUI は無改修** — 設定 UI は base_url/model/key しか持たず provider は自動判定なので、ユーザーの既存 `.env` がそのまま通る。PoC 3 本 Red→Green（自動判定+endpoint+語彙 / encode の形 / decode の function_call・message・usage・incomplete・壊れ args・非 JSON 本文）+ **live**: `tests/live_responses.rs`（`--ignored`、`PERPLEXITY_API_KEY`/`PPLX_API_KEY`）で structured+plain Green、CLI `play --package lakeside_manor` 2 ターン完走（prompt ~7k、却下ゼロ）。**キャッシュは同日に床を実測して決着**: `cached_tokens` は **床 8192 トークン**未満のプレフィックスでは 0 (当初「8192 ブロック単位」と書いたが 2026-08-21 実プレイで 12206 中 10496 = 非倍数を観測 → 粒度はより細かい・床の主張は不変)（同一プレフィックス ×3 をサイズ別に計測: 7.7k→0/0/0、13.3k→0/8192/8192、20.3k→8192/16384/16384、27.3k→16384/0(確率的 miss)/24576）。Kataribe の安定プレフィックス ≈6〜7k は床未満ゆえ 0、Fuseforks は 29k 超で当たっていた = **取得方法でなく床**。割引 ≈0.22× だが 8k ターンは非キャッシュでも ≈$0.001 なので**コードでは対処しない**（詰め物は smell、大盤面・長セッションは自然に越える）。`failures.md #80`。**✅Meta も Responses 口へ + キャッシュ警告の床 (2026-08-21、ユーザー報告「muse-spark/Perplexity のキャッシュが取れず警告が不安」起点 = failures #82)**: ①`api.meta.ai` をホスト自動判定で responses へ (実利 = 400→Auto 降格ダンスの解消・effort・usage 詳細)。**キャッシュは同日の追加実測で「口を問わず不安定」と訂正** — 当初「Responses 口は確実 (1073/3889)」と書いたが、それは**同一リクエスト再送の n=2** で、実プレイ形 (固定プレフィックス+可変 user) は **0/9**・同一再送でも 2/4 (ユーザー実プレイ turn 6 の警告発火が正しい観測だった。8/9 の互換口 n=2 過剰一般化と同じ轍 = キャッシュの live 検証は可変部を変えた列で測る)。ゆえに Meta は cache_floor = MAX (miss を数えない = 警告対象外、ヒットすれば計測に載る)。`responses::Flavor` 二値 (OpenAi=Perplexity 従来 / Meta=tool_choice 欄ごと不送出 + schema を prompt 末尾 system へ + effort max→xhigh 丸め)。tool_choice の 400→Auto 降格ダンス (セッション毎 2 往復) も口ごと消えた。api.llama.com は未測ゆえ互換のまま。②`CacheStat.floor` — キャッシュ最小プレフィックス未満の prompt は miss として**数えない** (Perplexity 8192 #80 / Anthropic 4096 #44 / 他 0)。構造的に不可能な条件で鳴る警告は真の警告の信頼を削る。frontend 無改修 (警告条件は consecutive_misses のまま)。PoC 3 本 (detect / Meta flavor encode / 床の計数則) + live 1 本 (`meta_responses_round_trip_with_cache`、ignored)。
- ✅ **画像生成 / 挿絵 — spec 24 (2026-08-20 起草→同日査読 rev2→Phase 0〜D 同日実装 = Done。ComfyUI・GUI ともユーザー実機 Green、**v0.5.13 で配布済み**)**: 物語に合わせた一枚絵を**背景/CG の上・文字の下の第三の画像層**に重ねる。先行 2 本 (SAIVerse = Nano Banana/ChatGPT 画像、alslime = ComfyUI) の和集合 = **OpenAI Images / Gemini (Nano Banana) / ComfyUI** が v1 プロバイダ。**提示層のみ** — gm_core は `Scenario.image_style` (作者の画風指針・500 字 lint `ImageStyleTooLong`・package.yaml から注入) 1 フィールドだけ、正本・語り・prompt caching 無風。**HTTP は全部 backend** (`app/src-tauri/src/image_gen.rs`: encode/decode 純関数 + HTTP ドライバ。CSP の connect-src は localhost のみ・クラウドのキーを WebView に置かない・ComfyUI も backend から = リモートでも CSP 対象外)、表示は data URL (**文字列なので差し替えれば GC される**。revoke が要るのは ObjectURL の側 = 採らない理由)。**プロンプト書きは GM クライアントの `generate` 1 回** (`harness::build_image_prompt_request`、あらすじ/エピローグと同型): 素材は**全てプレイヤー既視のもの** (直前の語り 1200 字 / `Location.description` = 開幕ログの文、GM 向け隠し説明は Location に存在しない / presence の profile = 主人公先頭・NPC id 順・上位 3 名 400 字・以降 120 字 / world 600 字 / image_style / ユーザー接頭辞、合計 2000 字) で `state_brief`・`hidden_*`・`secret_*` は構造的に入らない (PoC 固定) = **挿絵書きは GM ではない**。様式 tags|prose の既定はプロバイダに倒す (openai/gemini=prose, comfy=tags)。**probe で確定した癖**: OpenAI `quality` で消費 16 倍 (low 272 tok / high 4160) → `low` 既定 / Gemini は `v1beta` 必須・`imageConfig{aspectRatio,imageSize}`・**ブロックは 200+空** (`promptFeedback.blockReason` / `finishReason=SAFETY` を `Blocked{reason}` に、#61 の写し) / ComfyUI は API 形式ワークフローを設定に貼り `%prompt%/%negative%/%seed%/%width%/%height%` を **`serde_json::Value` を歩いて型を保って置換** (数値欄はクォート無し・未置換は既定値で埋める)・1 秒ポーリング (発行直後の 404 は正常)・`node_errors`/`status_str=error` を `ComfyNodeError` に。**サイズは UI 1 軸 (形 ×解像度段) + プロバイダ別写像表**。**生成は手動・都度・何度でも** (処理中は無効)、**揮発** (セーブに入れない・新規/ロード/遷移で消す・**受理ターンでは消さない**=プレイヤーが消すまで残す) + **レース防御二層** (`GameSession.scene_seq` を新規/ロード/遷移で進め backend が発行時の世代と比べて不一致なら破棄 / frontend は requestId)。**ロックは二度に分けて取る** (素材と GM 設定を写す → ロック無しでプロンプト書きと画像 HTTP → 再ロックで世代一致なら原本 `last_image` を置く) = 生成の間ターンが止まらない。**保存**: `{日時}_{slug}_T{GameState.turn}.png` を設定フォルダ (既定 `app_data/images`・ログと同じ流儀) へ、slug は ASCII 化・空なら (日本語名) タイトルの FNV 8 桁・パス要素拒否。**設定は 2 ソース** (非秘密 = localStorage `kataribe.imageGen` / API キー = `app_data/.env` の `IMAGE_API_KEY_OPENAI|GEMINI`)。**GUI**: 設定「画像生成」タブ (有効化・プロバイダ・URL・モデル・キー・形/解像度/様式・接頭辞・ネガ (ComfyUI のみ、他は欄を無効化)・ワークフロー + 汎用挿入・タイムアウト・不透明度・接続テスト・保存フォルダ) / 会話ペイン右下のホバー列 `ImageControls` (生成・保存・挿絵トグル・文字トグル = `invisible pointer-events-none`、TTS と同居時は左へ) / 第三層は `contain` + 不透明度 0.85 + **層自体に暗幕 0.35** (main の暗幕は背景にしか掛からず白文字とのコントラストが飛ぶ)。**作者ゲートは作らない** (挿絵は語りに触れないプレイヤー側の鑑賞物で押すのもプレイヤー。対比先だった `use_tts` も ✅2026-08-31 に同じ理由で撤去)。**Phase D 実測**: OpenAI (low・24.8 秒) / Gemini (1K・11.9 秒) とも実ワイヤ 1 枚ずつ Green で水彩・被写体・形が反映、プロンプト書きは lakeside_manor + Perplexity で 3 通りとも語りに無い人物なし・接頭辞反映・前置きなし。PoC: app 7 本 (写像/decode ×2/置換/history/保存名/base64) + harness 4 本 (素材と予算/秘密不在/予算/lint+注入)。設計は `specs/24_image_generation.md`、契約は data_contract `ImageGeneration` 節、outcast `package_spec.md` に `image_style` 節 (✅2026-08-20 ユーザーがサイトへ差し替え済み)。
- ✅ **設定画集 / 参照画像 — spec 25 (2026-08-20 起草→同日査読 rev2→Phase 0〜C 同日実装 = Done。実機 Green・**v0.5.13 で配布済み**。残 = outcast 側の書庫審査のみ)**: spec 24 が v2 に外した「キャラ一貫性」。ユーザー査読で **entity ごとの参照ファイル・プレイヤー側フォルダ・作者フィールドを撤回**し、**パッケージの `images/settings_sheets/` に置いた画像 (png/jpg/webp、大文字小文字不問、4MB 以下) をファイル名順で先頭 3 枚**、参照画像として添付するだけに単純化 (1 枚に複数人の立ち絵と名前・背景は分割して 1 枚、という設定資料集の形で作者/プレイヤーが作る = **誰の参照かを Kataribe は知らなくてよい**、engine 無改修)。`harness::pick_settings_sheets` は純関数で `Skipped{oversize|unsupported|over_limit}` を返し、トーストは呼び出し側 / `MAX_REFS` はプロバイダ別定数 (v1 は 3。gemini-2.5-flash-image の公式上限) / プロンプト書きは `refs>0` のときだけ system 末尾に箇条を足し、**0 枚なら spec 24 と byte 一致** (PoC)。**←2026-08-23 改訂 (failures #85)**: 当初は「プロンプト内で `(as in the reference sheets)` と指せ」と書かせていたが、**画像モデルがその語を被写体の指示と読み資料集のような分割画像を作った** (実機) — 参照は添付そのもので伝わるのでテキストで指す必要が無い。現行は対の 2 箇条 (見た目を具体語で書け + **参照を指す語は書くな** / **ひとつながりの 1 場面として描け** = 分割・コマ割り・並置・枠線を作らない)。運び方: Gemini = `inlineData` をテキストの前置 / OpenAI = 参照ありのときだけ `POST /v1/images/edits` (multipart `image[]` ×N, prompt, model, size, quality, n。**`input_fidelity` は送らない**)。**未決 1 は実測で決着 (2026-08-21)**: 既定モデル `gpt-image-1-mini` は `high` を **400 `invalid_input_fidelity_model`** で拒み (`low` は無指定と usage 完全一致 = 既定は low)、`gpt-image-1` では通るが**画像入力トークン 323→6531 = 20.2×** (1 枚 $0.019→$0.081 @quality=low)。見た目は **`quality=low` だと逆効果** (衣装の細部は残るが顔が崩れ砂目が出る) で、`medium` で撮り直すと崩れは消え high 側だけが参照の衣装細部と髪色を保った = **粗さは fidelity でなく出力品質側の artifact**。Kataribe の既定は `Detail::Standard = low` ゆえ既定では害が出る。**ゆえに既定にしない** — 一律送出は #76/#77 と同じ「モデルによって欄の有無が違う」族で、既定モデルで箱から出してすぐ全滅する。効くのは「上位モデル + medium 以上 + キャラ一貫性が主目的」の三点が揃うときだけで、opt-in (設定 1 チェック + 400 で自動降格 latch = `tool_mode_downgrade` と同型) は道として残すが v1 は送らない。接地の限界: トークン数・400・`low`==無指定 は wire の決定論的事実だが、**見た目の優劣は各アーム n=1・判定者は Neo** (Images API に seed が無く差にサンプリングの運が混じる) / ComfyUI = `/upload/image` の返名を `%ref_1..3%` に。**足りない分の `%ref_n%` を持つノードはノードごと外し辺を切る** (`comfy_prune_unfilled_refs`、✅2026-08-22 改訂 — rev2 の「残す・無効化はワークフロー側の責務」は画集の枚数ごとにワークフローを貼り替えろという意味で誰もしない = ユーザー報告「参照がほとんど意味をなしていない」の真因。空埋めは LoadImage の File not found を生むので剪定が唯一の形。ワークフロー 1 本が画集 0〜3 枚を受ける)。**同日 LAN 実機で Krea 2 を実測**: ComfyUI 本体が Krea 2 の参照をネイティブ実装 (`model_base.Krea2` の `reference_latents`、Kontext 同型) しており、core の `TextEncodeQwenImageEditPlus` (clip+vae+image1..3) だけで **LoRA 無しの turbo でも識別性が保たれる** (同 seed: 参照あり=銀髪/緑目/赤マフラー/紺×金鎧を保持、なし=金髪ロング/青目/銀鎧に化ける。n=1 目視)。副作用 = 参照の無地背景が構図に漏れ枠つきビネットになる → `refs_rule` に「無地背景・余白・枠・文字を持ち込まず場面の背景を全面に」を追加。同梱テンプレート `comfy_krea2_ref.json` (設定タブ「参照画像つき (Krea 2 Turbo) を挿入」) + 警告「`%ref_1%` が無い = 送られても使われない」。**←2026-08-26 に同梱テンプレートは撤去** (汎用 1 本だけに。警告は 3 本へ拡張 = spec 27 節)。PoC +1 (app) / harness 既存テストに assert 追加。設定タブに「今の盤面で見つかった設定画集」(✓ 名前 KB / ✗ 理由) + フォルダを開く。**Phase C 実測**: 参照 1 枚で Gemini (13.4 秒) は部屋ごと同じ男を、OpenAI edits (23.8 秒、`image[]` が 200 = キー名の接地確定) も同じ男と室内要素と画風を保持。**書庫の審査 (ユーザー決定)**: `images/settings_sheets/` は webp のみ・1 枚 4MB 以下・3 枚まで (中身は問わない) — outcast 側の実装が残。package_spec.md には書かない (ユーザー決定)。PoC: harness 3 本 (選別/IO/規律の byte 一致) + app 1 本 (3 プロバイダの運び方)。設計は `specs/25_character_reference_images.md`、契約は data_contract `ImageGeneration.settings_sheets`。**←2026-08-23 spec 27 で改訂**: パッケージの `images/settings_sheets/` は**種**になり、生成時に読むのは**セッションフォルダ** (`app_data/refs/<FNV>/`) の現物だけ。ここに書いた「パッケージに置いた画像をファイル名順で先頭 3 枚」はプレイ開始時の**写し取り**の規則として生きており、送信時の順序は `ref{n}` の index 昇順に変わった。
- ✅ **画像生成設定のプロバイダ別プロファイル / spec 26 (2026-08-21 起草→同日査読 rev2/rev3→同日実装 = Done。GUI 切替往復の目視残)**: 実測 2 件が動機 — ①Gemini→ComfyUI 切替で baseUrl が漏れ `/prompt` が Google に飛んだ (旧ヒューリスティック「既定値なら差し替え・カスタムなら温存」は `/v1beta` 付きカスタム URL を温存 = **カスタム温存は同一プロバイダ内でしか正しくない**) ②様式既定 comfy=tags が写実モデル (Krea2) に danbooru 語彙を当て、seed 固定 A/B で **`1boy` が三十代の主人公を子供に変えた** (タグの適否はプロバイダでなく奥のモデル族で決まり、Kataribe に判別材料が無い)。**解**: frontend localStorage を共有部 (enabled/shape/detail/userPrefix/opacity/folder) + **プロバイダ別スロット** `perProvider[provider]` (baseUrl/model/style/negative/workflowJson/timeoutSecs) に分離、切替は表示スロットの切替だけ (ヒューリスティック撤去・値は失われない・戻れば戻る)。**`toBackendConfig` の写像を凍結**: 他プロバイダのスロットは一切読まない / negative・workflowJson は comfy 以外で送らない (逆向き漏れの封鎖) / comfy の model は空を許容 / timeout 0 は null = backend 既定 `ImageGenConfig::timeout()` へ (**既定の置き場の線引き** = base_url/model は UI placeholder が表示に使うので frontend・timeout は表示に関与しないので backend 単一定義)。旧フラット形は初回ロードで現 provider のスロットのみへ一方向移行 (他スロットは既定・**明示 style も複製しない** = 現プロバイダの実効値は従来一致・他は既定に初期化 → フラット器由来の漏れはこの時点で解消 = **意図的な挙動変化であって無変化を装わない**)、部分欠損スロットは既定埋め (`?? EMPTY_SLOT` ガードと対)。backend `ImageGenConfig`/command は無改修。**付随改善 (prompt 層・harness)**: tags 形式指示に**年齢・体格タグ必須** (adult man 等) を追記 — writer は単発 generate ゆえキャッシュ無風・prose は不変。`userPrefix` は共有のまま散文推奨を placeholder へ (タグの年齢バイアスは言い換え後も残りうる)。査読 rev2 (3+3 点)・rev3 (4 点、「挙動変化ゼロ」の自己矛盾指摘含む) は spec の反映記録に凍結。PoC: harness 1 本 (Red→Green) + frontend 使い捨て 13 assertion (動機 1 を旧コードで Red 再現→新コードで Green)。設計は `specs/26_image_provider_profiles.md`、契約は data_contract `ImageGeneration.config_sources` / `prompt_writer`。
- ✅ **参照ストックとプロンプト工房 / spec 27 (2026-08-23 起草→同日査読 11 点→rev2→UI 3 点ユーザー決定→Phase A〜D + 追補 2 件→目視 Green 2026-08-24→**2026-08-26 に seed 固定 A/B の成立条件を実機で確定 = Done**)**: 挿絵の**入力**をプレイヤーの手に渡す。動機 3 つ — ①参照画像が作者の同梱物に固定されて育たない (spec 25 は UI 登録を意図的にスコープ外にした) ②スタイル指定が LLM に言い換えられる (`user_prefix` は最終プロンプトに入らずプロンプト書きの素材どまり = ComfyUI にタグ列を渡しても原文が保たれない) ③seed が乱数で A/B が成立しない。**✅Phase A**: **`images/settings_sheets/` は種であって参照先ではなくなる** — ゲーム開始後に効くのは `app_data/refs/<パッケージパスの FNV>/ref{1..3}.*` の現物だけ (`initial_state` の seed と同型)。**写し取りはフォルダ不在時のみ**・判定は**フォルダの存在だけ** (ファイル数を見ない = 全削除でも初期化済み・マーカーという第二の真実源を持たない)・package を再読するのは「入れ直す」だけ。**紐づきはパッケージ単位** (プレイスルー単位 = セーブ連動にするとロードのたびに育てた参照が巻き戻る)。**前詰め不変条件** (`ref1..k` が常に前詰め・削除は後続を前へリネーム) で **UI 番号 = ファイル名番号 = ワイヤ番号 `%ref_n%` が恒等的に一致**し、査読が「表示を足して埋めろ」と言った乖離を**乖離ごと消した**。**実装時に見つけた穴**: 送る列を当初案 (harness の読み取りをディレクトリ引数へ一般化 = **ファイル名順**) で読むと、設定タブに「フォルダを開く」がある以上ユーザーが手で置く `aaa.png` が `ref1.png` より前に並んで `%ref_1%` を奪い、削除は `ref1.*` を消すので**画面と送信内容が恒久的にずれる** → `ref_stock::load_refs` が **index 昇順**で読む形に変え (harness の一般化は差し戻し = 使われない汎用関数を残さない)、手置きは **`Unrecognized`** として理由つきで一覧に出す (沈黙を作らない代償が理由コード 1 個なら安い)。上限は二層 + 合計 (種 4MB = 配布作法 / プレイヤー挿入 8MB = 配布されないので作法非適用 / 送信合計 12MB = **Gemini の inline は base64 で 4/3 に膨らむので 1 枚の上限を上げただけだと静かな 400**)。セーブ無改修・出所メタ無し・表示は `asset://`。**✅Phase B (プロンプト工房の backend + 設定)**: 最終プロンプト = **スタイル原文 (LLM を通さない) + プロンプト書きの出力**の合成 (`harness::compose_image_prompt`、純関数)。**スタイルは LLM から隠さない** — 隠すと「水彩」の横で `photorealistic` と書いて 1 本のプロンプトが自己矛盾するので、素材節には要旨 (300 字) を「**前置き済みなので重複させるな・矛盾させるな**」の役割で載せる (**予算には算入したまま切る** — 査読の「予算から除外」案は入力の有界性を壊すので差し戻した)。`direction` (この一枚への指示・300 字・**揮発**＝localStorage に入れない) は LLM 経由 (場面依存の意図は語りと合成が要る)。**`prompt_override` はプロンプト書きを呼ばず verbatim で送る** (手書きが最終形＝待ち時間と課金も増やさない)。`GeneratedImageView.prompt` は**最終送信文字列**になった (従来は LLM の出力だけで「送られた文」を示していなかった)。**seed 固定** (`lock_seed`/`seed`、設定タブ) は comfy のときだけ有効表示かつ**wire にも comfy 以外へ送らない** (spec 26 の写像規則と同型) — これが入って初めて参照やプロンプトの A/B が成立する。**✅Phase C (`CodeEditor.vue` = CodeMirror 6 の汎用コンポーネント)**: `language` prop (text/json) + Compartment でテーマ・読み取り専用を差し替え、配色は CSS 変数を引くのでテーマ切替に追従 (`dark` フラグだけは Compartment で差し替え = ライブラリ内に `var()` で書けない既定があるため)。**動的 import だけにした** — 静的だと main bundle が 326KB→733KB (gz 105→238KB) になり**設定を一度も開かないセッションでも起動時に解析される** (TTS の `@aituber-onair/voice` と同じ判断。結果 main 333KB / CodeEditor 406KB の別 chunk)。まず**ワークフロー JSON 欄**へ適用 (数百行を貼り構文エラーが黙って通り `%ref_1%` を目で探す欄 = `lang-json`+`lint` が効く唯一の場所)。プロンプト欄は `text` (文法の無い文章に赤線は誤警告)。**将来のパッケージ YAML エディタ構想の器**でもあり、そちらは spec 17 の `tree_hash` と衝突するので**編集したらフォーク = 別パッケージ扱い**の方針だけ先に決めた。**✅Phase D (ダイアログ 2 枚)**: 会話ペイン右下のホバー列に 2 ボタン追加 → `RefStockDialog` (枠は `MAX_REFS` から生成・サムネイルは `asset://`・**押せる空き枠は末尾の 1 つだけ** = 前詰めを UI 側でも壊さない・**画像が無くても開ける** = 削除と入れ直しは挿絵の有無と無関係・省略理由も表示) / `PromptDialog` (上段=**最終送信文字列**を CodeEditor で編集し「そのまま生成」で verbatim 送出・下段=この一枚への指示)。どちらも動的 import。**✅追補 (2026-08-24 ユーザー要望 2 件)**: ①**ローカルファイルからの取り込み** (クリック/ドロップ) — **変換は WebView で** (`canvas.toBlob("image/webp")` = Chromium ネイティブ・長辺 1536・q0.88。`image` crate も libwebp も足さない)、送りは **raw body** (`invoke(cmd, Uint8Array, {headers})` → `tauri::ipc::Request`。JSON の number[] は 8MB で数千万要素)、**mime は backend が先頭バイトで嗅ぎ分け申告を信じない** (`sniff_mime`) ②**浮遊パネル** `FloatingPanel.vue` — モーダルの箱を捨て、会話ペイン右下に半透明のガラスとして置く。**幕を張らない**のが核 (挿絵や参照は「いま出ている絵を見ながら」触るもので、暗幕で覆うと比べる相手が消える)。参照の枠は机に置いた写真 (傾き・hover で正す・操作は hover で浮き出る)。**✅目視 Green (2026-08-24 ユーザー実測)**: 見た目 OK・サムネイル表示 OK (= `?v=` は asset protocol を素通り、#86 の未確認が解消)・**圧縮実測 1.3MB → 36KB**。追補 FB「入れ直すに警告が欲しい (フォルダを開くと間違えて押しそう)」→ `askConfirm` を挟んだ (不可逆で押した瞬間の画面上の異変が小さい操作には確認 = シードリセットと同じ線。削除は 1 枚ずつ・即座に見えるので付けない)。**✅seed 固定 A/B の成立条件を実機で確定 (2026-08-26、ユーザー報告「seed 値固定でも同じ絵にならない、内部でランダムにしてるのかね」起点)**: **止まっていない変数がもう一つあった — プロンプト**。`lock_seed` が固定するのは**サンプリングの運**だけで、`prompt_override` が空なら生成のたびにプロンプト書きの LLM 呼び出しが走り出力が毎回違う。**切り分けはユーザーが ComfyUI を直に叩いて済ませた** — 同 seed (12345) + 同プロンプトで Kataribe と同一出力が再現し、`%seed%` は正しく届いている・ComfyUI は決定論、が同時に取れて候補 2 つ (ワークフローに `%seed%` が無い = #83 同型の沈黙 / プロバイダが comfy でない = wire に載らない) が落ちた。**A/B の形**: 1 枚生成 → 工房を開く (上段には**その時の最終送信文字列**が既に入っている) → 「そのまま生成」(= `prompt_override`) → 以降**参照だけ**差し替える。seed 固定と `prompt_override` は**対で初めて A/B が立つ**——同じ Phase B に入れたのはそのためだったのに依存関係をどこにも書いていなかったので実際に踏んだ (`lockSeedHint` と specs/27 に明文化)。**分割と灰色は Krea 2 の性質 (n=2)**: 分割・コマ割りは ComfyUI 側の prompt/negative で防げるが、**参照の無地背景が灰色の帯として残るのは消えない** (2026-08-22 LAN 実機 n=1 の予測がそのまま持った)。理由は機構 — `reference_latents` は Kontext 同型でトークン連結するのでモデルにとって参照は画風の見本でなく**画面に入れる素材**で、その背景も素材に含まれる。**ここから出る Kataribe 側のコード変更は無い** (作者の調整の領分)。**ただし押し返す唯一のレバーである negative の既定値が空だった** → comfy スロットに既定値を焼いた (`COMFY_DEFAULT_NEGATIVE`。2026-08-20 に AI モデル欄の焼き込み既定を撤去したのとは**逆向きの判断** = あちらは値を入れると誤解と意図しない課金が生まれる欄、こちらは空でも普通に動いてしまい出た絵が悪いことしか手がかりが無い。適用は**新規スロットだけ**)。**同梱テンプレートの撤去 (ユーザー決定)**: 参照つき `comfy_krea2_ref.json` を撤去し同梱は**汎用 1 本** (`comfy_generic.json`) に。理由 = ComfyUI を用意できる人は自分で絵柄を作るし使うモデルも違う (SD 系・Z1)、かつ同梱すると**モデルの入手までサポート対象に見える**。撤去で「置換語の入れ方の実例」が消えるので**警告を 3 本に揃えた** (`%ref_1%` / `%seed%` / `%negative%`)。**どれも落ちる値が実際に在るときだけ鳴らす** (画集 0 枚・seed 非固定・negative 空欄なら差し込み先が無くても何も失われず、鳴らせば誤警告 = `workflowNoRefs` と同じ規則)。この日の切り分けで `%seed%` と `%negative%` の不在が二度候補に挙がり、どちらも警告が無かったのが動機。フィクスチャは `app/src-tauri/src/testdata/comfy_ref_workflow.json` に残り剪定の live テストが使う。**✅追補 (2026-08-26 ユーザー要望 3 件)**: ①**顔アイコンを参照ストックへドラッグ** — spec 25 が意図的に捨てた「キャラごとの参照」を、機械に推測させず**人が手で置く**形で取り戻した (ユーザーの言「以前に自動で無理にやろうとしていたことを手動でユーザーにやらせる」)。運ぶのは**アセット ID だけ** (`put_reference_from_asset` が `resolve_asset` で解決して読む = bytes は IPC に乗らない・ID 検証は既存規則がそのまま担う・mime は先頭バイトの嗅ぎ分けなので `.svg` アイコンは弾かれる)。**spec 25 の「entity 照合はしない」は不変**。**←2026-08-28 にドラッグ元を一つ増やした (ユーザー要望)**: マップタブの場所の絵も同じ経路で枠へ落とせる (`MapNode.image` は同じ不透明アセット ID なので frontend の `dragstart` を 1 本足すだけ・backend 無改修)。動機は「参照 = キャラの見た目」に限らないこと — 場所の絵を参照にすれば背景や画風の指定になる。未踏 (frontier) の場所は backend が image を伏せるので掴めない。②**ダイアログのボタンをトグル + 排他へ** (押し直しで閉じる・開いている側は熾火で灯る。浮遊パネルは 2 枚とも同じ位置に出るので、両方 ON にできると 1 枚しか見えないのにボタンは 2 つとも「開いている」と映り状態表示が嘘になる)。③設定タブの並びを「最初に決めるもの → 演出 → 道具」へ (表示/言語/AIモデル/グラフィック/画像生成/サウンド/ログ/開発者/ヘルプ) + **AI モデルの保存を 2 種**へ (`.env` だけ / `.env` と選択中の登録モデル両方。従来は API キーを変えるのに一度消して作り直すしかなかった。**.env を先に書き成功時だけ登録簿を書き換える**・id と表示名は保つので**あらすじ要約用の紐づきが切れず**、その登録が要約用なら要約側の env も追随させる = GM だけ直って要約が古いキーで静かに失敗し続けるのを防ぐ)。**実装で最初に詰まったのは自分のコードではなかった (failures #88)**: `tauri.conf.json` の `dragDropEnabled` が**未指定＝既定 true** で、Tauri が登録する OS 側 drop ハンドラがページ内の HTML5 ドラッグを横取りしていた。ネイティブの drag-drop イベントは一箇所も使っていない (grep でゼロ) ので切って失うものは無い。**予測が当たって機序も確認できた** — この設定が原因なら 2026-08-24 のローカルファイルドロップも一度も動いていなかったはずで、実機で両方いっぺんに動いた。一般化: **プラットフォームが親切のために既定で有効化している機構は、使っていなくても上位の同種機能を奪う。判定は安い — その機構に依存するコードが 1 行でもあるか grep し、ゼロなら切る**。設計は `specs/27_reference_stock_and_prompt_workshop.md`、契約は data_contract `ImageGeneration.reference_stock`。
  - **Gemini 明示キャッシュ / spec 13 (✅Phase A+B、2026-07-15)**: 暗黙キャッシュの総プロンプト ~8000 トークン崖 (#54) を**明示 cachedContent** で迂回。静的プレフィックス (systemInstruction+tools) を `POST /v1beta/cachedContents` で pin し、generateContent は `cachedContent` 参照で可変 contents だけ送る (Anthropic cache_control #44 の Gemini 双対)。`LlmClient.gemini_cache: Mutex<Option<CacheHandle{name,fingerprint}>>` を session 単位保持、`decide_cache_action`(無効/サイズゲート未満→Bypass / fingerprint 一致→Reuse / 別 scenario→Create) で reuse/再作成、失効 (Api 403/404) は handle クリア+full 再試行。**fallback 徹底**で turn を絶対落とさない (最適化と正しさの分離=正本に触れない)。`fingerprint`=model+先頭 system+tools の FNV-1a (可変 user 除外)、cache 中身は `encode_with_cache(None)` の抽出再利用で inline と乖離ゼロ。**lock は await を跨がない** (判定だけ lock 内で即 drop)。env=`LLM_GEMINI_CACHE`(既定on)/`_TTL`(900)/`_MIN_CHARS`(4000)、計測は cachedContentTokenCount 流用 (#44/#45 が改修ゼロで効く)。**Phase A〜D 完了 (Done)**: A (encode 分割+fingerprint・回帰ゼロ) / B (create/参照/lifecycle・純ロジック PoC) / C (台帳) / **D (実キー live 検証で目標達成)** — sun_girl_ntr (総プロンプト 8000 超) で**全ターン cachedContentTokenCount=7173** (暗黙のみだった Phase B では turn2 以降 0=崖)。エイリアス `gemini-flash-latest` は cachedContents create に受理 (具体版解決不要)、明示は静的プレフィックス全体 7173 をキャッシュ (暗黙の GM_SYSTEM 部分 4073 より多い)。**Phase D で D1 のバグを初回 live で捕捉・修正** (failures #55): Gemini は cachedContent 参照時に generateContent 側の system_instruction/tools/**tool_config** のいずれかがあると 400 → tool_config も cache 側へ移した (「静的は全部 cache・request には可変分だけ」)。llm_client 40→44、clippy clean、app backend green。契約は data_contract `gemini_explicit_cache`、設計は `specs/13_gemini_explicit_cache.md`。
  - **入力キャッシュの最大化 / spec 14 (✅Phase A〜C、2026-07-16、live 実測残)**: TRPG のコストは入力支配（1 ターン=フルプロンプト再課金）で、system 側レバーは使い切り済 → **残る成長項 = append-only あらすじ**（spec 10、章追加の間 byte 安定なのに可変 user の中にあってキャッシュに乗らなかった）を第二のキャッシュ段へ。**メッセージ構成を凍結**: turn.rs が `[system(静的), system(synopsis?), user(可変)]` — synopsis を独立した 2 本目の leading system に分離（空なら出さない、提示位置は「history の前」→「state の前」・role user→system = 未決2 の behavior 検証は Phase D）。**adapter 規約**: Anthropic = leading system 毎に cache_control（cap 4。1 本は従来同数=回帰なし、章追加ターンは第二段だけ失効し静的段は生存）/ **Gemini = 1 本目のみ pin（D4）** — encode/fingerprint/サイズゲートとも 2 本目 (synopsis) を除外し inline contents (user 降格) 非キャッシュ（章追加毎の cachedContent 再 pin churn を構造的に防ぐ）/ OpenAI・Grok = 自動延伸（並べ替えの副産物、章追加 dip は同型）。**観測 (D5)**: hit rate 定義凍結（cached=cache_read のみ・cache_creation は含めない/prompt=input_tokens）、`CacheStat` に累積 hit_tokens/total_tokens + 直近 32 件 `CachePoint` リングバッファ（有界）、`LLM_CACHE_DEBUG=1` で機械可読行 `[LLM_CACHE_STAT] req= input= cache_read= ratio= cum_ratio=`（曲線をログ grep で再構成）。gm_core/CLI/正本は無改修。A→B の順序凍結（B 先行は静的 breakpoint が消え現状より悪化）どおり実装。PoC 5 本 Red→Green（多段 cap4/Gemini 除外/2 本目 system 分離/計数則+累積/有界リング）。TS `CacheStatView` 追従（メーター実装は Phase E 任意）。**Phase D: Grok 分は同日 live Green (2026-07-16 ユーザー実測 40 リクエスト・~39T)** — 章圧縮のたび cached が 6144→6400→6656 の +256/章 階段 (synopsis が自動プレフィックスキャッシュに乗った)・章追加直後は旧プレフィックス分だけヒット→1〜2 リクエストで全量回復 (D3 の dip 予測どおり)・圧縮後は prompt 自体も縮む (spec 10 の注入経済)。副産物 = xAI キャッシュの確率的 miss の解明 (failures #57、TTL 説棄却)。`[LLM_CACHE_STAT]` に `t=`(unix 秒・間隔相関の弁別) と `conv=`(summary client 系列の弁別) を追加。**Anthropic 対照も同日 live Green (53 リクエスト・~50T)** — 通常ターン miss ゼロ (Grok の確率 ~11% と対照=決定論、260s 間隔もヒット=TTL 5min 読取更新)・cache_read が 8978→9359→9650→9978 の階段 (多段 breakpoint で synopsis 第二段が読まれた)・章追加ターンは静的分のみ読み+synopsis ブロック全体再書込 (breakpoint 単位 exact 一致、write=381/672/1000、予算 2000 字で有界=無視可)。**副産物 #58**: Anthropic native の input_tokens は非キャッシュ分のみ (総入力ではない) → ratio=6.88 で発覚、anthropic::decode が canonical prompt を総入力へ正規化 (Red→Green)。正規化後の累積 hit rate: **Anthropic 71.0% / Grok 60.6%**。**Gemini 対照も live Green (25 リクエスト・73 分中断込み)** — D4 実証: 章圧縮後も cached=5630 のまま**再 pin なし** (fingerprint=1 本目のみが効いた)・synopsis は inline (+214)。**副産物 #59**: Gemini cachedContent の TTL 900s は**作成時刻から固定・読取で更新されない** (Anthropic と真逆) → 連続プレイでも ~15 分周期で失効→fallback→再 pin (ターンは落ちない・暗黙が静的の ~72% を拾う=正常な鼓動)。**3 プロバイダの累積 hit rate: Gemini 72.6% / Anthropic 71.0% / Grok 60.6%**。**✅未決2 = synopsis system role 化の behavior 検証 (2026-07-18 ユーザー実プレイ)**: 章圧縮後にあらすじ限定の出来事をプローブ — Anthropic=整合+良好 (role 化の真の陽性)・Gemini=良好 (D4 user 降格ゆえ対照群=spec 10 機構の検証)・Grok=想起は整合するが**過去章の過強調** (求められないターンでも織り込みすぎ) → `synopsis_note` に salience 規律を対で追加 (抑止「背景であって主題ではない・求められない限り自発的に回想・引用しない」+ 保護「尋ねられたら正確に参照」= 検証済みの想起を殺さない、#41 の区別の適用。failures #60、PoC Red→Green)。退路 D2 拡張は不要と確定。**Phase D 残 = OpenAI 互換対照 (任意)**。候補: GUI キャッシュ警告の閾値再調整 (Grok warm-up 3 連 miss で開幕誤発火しうる → 3→5 か「一度 hit 後に武装」) / Gemini の先回り再 pin (created_at + ttl−margin、優先度低)。契約は data_contract `input_cache_maximization`、設計は `specs/14_input_cache_maximization.md`。
  - **キャッシュ健全性警告 (✅2026-07-11)**: #44/#45 の「キャッシュの静かな漏出は usage が一次ソース」を GUI へ引き上げた。`CacheStat{last_cache_read, consecutive_misses, total_requests}` を `LlmClient` が**両経路**（互換 `cached_tokens` / ネイティブ `cache_read_input_tokens`）の全リクエストで常時記録（`Mutex` interior mutability、`cache_stat()` で clone スナップショット）→ app `TurnView.cache`（Accepted/Rejected 両分岐）→ frontend が **total>=2 かつ連続 miss>=3** で会話ログに ⚠ 1 行（エッジトリガー latch・ヒット復帰で再武装・新規ゲームでリセット。キャッシュ非対応プロバイダ/ローカルモデル向けに「正常です」注記併記）。初回リクエストは書き込みゆえ miss が正常＝total で除外。作者が provider 判定外れ等の設定ミスにプレイ中に気づける。PoC 2 本（計数則 / 零値スナップショット）。**実機目視 (✅2026-07-11)**: `LLM_PROVIDER=openai` で意図的に互換経路へ落とし（=#44 の故障状態の再現、偽装不要）、cached=0 が 3 リクエスト連続 → GUI に ⚠ 表示を確認。このとき prompt 13k〜16k tokens ＝最小キャッシュ 4096 超なので「小プレフィックス偽陽性」ではなく真の miss を検知した。留意: 安定プレフィックス 4096 未満の小パッケージではネイティブ経路でも警告が出うる（観測されたら注記/閾値を再調整）。
  - **xAI Grok の sticky routing + 互換経路のキャッシュ計測 (✅2026-07-11, #45)**: xAI のキャッシュは自動だが**サーバ単位** — `x-grok-conv-id` ヘッダ無しだとロードバランサで散って同一プレフィックスでも miss (Kataribe は未送信だった=実質無効)。`LlmClient.conv_id`（生成時に一意、app=セッション毎/CLI=実行毎）を互換経路の全リクエストに送る（他サーバは未知ヘッダとして無視）。`ChatResponse.usage`（`prompt_tokens_details.cached_tokens`）をパースし `LLM_CACHE_DEBUG=1` で `[LLM_CACHE]` 行（#44 と同形、OpenAI/Gemini 互換の自動キャッシュもこれで見える）。~~Gemini の明示 `cached_content` は不要（2.5 系は暗黙キャッシュ自動）~~ **← 3.5-flash で falsify（2026-07-15、failures #54）**: gemini-flash-latest(=3.5-flash) の暗黙キャッシュは**総プロンプト ~8000 トークンで崖**（超えると cached=0）。制御実験（friday の world 段階パディング + user 側パディング）で「因果は total-prompt サイズ・sysInstr でも package 内容でもない」を単離。大シナリオ(sun_girl_ntr は turn1 から)/長セッション全般で不発。2.5-flash は 404 引退で対照不能。**明示 `cachedContent` が robust な解**（spec 12 Phase F/新 spec で起票）。PoC 2 本。
  - **Anthropic ネイティブ経路 + prompt caching (✅2026-07-11, Console コスト実測 #44)**: 全リクエストが input_no_cache だった真因は **OpenAI 互換層が prompt caching 非対応**(公式 docs 明記、cache_control 黙殺)。`LlmConfig.provider`(`LLM_PROVIDER`、未設定なら base_url に api.anthropic.com を含む時 anthropic へ**自動判定**＝受領者ゼロ設定)を新設し、Anthropic にはネイティブ `POST {base}/messages`(x-api-key + anthropic-version)を話す。先頭 system 群を system ブロック配列へ写し**末尾に cache_control: ephemeral を 1 個**(render 順 tools→system ゆえ emit_delta schema + GM_SYSTEM + scenario_brief の安定プレフィックス全体がキャッシュ対象。可変な user 側には置かない)。応答 tool_use は OpenAI 形へ写して `parse::extract` に合流(抽出経路は単一)。harness/app/CLI は無改修。ネイティブ経路は常に tool-use(use_tools 無視)。検証は `usage.cache_read_input_tokens > 0`(`LLM_CACHE_DEBUG=1` で stderr に `[LLM_CACHE]` 行)。留意: 最小キャッシュ 4096 tokens (opus-4-8)・TTL 5 分(読取で更新)・**system プロンプトに可変値を入れると全キャッシュ無効化**(現構造は可変値を全て user 側に分離済み、以後も守る)。PoC 5 本 (Red→Green)。実 LLM での cache_read>0 の実測は次プレイで確認。
  - **no-tools / JSON モード (2026-06-24, さくら AI Engine 対応)**: `tool_choice` 強制を**実装しないサーバ**(さくら AI Engine / ローカル OpenAI 互換) 向けに、`LlmConfig.use_tools`(`LLM_USE_TOOLS`、既定 true)で切替。off の時 tools/tool_choice を送らず、`json_instruction`(schemars schema を載せた「JSON だけ出せ」system 指示)を messages に積み、`extract` の content フォールバックで `StateDelta` 化。content が prose に包まれても balanced な `{...}` 抽出(`json_objects`)で救済。GM_SYSTEM の提出行を mode 非依存に。**実機実証**: さくら `gpt-oss-120b` + `LLM_USE_TOOLS=false` で通しプレイ成功(`failures.md #29`)。同人配布の北極星(受領者は tool 非対応の安い/ローカルモデルを使う)に直結。
  - **推論モデルの CoT 除去 (2026-06-25, Gemma4 実機発見)**: no-tools モードで Gemma 等が `<thought>...</thought>` に chain-of-thought を吐き、その中の JSON 断片(`{"op":...}`)とフェンス前置きで本体 JSON 抽出が壊れた(「parse 失敗: expected value at line 1 column 1」)。**二層で堅牢化**: (a) `parse::strip_reasoning_blocks` が `<think>`/`<thought>`/`<thinking>` を抽出前に除去、(b) content フォールバックを first`{`..last`}` から **string-aware な balanced 抽出 `json_objects` の最後の object** へ置換(`StateDelta` は serde(default) ゆえ空断片すら parse 成功する → 「答えは推論の後」原則で本体を拾う)。`#28` と同じ「推論モデルが構造化出力の周りにノイズを足す」系の後処理。`failures.md #30`。
  - **narration の漏れマークアップ掃除 (2026-06-24, 実プレイ発見)**: 強モデルでも稀に narration の**文字列値**へ tool-call の format token（`</narration>`/`<parameter name="ops">` 等）を漏らす（ops 配列は別フィールドで正常、tool_call は valid JSON ゆえ `extract` は素通り）。narration は非検証なので `parse::sanitize_narration` が `generate_delta` で提示前に掃除（先頭開きタグ剥がし＋構造マークアップ以降を切断）。`\n` 正規化(#16)と同じ「正本を汚さない後処理」。`failures.md #28`。
  - **Grok (xAI) tool-use 対応 (2026-06-28, 実機発見)**: Grok で `use_tools=true` だと「エラー無し・GM が動かない」→ 却下ループ→タイムアウト、と段階的に発覚。**三つの真因と修正**: ①Grok は schema を tool-call grammar にコンパイルして制約デコードするが `$ref`/`definitions`/`$schema` 非対応（xAI docs に記載なし）→ schemars の `#/definitions/StateOp` 参照を解決できず ops を制約できない→`StateDelta` の serde(default) で静かに空デルタ。**`inline_schema_defs`** で `$ref` を実体へ inline・メタキー除去し自己完結化（Anthropic は $ref 解決可ゆえ従来動作、Grok/OpenAI 厳格系は自己完結要）。②空デルタ解消後、Grok が **authored 専権 op（`set_presence` 等）を連発して全却下**（presence は物語で頻出ゆえ特に刺さる）→ **`filter_authored_only_ops`** が `gm_core::AUTHORED_ONLY_OPS` を ops の oneOf から除外＝LLM にそもそも提案させない（Grok の grammar にも出ない＝構造的遮断）。③却下理由が受理時に捨てられ見えなかった→`TurnOutcome::Accepted.rejected` で運び、GUI（「却下された試行」ブロック）/CLI（`✗ N回目却下`）に surface（author が「なぜ N 回かかったか」を診断できる）。診断装置 `LLM_DEBUG`（送信ボディ+生応答を stderr）と `LlmError::Http` の source 連鎖平坦化（"error sending request" の下の真因を出す）も追加。PoC: `schema_excludes_authored_only_ops` / `inline_schema_defs_resolves_refs_self_contained`。
- ✅ `crates/harness` GM ターンループ: 提案 → 裁定 → 却下なら理由を戻して再生成（`_self_repair_loop` 同型）→ 受理なら原子適用。`DeltaProposer` trait で依存性逆転し、`ScriptedProposer` で「却下→再生成」を実 API なしで実証。PoC green（一発合格・再生成・理由還流・最大試行で state 無傷・ダイス経路・prompt 健全性・外部キャラロード・memoria_bridge 結線）。`TurnOutcome::Accepted` は発火ビート(`fired`)も載せる。罠 4 件（#7-10）は `failures.md`。
- ✅ `crates/harness::memoria` **Memoria 脚 (memoria_bridge)**: 三権分立の「Memoria が覚える」。トリガー発火点で**伏線・キャラ性格を recall** し語りに注入する。**北極星の不変条件＝可変世界状態は絶対に持たない**（HP/所持品/フラグ/位置/数値は正本の専有）を**型で構造保証**: `MemoryFragment{id,tags,text}` は state フィールドを持てず、`Memoria::recall(&self)` は retrieval only。`DeltaProposer` と同型の依存性逆転 — `Memoria` trait の実装 `LoreStore` は**文字 bigram TF-IDF の cosine semantic recall**（依存ゼロ・決定論・テスト可能、日本語は単語境界が無いので文字 n-gram）。**exact wins (2026-07-09, ユーザーFB)**: cue が id/tag と完全一致（score 1.0）したら**その完全一致だけ**を返す（作者が正確な cue を書いた＝狙いの断言なので、語彙の似た別フラグメントを 0.08 の cosine ノイズで巻き込まない — mujinto の数千字 memoria が `メスの配偶者選択` recall で male_instinct/survival まで引っ張った実例の解消）。**exact が一つも無いときだけ** fuzzy（cosine ≥ 閾値）にフォールバックし、「樫の木の下で誓った」のような exact 不一致 cue も近い伏線を引く旧挙動を保つ。PoC: `exact_tag_match_suppresses_fuzzy_siblings`（Red→Green）。神経 embedding 版が要れば同 trait 裏で差し替え可（`()` は null recall）。`Trigger.recall`(cue) を engine が不透明 String として passthrough → `FiredTrigger.recall` → harness `resolve_recall(Memoria,&fired)` が `FiredBeat{narration, recalled}` に解決。`memoria/*.yaml`（ファイル名=id）を `load_lore` が読む。約束の想起シナリオで「好感度 30→発火→cue=childhood_promise→丘の樫の木の伏線を recall」を端から端まで実証（可変の好感度は engine、Memoria は伏線のみ＝境界実証）。**輪の閉じ (✅prompt 注入)**: `run_turn(recalled_lore)` が前ターン発火の伏線を `prompt::recalled_lore_note` で次ターンの prompt に「いま思い出された記憶」として注入 → ナレーターが語りに織り込む。状態変更でない旨を明示し ops に書かせない（境界を prompt 層でも維持）。bin は `pending_lore` を1ターン持ち越す。
- ✅ **閉世界 capability / メアリー・スー遮断 (2026-06-24)**: 能力(スキル)を正本の閉じた宣言集合にした。`CharacterDef.skills`/`Scenario.initial_skills` で宣言（初期=`GameState.skills`）、`Gate::HasSkill` で能力を前提条件にでき、**開花は authored トリガーの `grant_skill` 効果のみ** — LLM が `grant_skill` op を提案すると `adjudicate` が `SkillGrantNotAllowed` で却下（trigger effects は `apply_ops` 直行なので付与可＝禁忌/トリガー双対の三例目「開花は許される、ただし作者の gated 発火としてのみ」）。`#23`（所持物）の一般化＝「未宣言 capability は存在しない」。二層: engine が未宣言スキルの op gate を遮断＋LLM grant 却下／prompt が `state_brief` の「使える能力」提示＋「列挙された能力しか使えない/勝手に開花しない」接地。**実 LLM**: 「眠っていた予知を思い出して発揮」→ GM が「予知なんて最初から無かった」と接地・状態変化ゼロ。`failures.md #24`、覚醒シナリオで儀式→開花→予知 gate 通過→goal を実証。UI も能力を表示。
- ✅ **NPC inventory + 譲渡 (2026-06-24, `#23` の engine 化)**: 所持物をキャラ別の閉世界（`GameState.inventory{entity:[ItemId]}`）にし、譲渡を検証可能な op にした。`StateOp::GiveItem{from,to,item}` は `from` 未所持→却下（`ItemNotHeld`）/ `to` 未知 entity→却下（`UnknownEntity`）＝**持っていない物は渡せない**を engine が保証（行商ネックレス `#23` の op 側バックストップ）。波及最小化のため `AddItem`/`RemoveItem` は player 専用のまま（拾得＝世界→player）、per-entity 化は `Gate::HasItem{entity,item}`（既定 player）と GiveItem のみ。**二層完成**: narration 経路は prompt 接地（`#23`）／op 経路は engine 却下。**実 LLM**: gift シナリオで花を摘み→アリスに渡し→goal を LLM がプロンプト変更ゼロで駆動。`failures.md #25`。UI も NPC 所持を表示。
- ✅ **技能判定 (2026-06-24)**: `StateOp::Check{entity,stat,sides,dc}` — エンジンが `1d{sides}+stat修正 vs dc` を振り成否を裁定（`CheckOutcome` を返す）。LLM は出目も合計も詐称できない（op 構造上）、stat 未宣言→却下。**核心**: 出目は apply 後確定なので同一ターンの narration に間に合わない → 結果を**次ターンの prompt に「直前の判定結果」として還流**し GM に結果へ沿って語らせる（memoria_bridge の輪の閉じと同じ）。`apply_ops` を out-param 化（rolls/checks）、`run_turn` に `recent_checks` 追加。GM_SYSTEM に「不確実な行動は check で、この turn は『試みる』止まり」を接地。**実 LLM**: 力ずくで石扉→ LLM が check str/1d20/DC15 発行→ `🎯 1d20(14)+12=26 成功`→ 次ターンが前回成功を踏まえて語る。`failures.md #26`。UI も判定を表示。
  - **後付け接地の精密化 (2026-06-24, 実プレイ発見)**: 出目の成否は数字でしかない → GM に「**なぜ**成功/失敗したかを物語内の原因として後付けで語れ」を要求し、その**強さの原料**として margin(`total - dc`=「DC を N 上回った/N 届かなかった」)と tier(極=大失敗/大成功)を `check_outcome_note` で surface（差が大きいほど決定的に、僅差なら紙一重に、極なら劇的に）。GM_SYSTEM にも「確定した出目に理由を与え物語の因果へ翻訳」を刷り込み。**prompt 層のみ**（engine 不変＝数字は engine・因果は LLM の分業を保つ）。`check_note_demands_causal_reason_and_surfaces_margin_and_tier` / `gm_system_demands_post_hoc_reason_for_checks` で固定。
- ✅ **fumble-as-trigger PoC-1 (2026-06-24)**: 「失敗を罰でなく分岐に」の前半を実体化。`StateOp::AttemptChallenge{entity,challenge}` は **authored challenge を LLM が「選ぶ」だけ** — 判定の `stat/sides/dc` も極(tier)も帰結フラグも `Scenario.challenges`(`ChallengeDef`)側の専権で、**LLM は帰結を持てない**（tier/flag を Check op に畳む案は却下＝op は LLM 提案なので #23/#24 の穴、`failures` 級の設計判断。閉世界の三例目）。engine は apply 時に `1d{sides}+stat` を振り、**自然出目**が tier(`Natural::Min`/`Max`、sides 相対で die サイズ非依存)に該当すれば authored な帰結フラグを **直書き**（`flag_rules` gate 迂回＝authored 効果、trigger effects と同型）→ それを gate にした既存 trigger が同一適用で発火 → goal。`CheckOutcome.tier` に極名を surface。**load 時参照整合** `Scenario::validate()->Vec<ScenarioError>`（tier flag ⊆ `allowed_flags`、apply 中 panic でなく load 時に弾く＝幻フラグを実行経路に乗せない）。seed 19 で 1d6=natural 1 → `fumble_drawer` → `drawer_jam` 発火を Red→Green（`scenarios/fumble_check.yaml`）。後半 `→reached()→transition` は campaign keystone 依存の PoC-2。
- ✅ **transition PoC-2a / campaign keystone (2026-06-24)**: 「状態を持ち越したまま骨格を差し替える」を実装。`Scenario::transition(prev_state, prev_scenario) -> GameState`（`self`=遷移先骨格）は `initial_state` の双対 — あちらは初期化、こちらは**持ち越し**（リセットしないことで状態が場所を跨ぐ）。**持ち越す**: 数値(entities)・所持品・能力・RNG ストリーム・累積ターン・**世界フラグ**（`prev_scenario.global_flags` 宣言分＝source が「出ても残る」と宣言）。**捨てる**: 局所フラグ・`fired`。`location` は `self.start` にリセット。新規宣言(entity/stat/skill)は初期化・既存値は持ち越しが上書き。村→森で hp(減った値)/ランタン/tracking/alice 好感度40/met_alice が運ばれ door_open は消え stamina は初期化、を Red→Green（`transition_carries_state_drops_local_flags`）。`global_flags ⊆ allowed_flags` も `validate` に追加（`GlobalFlagUndeclared`）。**帰結は全て engine が運ぶ＝生成器/LLM は値を持てない不変条件をシーン跨ぎでも維持**。残り **PoC-2b** = `reached()`（名前付き goal → 分岐）で spine の尾 `Check→tier→trigger→goal→transition` を閉じる。
- ✅ **reached / 名前付き goal PoC-2b (2026-06-24)**: spine の尾を閉じた。`goal: Gate → goal: Option<Gate>`（単一・後方互換・既存7本は無改修）＋ `goals: Vec<GoalDef{id,when}>`（名前付き・authored 順）。`Scenario::reached(state) -> Option<GoalId>` ＝ `goals` 非空なら最初に成立した `id`（決定論）、空なら単一 `goal` を `DEFAULT_GOAL` で評価。`is_goal = reached().is_some()`（既存の is_goal 挙動は不変、harness/app 無改修）。**発火 GoalId が次モジュールの transition 分岐セレクタ**。大失敗→`fumble_drawer`→trigger→`drawer_jammed`→`reached()=="jammed_ending"`（`opened_ending` でなく）を Red→Green（`reached_selects_named_goal_from_fumble_branch`）。`NoGoal`（goal も goals も無い＝到達不能）を `validate` に追加。prompt の「クリア条件」表示も goals 対応。**spine 全長 `Check→tier→trigger→goal(reached)→[分岐セレクタ]` が gm_core 内で閉じた**。
- ✅ **orchestration / campaign 結線 PoC-2c (2026-06-24)**: `reached()→transition` を **harness 層**で結線（gm_core 無改修＝純粋のまま、file path を知らない）。独立 **Campaign file**（`campaigns/*.yaml` = `{title, start, modules:{id→path}, edges:[{from,on_goal,to}]}`）に authored なモジュール接続トポロジを凍結（scenario は自己完結のまま別キャンペーンで再利用可）。`harness::Campaign`（`from_yaml`/`next(from,goal)`=純粋参照/`module_path`）＋ `load_module`（read+`inject_cast`+`validate`、幻フラグ/幻 goal を load 時に弾く）＋ `advance_campaign(campaign,root,current_module,scenario,state) -> Option<Advance>`。駆動は **LLM 非依存**: 「engine が決める発火 GoalId」と「作者が書いた edges」だけで次モジュールが決まり、`transition` が状態(hp/所持品/能力/世界フラグ)を糸通しする。`reached()=None`(未到達) or 辺無し(終端エンディング)なら `None`。`study`(大失敗→`jammed_ending`)→`cellar` へ hp(8 が cellar 宣言 12 を上書き)/世界フラグ持ち越し・局所フラグ(`lamp_lit`)/`fired` 投棄を Red→Green（`advance_carries_state_across_fired_goal_branch`、他に地図参照/終端/未到達の計4本）。CLI `play --campaign campaigns/escape.yaml` で複数モジュール通し対応。per-flag 持続性（`flag_persistence` の宝箱問題）は当初この結線のスコープ外だったが ✅spec 02 (2026-06-27) で三値化（`persistent_flags` + `CampaignMemory`、`advance_campaign` に `&mut memory` を糸通し）。app(Tauri command/frontend) のモジュール跨ぎ表示は ✅2026-06-26（`advance_campaign_injected` + `TurnView.transition`）。
- ✅ **narration 継続性 / 情景の二度出し対策 (2026-06-24, 実プレイ発見)**: ターンループは毎回 messages を新規構築する（state が唯一の真実）ため LLM は**自分の直前の語りを記憶しない** → 静的情景（夕日・部屋）を毎ターン再 establish し、一度きりの登場ビート（「気づいて驚く」）まで再演した＝「矛盾しない GM」の破れ（`failures.md #27`）。**核心**: 正本は state（数値/フラグ/位置）の真実は握るが、**narration の継続性は state-grounding がカバーしない別チャネル**（state-truth とは独立した第二の失敗軸）。**解**: 直前の語りを `recent_narration` として次ターン prompt に還流（check/lore carryover と同じ輪）、`recent_narration_note` で「既出の静的情景・済んだ登場/挨拶/初対面の驚きを繰り返すな、続きから変化だけ描け」と接地。**prompt 層のみ**（engine 不変）。`run_turn` に引数追加、CLI/app 両ループが `last_narration` を持ち越す（campaign 遷移時はリセット）。実 LLM で2ターン目以降の再描写・驚き再演が消えたのを確認。`recent_narration_is_woven_into_prompt_for_continuity` 他で固定。
- ✅ **Package / 配布単位フォルダ B-core (2026-06-24)**: 「自己完結 file の純粋実行器」を **file → フォルダ**へ広げた配布単位。`packages/<name>/` に `package.yaml`（世界をまとめる1ファイル）+ `characters/` + `scenarios/`(+`campaign.yaml` 任意) を同梱 → zip→解凍→そのまま動く。狙いは配布容易性・受領側の管理容易性・将来の ZIP マーケット（マネタイズ）。**gm_core 無改修**＝harness が package の共有定義を各モジュールへ注入する（`inject_cast` と同型）。`PackageManifest`(title/description/author/version/engine/entry/world/player/globals) を data_contract に凍結。`inject_package`: `player.stats` を `initial_stats` へ **package 優先 merge**（同じ主人公がモジュール毎に HP10/HP8 へ分裂するのを防ぐ＝再定義禁止の実装）/`player.skills` を union/`globals.flags` を `allowed_flags`(使える)＋`global_flags`(跨いで生きる) の**両方へ union**（一元宣言が閉世界検査を自動で通る）。`load_package`: `package.yaml`→entry を read+`inject_cast`+`inject_package`+`validate`、**package.yaml 不在/entry 不在は load 時エラー**（自己完結＝フォルダ内参照の保証）。dogfood `packages/houkago/`（放課後の教室、entry=classroom）で player(hp 0→10 package 勝ち)・globals(met_moka を両集合へ)・cast(同梱 moka 注入) を Red→Green（`load_package_injects_player_and_globals_and_is_self_contained` 他3本）。**後続**: campaign-entry の複数モジュール束ね / `world`・`player.profile` の prompt surfacing / engine semver 検証 / GUI のフォルダ列挙＋一覧。自己完結のため共有キャラ(moka)はパッケージへ複製（配布 > DRY のトレードオフ）。
- ✅ **fixture / 配布物の完全分離 + GUI パッケージ一覧 (2026-06-24)**: フラットな `scenarios//characters//memoria//campaigns/` を解体し、二つの役割を分けた。**エンジン PoC 試験データ**（locked_room/strength_trial/heroine_route/trigger_recall/skill_awakening/gift/fumble_check）→ `crates/gm_core/fixtures/`、harness 統合 fixture（locked_room/heroine_meet/trigger_recall + alice + childhood_promise）→ `crates/harness/fixtures/`（`include_str!` を `../fixtures/` へ repoint、13箇所）。**配布物**は packages へ: `packages/houkago/`(galge classroom+moka)、`packages/escape/`(campaign study→cellar/forest、entry=campaign.yaml)、`packages/promise_demo/`(memoria_bridge demo)。gm_core は packages/ を知らないまま（純粋＝配布レイアウトに依存しない）。CLI 既定も houkago へ。**GUI（app）**: ハードコード scenario 一覧を廃し、`localStorage["kataribe.packagePaths"]` のフォルダパス一覧から `list_packages`→`read_manifest` で title を引いて表示、選択で `new_game(package_path)`→`load_package`（player/globals 注入込み）。フォルダパス追加/削除 UI 付き。campaign-entry パッケージ(escape)は一覧に出るが playable=false（load_package の campaign-entry 対応が後続）。検証: 103/103 green + frontend vue-tsc/vite build green + app backend cargo check green。
- ✅ **主人公(プレイヤー)の認識 / world・profile surfacing (2026-06-24, 実プレイ発見)**: package の `player.profile`/`world` が prompt に出ず、GM がプレイヤーを教師と認識しなかった（モカが生徒なのに教師扱いしない）。gm_core `Scenario` に `world: String` + `protagonist{name,profile}` を**純粋データ field**として追加（CharacterDef.profile と同類の語り素材、engine 非解釈）。`inject_package` が package の `world`/`player.{name,profile}` を注入、`scenario_brief` が「世界観」「主人公（プレイヤー）」を surface、GM_SYSTEM に「NPC は主人公の設定（職業/年齢/立場）を認識し沿って接する」を刷り込み。`scenario_brief_surfaces_world_and_protagonist` 他で固定。run_turn 等は無改修（scenario が context を運ぶ）。
- ✅ **初期所持品 / initial inventory (2026-06-24)**: 「最初からアイテムを持たせる」経路を `initial_skills` と同型で追加。`Scenario.initial_inventory`(主人公) / `CharacterDef.inventory`(NPC) / package `player.items` を `initial_state` が `add_to_inventory` で seed（gm_core 純粋データ + harness `inject_package` 注入）。場所アイテム(`Location.items` マップ=複数可・各 Gate 付き)からの拾得/譲渡/transition 持ち越し以外の「最初から所持」。閉世界（宣言したアイテムだけ初期所持）。
- ✅ **HP0等の End を goal に / goal 結末ナレーション (2026-06-25)**: 「失敗/死も goal 機構に乗せる」+「複数 goal のどれに達したか」。`Gate::StatAtMost`(`StatAtLeast` の双対) で hp は 0 クランプゆえ `stat_at_most(hp,0)` を「気絶/死」の勝敗条件に書ける（hp 専用でなく汎用上限＝好感度100で別 End 等も同機構）。`GoalDef.narration`(authored・非検証の語り素材) + `Scenario::reached_goal(state)->Option<&GoalDef>` で到達 goal の id+結末ナレーションを提示層へ。`reached`(GoalId) は transition セレクタとして不変（後方互換）。app `TurnView` に `goal_id`/`goal_narration`、frontend が結末ナレーション+「結末『id』に到達」を表示。PoC `stat_at_most_death_goal_surfaces_id_and_narration`。
- ✅ **文字列属性 / attributes — 第4の可変状態 (2026-06-25)**: クラス/職業/種族 のような**文字列の単一値**を持てる可変状態を正本に新設（flags=bool / stats=i64 / inventory・skills=集合 の隣）。fantasy の転職（戦士→魔法剣士）を機構で扱う。`GameState.attributes{EntityId:{AttrKey:String}}`（可変・セーブ対象）。`StateOp::SetAttribute{entity,key,value}` は **authored トリガーの専権** — LLM が提案すると `adjudicate` が `AttributeSetNotAllowed` で却下（クラス捏造遮断＝`grant_skill`/#24 と同型）、trigger effects は `apply_ops` 直行で書き換え可。**閉世界キー**: player=`initial_attributes` / NPC=`CharacterDef.attributes` / package=`player.attributes`(merge) で宣言、未宣言キーへの trigger `set_attribute` は `validate` が `AttributeKeyUndeclared` で load 時に弾く（値は authored 自由文字列）。`Gate::AttributeIs{entity,key,value}` で転職後を縛れる（魔法剣士のみ…）。`transition` は属性も丸ごと持ち越し（クラスは跨いで生きる）。`state_brief` が毎ターン surface、app も per-entity 表示。数値 stat（STR/AGI/MAG/SPD）は既存 `initial_stats` 糖衣で改修ゼロで書ける（境界が要れば後続で player の CharacterDef 化）。PoC: `attribute_seed_trigger_rewrite_and_gate` / `llm_proposed_set_attribute_is_rejected` / `validate_rejects_undeclared_attribute_key`。
- ✅ **短編ファンタジー TRPG サンプル `packages/sealed_shrine/`「封印の祠」(2026-06-25)**: 「1時間で終わる短編TRPG集」の方向づけサンプル兼テンプレ。**単一シナリオ＝1パッケージで GUI 即プレイ可能**（集の正しい単位＝自己完結短編。campaign-entry も今は GUI 対応済だが、短編は単一シナリオが自然な単位）。15〜20分。新機能を実content で見せる: 主人公は `stats{hp/STR/AGI/MAG/SPD}` + `skills[剣術]` + `attributes{クラス:見習い,種族:ヒューマン}`。聖剣を拾うと `awakening` トリガーが `set_attribute`(見習い→魔法剣士) + `grant_skill`(魔法) で**転職**。3つの named goal で分岐: `defeated`(魔に素手で挑み hp0=死、GM の戦闘ダメージ adjust_stat で創発)/`sealed_ending`(魔法 gate=封印)/`slain_ending`(魔法剣士 gate=討伐)、各 `narration` 付き。石扉は `祠の鍵`(has_item で SetFlag) か **`force_door` 挑戦**(STR 判定 1d20+STR vs DC16、成功で door_open、自然出目1で `罠作動`→落石トリガー→hp0=死) の二択。engine 動線(鍵→開扉／力ずく成功・大失敗→死／聖剣→転職→封印・討伐)は load_package 込みで検証済(使い捨てテスト、配布contentに恒久結合しない方針)。GUI で `packages/sealed_shrine` パスを追加すれば遊べる。
- ✅ **NPC 数値が上がらない → 数値 op の entity 省略で却下 (2026-06-25, 実プレイ発見)**: 「NPC のステータスが上がらない」。engine 再現で `adjust_stat{entity:moka,好感度,+30}` は 15→45 正常＝**エンジン無実**。`entity` 省略は既定 player に当たり、player は好感度を持たず `UnknownStat` でデルタ全体却下→好感度が動かない。narration↔op の翻訳と entity の正しさは prompt 層の責務（#23 同型）。**二層修正**: (a) GM_SYSTEM に「数値変化は必ず adjust_stat op で起こせ／NPC 数値は entity を必ず明示（省略=主人公で却下）」を刷り込み、(b) `RejectReason::UnknownStat` に entity を載せ「{entity} は stat '{key}' を持っていない」へ＝self-repair が宛先違いに気づける。`failures.md #31`。
- ✅ **challenge を「ダイス→フラグ」の正式機構に拡張 + 実プレイ surface (2026-06-26)**: 判定の**通常成否**(極だけでなく)でフラグを立てられるようにし、LLM が実プレイで選べるよう提示した。`ChallengeDef` に `on_success`/`on_failure`(`total>=dc`/未満で engine が直書きするフラグ、`allowed_flags` 必須＝validate)、`description`(提示文)、**`stat` を `Option` 化**(省略＝能力に依らない純粋ダイス・修正0の運試し)。`apply` が通常成否フラグを直書き(既存の crit `tier` と併存)。`scenario_brief` に「## 挑戦」を surface(id/提示文/使う stat or 運) + GM_SYSTEM に「該当 challenge があれば自分で check を組まず `attempt_challenge` で選べ」を接地 → **これで challenge が実 LLM プレイで使える**(従来は unit test の `apply()` 直叩き専用だった既知ギャップを解消)。`when:{kind:all,of:[...]}` で「ダイス結果フラグ × 他条件」を組める(Gate は純粋述語のままで、ダイスは op で振り結果を flag に書く＝境界を保つ)。ドッグフード: `sealed_shrine` の `force_door`(STR 判定でこじ開け、大失敗で罠→死)。PoC: `challenge_outcomes_set_flags_with_and_without_stat`(stat有=修正乗る/stat無=修正0、sides:1 で決定論) / `validate_rejects_undeclared_challenge_outcome_flag`。
- ✅ **challenge のフラグ条件化 — 解禁(requires) と 有利/不利(modifiers) (2026-06-26)**: 「フラグで挑戦を解禁/封鎖」「フラグで勝ちやすく」をどちらも**純粋 Gate** で書けるようにした(ダイスは op で振り、条件は Gate＝境界を保つ)。`ChallengeDef.requires`(`Option<Gate>`): 偽なら `attempt_challenge` を `ChallengeLocked` で却下(「導師に会うまで秘奥義に挑めない」)、`scenario_brief` に【前提: …】surface。`ChallengeDef.modifiers`([`ChallengeMod{when:Gate, bonus:i64}`]): `when` が真の分だけ `bonus` を出目合計に加算(合算・負も可)＝「導師の教えで +5」「傷で −3」。`when`/`requires` は flag/stat/attribute/skill/all/any どれでも条件にできる。tier(極)は素の出目で見るので bonus 無影響(自然出目1は +10 でも大失敗)。PoC: `challenge_requires_gate_and_conditional_modifier`(教え無し=ChallengeLocked／教え後=requires 通過+修正5で DC 越え、sides:1 決定論)。
- ✅ **challenge 結末のインライン narration — 繰り返す失敗を毎回語る (2026-06-26)**: 「失敗時に語る LLM と語らない LLM がある」→ authored 固定文で**失敗が必ず描かれる**ことを保証。トリガーは latch(一度きり)で繰り返す判定に不向きなので、非 latch の経路を新設。`ChallengeOutcome{flag,narration}`(on_success/on_failure を構造体化) + `TierDef.narration` + `CheckOutcome.narration`。`apply` が成否/極から結末文を解決(極の narration 優先)し `CheckOutcome.narration` に載せ、**毎回・同ターン**に提示(フラグ無しの失敗・フラグ無しの極でも語れる)。二重語り回避: authored narration 付きの判定は `check_outcome_note` から除外し LLM に再描写させない(narration 無しの素 Check は従来どおり次ターン還流)。app は CheckView.narration を結末文として表示。ドッグフード: `sealed_shrine force_door` の成功「押し開いた」/通常失敗「びくともしない」(毎回)。PoC: `challenge_outcome_narration_surfaces_every_attempt`(繰り返し失敗で毎回・非latch) / `check_note_skips_checks_with_authored_narration`。
- ✅ **没入アセット基盤 + 場所背景画像 (2026-06-26, spec 01 Phase 0+1)**: packages に `images/`/`audios/` を同梱し画像/音声で没入を出す機構の土台と第一弾。設計は `specs/01_immersion_assets.md`（**spec は `specs/NN_*.md`、docs/ は Qiita/note 投稿用**）。北極星: **gm_core は純粋のまま**——`Location.image: Option<String>` は不透明 ID を運ぶだけ（description/narration と同類、パス join もファイル IO もしない）。解決は harness `resolve_asset(root, AssetKind::{Images|Audios}, id)`（ID を `^[A-Za-z0-9._-]{1,64}$`＋`.`/`..`除外で検証＝トラバーサル遮断、`root/{kind}/{id}` 実在で Some・不正/不在は None＝寛容）。app: `GameSession.package_root` 起点に背景を絶対パス解決→`GameView/TurnView.background`、frontend が `convertFileSrc` で `asset://` URL 化（`Map` メモ化）し会話ペイン `<main>` に暗幕付きで敷く。Tauri `assetProtocol` enable + `new_game` で当該パッケージ dir のみ `asset_protocol_scope().allow_directory`（静的 allowlist でなく動的＝localStorage 任意パス対応かつ安全）+ CSP 締め。ドッグフード: `sealed_shrine` に `gate.svg`/`altar.svg` プレースホルダ背景を同梱・配線。PoC: `asset_id_validation_blocks_traversal` / `resolve_asset_returns_existing_files_only`。**残 Phase**: イベント CG(2) / BGM・SE(3)。実描画は GUI 起動で要目視確認。**アセットのフォーマット推奨 (✅2026-07-18, 受領者FB)**: 配布サイズの苦情（png 1 枚 10MB・パッケージ数十 MB）を受け、**画像=WebP / 音声=Ogg 推奨**を作法化。機構は拡張子非依存の不透明 ID（`resolve_asset` はファイル名をそのまま解決・書庫 denylist も不干渉・描画/再生は WebView2=Chromium が両対応）ゆえ**コード改修ゼロで今すぐ使える** — 問題は作者の作法のみ。ドッグフード: friday_lemmon の顔アイコン 5 枚を png→webp（各 480KB→10〜14KB、約 1/37）。outcast package_spec.md への推奨節は ✅追記済み（2026-07-20 確認: アセット節+作法 7）。
- ✅ **顔アイコン + presence + クリックでステータス (2026-06-26, spec 01 Phase 1.5)**: 右ペイン下部に「この場にいる」顔アイコン行（**主人公 + 今いる NPC**）、クリックでそのキャラのステータス（stats/skills/attributes/items）をポップオーバー表示。**居ない人物のパラメータは出さない**（presence のみ可視＝右ペインの旧「登場人物」常時一覧は撤去）。**engine 改修ゼロの見せ場**（クリック先は既存 `StateView.entities`）。gm_core: `CharacterDef.icon`/`Protagonist.icon`（顔アイコン ID・不透明）+ `Location.present: BTreeSet<EntityId>`（この場の NPC。`Scenario.cast`=注入スコープと語衝突回避で `present`。当初「空なら全 characters」→ ✅2026-07-02 改訂で**明示宣言**＝空なら誰もいない）。harness: `PlayerDef.icon` → `inject_package` が `protagonist.icon` 注入。app: `present_characters()` が主人公を先頭に presence を解決 → view DTO `present_characters:[{id,name,icon}]`。frontend: 顔アイコンは **CSS `background-image` で描画**（Tauri asset protocol の `.svg` MIME に `<img>` は厳格だが CSS background は寛容＝背景と同経路で確実に出る）、icon 無しは initials フォールバック。設定に**グラフィックタブ**（背景明るさ Slider・暗幕 alpha 可変・localStorage 永続）も追加。ドッグフード: houkago の moka に `moka.svg` + `classroom.present:[moka]`。PoC: `location_present_and_character_icon_parse`。**プロフィールカード化 (✅2026-07-02)**: 素のテキスト羅列モーダルを再設計——`EntityView.profile` を新設（player=`protagonist.profile`/NPC=`characters[id].profile` を surface、engine 無改修の語り素材）、ヘッダ（大アイコン ring + 名前 + 属性チップ）/ ステータス3列グリッド / 能力・所持チップ / フェード+浮上トランジション / Esc・幕クリックで閉じる。幅 30rem（スクロール抑制、ユーザーFB反映）。**profile 本文は二段構え**（同FB）: 初期は畳み、ヘッダの顔アイコンクリックで開閉（profile 有はバッジ「…」で示唆、キャラ切替でリセット）。Icon に `gauge`/`sparkle` 追加。cargo check + vue-tsc/vite build green、実描画は GUI 目視。
- ✅ **stats/attributes を宣言順で表示 (2026-07-15, ユーザーFB)**: 状態パネル/プロフィールカードの数値・属性が BTreeMap 由来で**アルファベット順**に潰れ、YAML に書いた順が失われていた → **authored 宣言 (`Scenario.initial_stats`/`initial_attributes`/`CharacterDef.stats`/`attributes`) を IndexMap 化**して**記述順を保持**。`Scenario::stat_order`/`attribute_order`(entity) が player=`initial_*`・NPC=`characters[id].*` の記述順を返し、app `state_view` がその順で並べる（宣言に無い runtime stat=role_assignment の帳簿等は末尾に BTreeMap 順で安定）。**実行時 `GameState` は BTreeMap のまま**＝決定論・セーブ不変、engine 意味論 (gate/validate/seeding はキー lookup) も順序非依存で不変。順序保存のため `indexmap` を gm_core 依存に追加。PoC: `stat_and_attribute_order_follow_yaml_declaration`（記述順が保たれ alpha 順にならない）。
- ✅ **campaign-entry パッケージの GUI 対応 (2026-06-26)**: 「Campaign.yaml のあるパッケージが一覧で選べない（playable=false）」を解消。**gm_core 無改修**＝harness が campaign の各モジュールへ package を注入する経路を足した: `load_module`/`advance_campaign` を `build_module(manifest: Option)` に内部リファクタし、`inject_package` を `inject_cast` の後・`validate` の前に挟む注入版 `load_module_injected`/`advance_campaign_injected` を新設（player/globals/world が各モジュールへ継承、注入後に閉世界検査ゆえ幻フラグ扱いされない順序）。`load_campaign_package(dir)`＝`load_package` の campaign 版（entry=campaign.yaml を読み、開始モジュールを注入込みで返す）+ `is_campaign_entry`。**app**: `new_game` が `is_campaign_entry` で分岐（campaign は `load_campaign_package`、単発は従来 `load_package`）、`GameSession` に `campaign`/`current_module`/`manifest` を保持、`play_turn` が goal 到達時に `advance_campaign_injected` で次モジュールへ遷移（CLI `play --campaign` と同型）— 遷移時は `TurnView.transition`(遷移先 title/location/description) を載せ、state/背景/presence を遷移先へ差し替え、`goal_reached=false` で入力を締めず続行（終端=辺無しは true のままクリア）。`list_packages` の `playable` を常時 true（読込エラー時のみ false）に、stale な「campaign 後続」UI マーカーを撤去。**frontend**: `transition` で「結末→次の章へ」+遷移先開幕を log に出し、持続イベント CG をリセット。PoC: `load_campaign_package_loads_start_module_with_injection`（既存 `advance_carries_state_across_fired_goal_branch` が注入無し advance を、本テストが campaign パッケージのロード＋注入を固定）。app 実機の通しプレイは GUI 起動で目視確認。
- ✅ **イベント CG (2026-06-26, spec 01 Phase 2)**: 発火トリガーで背景を差し替えて没入の山場を作る。**gm_core 純粋維持** — `Trigger.image: Option<String>`（不透明 ID、`narration`/`recall` と同列の語り素材）+ `Trigger.image_mode: Option<ImageMode>`（`background`=上書き持続／`overlay`=予約、省略=None）を `fire_triggers` が `FiredTrigger` へ passthrough。harness `resolve_recall` が `FiredBeat.image`/`image_mode` へ運ぶ（純粋 retrieval 不変）。app: `BeatView.image`（`resolve_asset` で絶対パス化）+ `image_mode`（文字列化）。frontend は**瞬間（momentary）** — 発火ターンに場所背景を上書きし、**次の受理ターンで場所背景へ復帰**（却下は保持＝物語未進行）。当初の「場所が変わるまで持続（案A）」は実プレイで「落石 CG が祠を出るまで壁紙化」が露呈し改訂（#27 視覚版、spec 01 #3、`eventCg`/`cgLocation` 状態を撤去＝収縮）。CG は state でなく提示層が turn から導く派生値（恒久的な見た目変化は location＝engine 接地で表す）。overlay は描画せず予約のみ。PoC: `trigger_image_passthrough_to_fired` / `trigger_image_mode_defaults_to_none`。ドッグフード: `sealed_shrine` の `awakening`(覚醒)/`rockfall`(落石) に CG プレースホルダ同梱（使い捨てテストで動線確認、配布 content に恒久結合しない）。
- ✅ **イベント CG の保持 `image_hold` (2026-07-28, ユーザーFB「チャットを打つとすぐ消えてしまう」)**: 2026-06-26 に「場所が変わるまで持続」(案A) を撤回して瞬間 CG にしたが、**壁紙化の原因は「持続」でなく「消す手段が無かったこと」**だった。作者が明示的に消せるなら持続してよい、という再解釈。`Trigger.image_hold: Option<ImageHold>`(`show`=消すまで持続 / `hide`=消す。**省略は従来どおりそのターンだけ**＝既存 content の挙動不変)。**`image_mode`(どこに描くか)とは別フィールド**にした — 一つの名前に二つの軸を載せると必ず混乱する (2026-07-19 の `hidden_*` の反省)。値を `on`/`off` にしなかったのは YAML が真偽値と解釈しうるため。**多重 `show` は差し替え**(後勝ち・ユーザー確認)＝CG は背景の上書きなので同時に二枚は在り得ず「前のを消してから出す」を作者に書かせない。`hide` に `image` を併記すると**表示中のものと一致するときだけ**消す(差し替え後の古い取り消しが暴発しない)。**engine は解釈しない passthrough**(`Trigger`→`FiredTrigger`→`FiredBeat`)で、保持は提示層: app `GameSession.sustained_cg` を `resolve_sustained_cg`(純関数・authored 順に畳む)が更新し、実効背景 = 持続 CG ?? 現在地の背景。**`SessionSave.sustained_cg`(serde default)** ＝セーブしないと再開で背景だけ巻き戻る (`last_narration` と同じ「語りの継続性」の扱い)。campaign 遷移でリセット。frontend は `hide` ビートを瞬間 CG 候補から除くだけ (持続分は backend が `turn.background` に畳んで返すので、CG 状態を frontend に戻さずに済んだ＝2026-06-26 の「CG は提示層が turn から導く派生値」の原則を保てた)。PoC 2 本 (gm_core passthrough+既定 None / app の畳み込み＝多重 show の差し替え・名指し hide の一致条件・瞬間 CG は持続値を動かさない・同 settle 内は後勝ち)。
- ✅ **音声 / BGM・SE (2026-06-27, spec 01 Phase 3 = spec 01 完了)**: 没入アセットの最後の Phase。**gm_core 純粋維持** — `Location.bgm: Option<String>`（ループ BGM）/ `Trigger.sound: Option<String>`（発火 SE）は不透明 ID を運ぶだけ（`image` と同列の語り素材）。`Trigger.sound` を `fire_triggers` が `FiredTrigger.sound` へ passthrough → harness `resolve_recall` が `FiredBeat.sound` へ（純粋 retrieval 不変）。app: `bgm_for`（`AssetKind::Audios` で解決、`background_for` の双対）→ `GameView.bgm`/`TurnView.bgm`、`BeatView.sound`（`resolve_asset(Audios)` で絶対パス化）。frontend は**不可視 `<audio loop>`** が `store.bgm` に追従（場所変化で差し替え・**同一 URL なら再代入せずループを切らさない**＝CG の瞬間と違い BGM は持続）、SE は `playSe` が `new Audio` で one-shot（受理ターンのみ・ミュート/音量0 で無音、自動再生制約は握り潰す）。音量/ミュート UI は設定の**「サウンド」タブ**（BGM/SE 共通ゲイン・localStorage 永続）。CSP `media-src asset:` と asset scope `allow_directory(root, true)` は Phase 0 の既存基盤が `audios/` をそのまま覆う（追加設定ゼロ）。PoC: `trigger_sound_passthrough_to_fired`（gm_core）/ `resolve_recall_carries_sound_and_image`（harness）。ドッグフード: `sealed_shrine` に tiny WAV プレースホルダ同梱・配線（`shrine_ambient.wav` 祠 BGM / `rockfall.wav` 落石 SE / `awakening.wav` 覚醒 SE）。実再生は GUI 起動で目視確認。**spec 01（没入アセット）は Phase 0〜3 全実装で完了**。**challenge 結末効果音 (2026-07-09, ユーザーFB)**: 従来 SE はトリガー発火のみだったが、`ChallengeOutcome.sound`/`TierDef.sound`（audios/ アセット ID・非検証の不透明 string、narration と同列）を新設し judgement の結末でも鳴らせるようにした。`apply_ops` が narration と同じ優先順（tier 優先→通常成否）で `CheckOutcome.sound` に解決 → app `CheckView.sound`（`resolve_asset(Audios)` で絶対パス化）→ frontend が受理ターンに `playSe`（ビート SE と同経路・one-shot）。素の Check は空。ドッグフード: `sealed_shrine` の `force_door` 成功に `rockfall.wav`。PoC: `challenge_outcome_sound_surfaces_on_check`（gm_core）。outcast `package_spec.md` に sound 欄追従済み。
- ✅ **遅延イベント / ターンスタンプ (2026-06-27)**: 「〇〇から N ターン後に発火」を正本のプリミティブにした。`StateOp::RecordTurn{entity,key}`（現在 `GameState.turn` を stat に**生値で刻む**＝clamp なしのタイムスタンプ。**authored 専権**＝LLM 提案は `adjudicate` が `TurnRecordNotAllowed` で却下＝タイマー詐称遮断、grant_skill/set_attribute と同型の閉世界）+ `Gate::TurnsSince{entity,key,turns}`（`現在turn - 刻まれた stat >= turns`、turn を eval 時に生で読むので settle 干渉・オフバイワン無し）。対で使う: X トリガーが `set_flag x_done` + `record_turn x_turn`、遅延トリガーが `when: all(flag_is x_done, turns_since x_turn N)`（未記録=0 での誤発火を flag で守る）。`turns_since` はクールダウンや「N ターン放置で〜」にも効く汎用条件。stat は transition で持ち越すのでタイマーはモジュール跨ぎでも生きる。検討した案A（カウントダウン stat + repeatable 減算）はオフバイワン＋armed フラグ＋3 トリガーで却下。`turn += 1` は `apply_ops → turn+=1 → fire_triggers` の順なので `turns:N`＝記録 apply の N 個後の apply で発火。PoC: `record_turn_and_turns_since_schedule_delayed_event` / `llm_proposed_record_turn_is_rejected`。**内部 stat の秘匿 (2026-06-28, spec 04 追補)**: タイマー stat (`x_turn` 等) は `entities` に同居するので一度刻むと UI/`state_brief` に露出する → `Scenario.hidden_stats: BTreeSet<StatKey>`（提示層が skip する宣言・engine 非使用/非検証、タイマーも repeatable カウンタも一括で隠せる）を新設。提示層 3 経路（app `state_view` / prompt `state_brief` / CLI `stats_line`）がこのキーを除外。PoC: `state_brief_hides_hidden_stats`。
- ✅ **右ペインの UI アイコン + select 配色 (2026-06-27)**: StatePanel の `「立っている状態」→「フラグ」` に改名 + 現在地/所持品/フラグ/ターンに**モノカラー線アイコン**を左付け（新 `Icon.vue`＝`stroke=currentColor` の inline SVG、テーマ色追従、パッケージ同梱画像でなく UI 装飾）。あわせて `<select>` ドロップリストの配色を `select option` で暗色テーマに統一（ink 背景＋parchment 文字、WebView2 既定の灰背景で読めない問題を解消）。frontend のみ。
- ✅ **山場を 1 判定で潰させない接地 (2026-06-27, 実プレイ発見)**: 「魔王に剣を構えたら LLM がダイスを振り、1 回の成功であっけなく撃破」＝ LLM が Check op の裁量で **1 判定をイベント全体の決着へ飛躍**させる問題（#23 の判定版: 重大な帰結が engine 非接地の narration で既成事実化）。**エンジン非改修**＝prompt 接地 + シナリオの書き方で対処（無難策、`authored_dice_only` engine スイッチは見送り）。GM_SYSTEM に「**態勢（構える/対峙）では振らない**／**1 判定はその狭い行動だけ解決し山場の決着へ飛躍させない**／重要な相手の撃破は authored 条件（挑戦・ゴール）を複数ビートで満たした時のみ／**engine 未記録の決着（ゴール未達）はまだ起きていない**」を刷り込み。authoring 指針を data_contract に凍結（①決着は named goal/flag に接地 ②大敵 challenge に `requires` 前提 gate で段組み ③on_success→次の requires で数珠つなぎ）。二層（narration の砦は prompt のみ＝#23 同型）。PoC: `gm_system_grounds_dice_timing_and_no_one_shot_climax`。
- ✅ **フラグの三値持続性 / spec 02 (2026-06-27)**: campaign 跨ぎのフラグ持続性を二値（global/局所）から三値へ。**真因の接地**: `transition` は遷移**元**モジュールの `global_flags` だけを運ぶので、フラグが campaign 全体を生き残るには**通過する各モジュールが個別に global 宣言**せねばならず脆い → 双方向グラフ（A→B→A）で局所フラグ `chest_opened` が再訪時 `initial_state` で空に戻り「宝箱が復活」（矛盾する GM）。**解（campaign 状態に蓄積）**: `Scenario.persistent_flags: BTreeSet<FlagKey>`（場所フラグ・`allowed_flags` の部分集合）を新設、蓄積は **gm_core でなく harness** が担う — `harness::CampaignMemory = BTreeMap<ModuleId, BTreeMap<FlagKey, bool>>` に**モジュール別**でフラグ最新値を覚え、`advance_with` が遷移**元**を `harvest_persistent`・遷移**先**を `overlay_persistent` で糸通しする。これで「そのモジュールを再訪したときだけフラグが蘇る」第三の値が **gm_core 無改修**（宣言フィールド + validate の `PersistentFlagUndeclared` だけ）で成立。namespace は `ModuleId` キーで分離（A の `chest_opened` は B に漏れず、B の同名フラグとも衝突しない）。数値/所持品/能力/属性は本質的に world ゆえ常に持ち越し（三値の問いは bool flag だけ）。`advance_campaign(_injected)` に `&mut CampaignMemory` を糸通し（app `GameSession.campaign_memory` / CLI ループ保持、save/load は saves 機構が来たら）。frontend 無改修（復元フラグは既存 `StateView.flags` パネルに自動で出る）。PoC: `persistent_flag_survives_revisit_local_flag_does_not`（village⇄forest 再訪で `chest_opened` は蘇り `torch_lit`/forest 局所は漏れない）/ `validate_rejects_undeclared_persistent_flag`。fixture: `crates/harness/fixtures/campaign_revisit.yaml`(+ village/forest)。**スコープ外**: モジュール内の単一 location 粒度 / `CampaignMemory` の save 永続化。
- ✅ **知識フラグのヒント / spec 03 (2026-06-27)**: 「〇〇を知る」のような**会話で立つ知識フラグ**を LLM が確実に `set_flag` できるよう prompt に surface（弱モデル向けロバスト化＝同人配布の北極星、no-tools #29・CoT 除去 #30 と同層）。**穴の接地**: `scenario_brief` は `allowed_flags` を一覧にせず、フラグが LLM に見えるのは①下流 gate 参照（`gate_brief`）②`state_brief` の**既に true** のみ → 下流参照のない純粋な知識フラグは立つ前は不可視。**既存機構では本質的に届かない**（`flag_rules` gate は**守る**バックストップで促さない／authored トリガーは行動・場所に紐づく学びには最適だが純粋な会話イベントは状態条件に乗らない）残る狭い穴＝**会話で立つ知識フラグ × 弱モデル**にだけ足す。**解**: `Scenario.flag_hints: BTreeMap<FlagKey, String>`（flag→「立てる条件」の説明文）。**gm_core 純粋維持** — 非検証の語り素材（`world`/`profile` と同類）だがキーは `allowed_flags` 必須（`validate` の `FlagHintUndeclared` で幻フラグへのヒントを弾く）。prompt 層のみ: `scenario_brief` に「## 状態フラグ」を **opt-in** surface（全 `allowed_flags` を撒くと authored トリガー/challenge 専権フラグまで LLM に立てさせたくなる誘惑＝ノイズ、opt-in で回避）+ GM_SYSTEM に「条件成立の瞬間 set_flag、ただし先回りで立てるな」。**定石は `flag_rules` と対**（ヒント＝促し／gate＝守り、早まった set_flag は gate が却下）。frontend 無改修。PoC: `validate_rejects_undeclared_flag_hint`（gm_core）/ `scenario_brief_surfaces_flag_hints_and_gm_system_demands_setting`（harness）。弱モデルでの実効性は実 LLM プレイで計測（強モデルは命名で既に立つ）。
- ✅ **キャラクターの入退場制御 / spec 04 (2026-06-28)**: 「登場（入場）/退場」をトリガーで制御し、モジュール跨ぎで持ち越す。presence（顔アイコン行）を **authored 静的（`Location.present`）から可変状態へ昇格**。**authored 専権 op の第4例**（`grant_skill`/`set_attribute`/`record_turn` と同型）: `StateOp::SetPresence { entity, present }` は LLM 提案だと `adjudicate` が `PresenceSetNotAllowed` で却下（キャラ勝手登場の捏造遮断）、trigger effects は `apply_ops` 直行で登場/退場させる。**オーバーライド層モデル**（実プレイ前の設計判断、ユーザー選択）: `Location.present`（authored 場所ベース）を残したまま `GameState.present_overrides: BTreeMap<EntityId, bool>`（第5の可変状態・セーブ対象）を重ねる。実効 presence = `Scenario::present_at(state)`（純粋）= 場所ベース ± override、既知 characters に retain。`transition` が `present_overrides` を持ち越す（登場させた仲間が次の画面に同行）。**跨ぎ注入の依存**: override は bool を運ぶだけなので、未注入キャラへの force-present は `present_at` の retain でそのモジュールでは黙って落ち、override は state に残り**そのキャラを持つ次のモジュールで現れる**（同行者は各モジュールの cast に宣言）。app `present_characters` を `present_at` 経由へ（frontend 無改修＝顔アイコン行はそのまま実効 presence を描く）。PoC: `presence_override_via_trigger_changes_present_at` / `llm_proposed_set_presence_is_rejected` / `transition_carries_present_overrides`。**スコープ外**: 立ち絵・差分表情 / per-location override（現状 override はモジュール内全 location に効く）。**✅present の明示宣言化 (2026-07-02, ユーザーFB)**: 「空なら全 characters」フォールバックを廃止——無人の場所を作るのに全キャラを `set_presence false` する逆転が起きるため。**空（未宣言含む）＝誰もいない**、NPC を出す場所には `present` を必ず書く（override での登場は不変）。フォールバック依存の fixture 6 本（gift/heroine_route/skill_awakening/trigger_recall×2/heroine_meet/promise_demo）へ `present: [alice]` を明示。typo った present entity は `present_at` の retain で黙って落ちる（validate 化は必要になったら）。PoC: `empty_present_means_nobody`（Red→Green）。
- ✅ **presence の prompt 接地 (2026-07-02, 実プレイ発見)**: 「LLM が今誰がいるか把握できない（減らしても語りから消えない/増やしても居ないと扱われる）」→ **presence は UI（顔アイコン行）にしか出ておらず GM の prompt に一度も出ていなかった**接地漏れ。場所説明文（「カウンターの奥にアリスが…」）は静的ゆえ退場後も GM が読み続けるのが真因。**二層修正（prompt 層のみ・engine 不変、#23/#31 同型）**: (a) `state_brief` に「この場にいる: アリス (alice), …」を毎ターン surface（`present_at` 直結＝実効 presence、無人は「誰もいない (主人公のみ)」を明示、name (id) 形式で語り＝name / ops＝id の両方に接地）、(b) GM_SYSTEM に「一覧が唯一の真実（説明文は静的・一覧が現在）／一覧に無いキャラを出すな・台詞させるな／居るキャラを不在扱いするな／登場退場はあなたが起こせない（筋書きの出来事）」を刷り込み。PoC: `state_brief_surfaces_effective_presence_and_gm_system_grounds_it`（Red→Green、override 反映・無人明示・GM_SYSTEM 文言）。
- ✅ **移動の置き去り接地 / 同行の既成事実化の遮断 (2026-07-11, 実プレイ発見 #49)**: 主人公が移動すると居ない NPC がついてきたことになる — GM 自身の移動ターンの語りの**同行の素振り**（「一緒に歩き出す」等、冗長なモデルほど書きがち）が recent_narration/chronicle に確定事実として乗り、presence 一覧（一般規律）が具体的な語りに負ける（**#47 の自己汚染版** — 汚染源が authored トリガーでなく GM 自身の語りなので content 修正では防げない）。**二層修正（prompt 層のみ・engine 不変）**: (a) GM_SYSTEM「移動しても NPC は勝手についてこない／同行の素振りを書くな」（汚染源の抑制）、(b) `prompt::moved_note` — 直前ターンで場所が変わっていたら置いていかれた NPC を**固有名で「ついてきていない」**と否定接地（#37 の接地強度の適用）。移動検知は **history 末尾 2 件の location 差**（`TurnLog.location` は適用後の現在地ゆえ移動ターン自身のログは新しい場所を指す＝state と比べても差が出ない、が要注意点）。置き去り＝前ターンの `TurnLog.present`（spec 08-B 機械タグ）− 現在 `present_at`＝engine 事実のみで LLM 非関与。旧セーブ/履歴 1 件以下は沈黙。一般化: **非検証チャネルの自己汚染は「書くな」の規律と「書かれた後を事実で上書きする還流」の二層で塞ぐ**。PoC: `moved_note_grounds_left_behind_npcs_with_names`（Red→Green）。`failures.md #49`。
- ✅ **経緯ログ / chronicle — GM の中期記憶 (2026-07-02, 実プレイ発見)**: 「経過を忘れてしまっている」→ GM が過去を知る経路は state（事実の現在値のみ・経緯なし）と `recent_narration`（直前 1 ターンのみ）だけで、**数ターン前の出来事を知る経路が存在しなかった**。**解（三層の中期記憶を新設）**: `StateDelta.summary`（gm_core 新設・serde default）に GM 自身が毎ターン「経緯 1 行（誰が何をして何が起きたか）」を書く（narration と同じ非検証の語り素材、schemars で LLM schema に自動露出、GM_SYSTEM が記述義務を刷り込み「書かなければ忘れられる」）→ 呼び出し側（CLI/app）が `harness::chronicle_entry`（summary 空なら narration 冒頭 80 字へ fallback＝弱モデル対応）で `TurnLog{turn, player, summary}` を蓄積 → `run_turn(history)` が `history_note` で「# これまでの経緯（古い順・確定した記録）」として毎ターン注入（文字予算 2400・新しい方優先・溢れた古い方は「省略」明示、矛盾禁止・再演禁止も同梱）。**campaign 遷移では捨てない**（章替わりマーカーを刻む＝章を跨いで覚える。lore/checks/narration はリセットのまま）。正本の境界は不変: chronicle は語り素材であって可変世界状態を持たない（Memoria 同様）。`TurnLog` は Serialize 派生＝将来のセーブ同梱可。PoC: `chronicle_history_is_woven_into_prompt_and_summary_carried` / `empty_history_means_no_chronicle_block` / `chronicle_entry_falls_back_to_truncated_narration` / `history_note_respects_budget_drops_oldest` / `gm_system_demands_turn_summary`（Red→Green）。実効性（強/弱モデルが summary を書くか・経過矛盾が消えるか）は実 LLM プレイで計測。
- ✅ **フラグの語彙提示 + 表示名 + 真化ターン記録 (2026-07-03, ユーザーFB)**: 「LLM がたまに無いフラグを呼び出す」→ GM_SYSTEM は set_flag を義務化しつつ**語彙 (フラグ名の閉集合) を見せていなかった**非対称が真因 (幻フラグ発明は必然)。**三点セット**: ① **語彙提示**: `Scenario::authored_only_flags()`（トリガー効果/challenge 帰結が書くフラグを宣言走査で機械判別）と `usable_flags()`（= allowed − 専権）を新設、`scenario_brief` が「状態フラグ (set_flag で立てられるのはこれだけ)」を id+title+hint 付きで列挙（`filter_authored_only_ops` のフラグ版＝専権は見せず先取り誘惑を作らない）。`RejectReason::FlagNotAllowed{key, available}` も同語彙を運び self-repair が一発修正（#31 同型）。② **`Scenario.flag_titles`（エイリアス）**: id=機械用キー / title=人間向け表示名（goal title と同思想、validate `FlagTitleUndeclared`）。UI フラグ一覧はチップ（title || key）、`state_brief` は「id（表示名）」形式。③ **`GameState.flag_turns`**: フラグが true に真化したターンを **apply 末尾の差分で一括記録**（op/トリガー/challenge 帰結の全経路捕捉、セーブ対象、transition は生存フラグ分持ち越し）→ app が chronicle の該当ターン要約と join し「何をして立ったか」を hover で表示（`FlagView{key,title,turn,cause}`）。ドッグフード: houkago/sealed_shrine に flag_titles。**④ `Scenario.hidden_flags`（同日ユーザーFB）**: タイマーの armed フラグ（`x_done` 等）のような**変数用の帳簿フラグ**を提示層 3 経路（UI フラグ一覧 / `state_brief` / 語彙節）から一括秘匿する `hidden_stats` のフラグ版（engine 非使用・非検証＝gate/トリガー評価は不変、キーは allowed_flags 必須 `HiddenFlagUndeclared`）。PoC: `authored_only_flags_are_excluded_from_usable_vocabulary` / `flag_turns_record_when_flags_became_true` / `validate_rejects_undeclared_flag_title` / `scenario_brief_lists_usable_flags_with_titles_excluding_authored` / `hidden_flags_parse_and_validate_membership` / `hidden_flags_are_skipped_by_prompt_layers`（Red→Green）。
- ✅ **可視性の二軸命名整理 — `hidden_*` を「GM は見る秘密」へ・`internal_*` 新設 (2026-07-19, ユーザーFB)**: `hidden_flags`/`hidden_stats`（GM もプレイヤーも見ない帳簿）と `hidden_attributes`（プレイヤー非表示だが GM は〔秘匿〕付きで見る）が**同じ `hidden_` 接頭辞で GM 可視性が逆**という罠を解消。フラグ/数値も属性に揃え、**`hidden_*` = プレイヤー非表示・GM は〔秘匿〕付きで見る（明かすな規律を GM_SYSTEM が刷り込み）**へ意味変更。「GM も見ない engine 帳簿」（タイマー/カウンタ）は新設 **`internal_flags`/`internal_stats`** へ分離。可視性は**専権（誰が書けるか）× 秘匿（誰が見るか）× グローバル（跨いで生きるか）の直交 3 軸**に整理。提示層: プレイヤー UI/CLI は `hidden_*`・`internal_*` とも隠す（プレイヤーからは同一）／`state_brief` は `hidden_*` を〔秘匿〕付きで見せ `internal_*` は隠す／set_flag 語彙節は両方除外。`internal_flags` は allowed_flags 必須（validate `InternalFlagUndeclared`）、`internal_stats` は無検証（stat キー開集合）。**破壊的変更**: 旧 `hidden_stats`/`hidden_flags` を「GM にも隠す」用途で使っていた配布 content は `internal_*` へ移行が必要（in-repo はゼロ使用、移行対象は vampire 盤面等の作者ローカル/配布パッケージのタイマー・人狼カウンタ）。PoC: `internal_flags_and_stats_parse_and_validate`（gm_core）/ `flag_and_stat_visibility_internal_vs_hidden_split`・`state_brief_hides_internal_stats`（harness、Red→Green）。outcast package_spec.md は ✅追従済み（2026-07-20 確認: 可視性 3 段表）。
- ✅ **発火ビートの GM 還流 (2026-07-03, ユーザー質問で発覚)**: トリガーの authored `narration` は**プレイヤーには表示されるが GM は一度も見ていなかった**（発火は GM の提案後に engine 側で起きる。GM に届くのは効果の帰結＝state のみで、語られた出来事そのものは不可視＝#27 と同じ「言語チャネルが state-grounding でカバーされない」系）。**二層還流（prompt 層のみ・engine 不変）**: ① `harness::carryover_narration(narration, beats)` が「（直後に筋書きの出来事が起きた）」としてビートを継続文脈（recent_narration）へ連結、② `chronicle_entry(…, beats)` が summary に「／出来事: …」を併記（中期記憶にも残る）。CLI/app 両ループ配線。PoC: `fired_beats_flow_into_carryover_and_chronicle`（Red→Green）。
- ✅ **tier の閾値 (at_most/at_least) — 大失敗/大成功に幅を持たせる (2026-07-12, ユーザーFB)**: `Natural` が `Min`(roll==1)/`Max`(roll==sides) の 2 点のみだったので、d100 のように sides が大きい盤面で `min`(=1, 1%) が滅多に発火しない問題を解消。`Natural` に `AtMost`/`AtLeast` を追加し、`TierDef.threshold: Option<u32>`(兄弟フィールド) と組で `roll <= threshold` / `roll >= threshold` を判定（「20 以下は大失敗」「96 以上は大成功」）。**判定は自然出目そのもの**（stat 修正も modifiers の bonus も乗る前）＝「素の下振れ＝大失敗」の tier 設計思想を幅でも保つ。**data 付き enum を避けフラットフィールド**（serde_yaml は data 付きバリアントに `!tag` を要求＝Gate の `kind` と同流儀）: `{ natural: at_most, threshold: 20 }`、既存 `natural: min` は不変。複数 tier 該当時は BTreeMap キー名昇順で最初が勝つ（決定論）。validate に `TierThresholdOutOfRange`（threshold が `1..=sides` 外、または At* で欠落＝無制限を load 時に弾く。min/max は threshold 不要）。PoC: `tier_threshold_at_most_and_at_least_fire_on_band`（seed 2→d100=11 で下位帯/13→96 で上位帯/3→54 で帯外＝不発火）/ `validate_rejects_out_of_range_tier_threshold`（Red→Green）。outcast package_spec.md の tier 節は ✅追従済み（2026-07-20 確認）。**←2026-07-23 に count-aware へ訂正**: 本節の `roll==1`／`roll==sides`／`1..=sides` はどれも count=1 の場合の表記で、2026-07-20 の複数ダイス以降は `Min`=合計最小（全部 1＝`roll==count`）／`Max`=`count×sides`／threshold 範囲 `1..=count×sides` が正。
- ✅ **全帰結共通効果の射影 — spec 09 の精密化 (2026-07-12, 実プレイ発見)**: 日次ループ盤面（1ldk）で GM が [バイト挑戦, 帰宅 move] を毎日束ね、move gate のフラグが挑戦の帰結側にあるため**毎回 1 回目却下**が発生。`guaranteed_challenge_effects(def)` — attempt_challenge の効果のうち **on_success/on_failure の両方に共通するもの**（=どの出目でも必ず起きる。**多重集合の厳密交差**＝過剰射影は誤受理の芽なので個数も一致 + 両帰結が同じフラグを立てる場合の set_flag）だけを `validate_ops` の射影に乗せる。全帰結共通なら裁定時に確定扱いしても「裁定は apply のドライラン」の健全性を破らない。**帰結依存の効果（片側だけ/tier）は非射影のまま**＝「ダイスはターンを割る」原則は帰結依存の手に対して不変（割りたい連鎖は帰結を成功/失敗で変えれば割れる）。tier は加算的で outcome 効果を打ち消さないため交差の健全性に影響しない。data_contract の sequencing/pacing 追従済み。PoC: `adjudication_projects_outcome_invariant_challenge_effects`（共通効果の束ね一発受理 / 成功限定フラグの move は却下のまま、Red→Green）。
- ✅ **challenge の判定主体の authored 固定 `ChallengeDef.entity` (2026-07-12, 実プレイ発見)**: NPC の stat で振る challenge（裏でヒナの浮気進行判定等）を LLM が **entity 省略（=player 既定）で挑んで UnknownStat 却下×2 が毎回発生**（#31 の却下文言「entity にその NPC を指定」では**どの** NPC かまで伝わらない）。`ChallengeDef.entity: Option<EntityId>`（serde default）を新設し、**Some なら op の entity を上書き**して adjudicate/apply とも authored 主体の stat で判定（LLM の省略/誤指定に依らない＝「判定の素性 stat/sides/dc/帰結は authored」の主体版・閉世界の一貫）。`CheckOutcome.entity` は実際に振った主体を surface（UI/還流が正しい名を出す）。validate に `ChallengeStatUndeclared`（authored 主体が判定 stat 未宣言＝幻主体を load 時に名指し。プレイ中の UnknownStat より早い）。`scenario_brief` は「{entity} の {stat} 判定 (主体は固定済み・entity 指定不要)」と surface。None なら従来どおり op の entity（後方互換）。PoC: `challenge_authored_entity_overrides_op_entity` / `validate_rejects_challenge_authored_entity_without_stat`（Red→Green）。outcast package_spec.md の challenge 節は ✅追従済み（2026-07-20 確認）。
- ✅ **専権フラグの engine バックストップ (2026-07-13, 実プレイ発見 #50)**: authored 専権フラグ（トリガー/challenge の帰結が書く）の除外が **prompt の語彙提示だけ**で、engine の却下条件に無かった。しかも set_flag の flag_rules gate 検査は value=true 限定＝**false は素通り受理**。実測 (1ldk): GM が「退勤」の意味論で `[set_flag 会社で仕事をする=false, move(gate:会社==true)]` を束ね、**逐次射影の中で自分の move を壊す** →「画面は true なのに『true が必要』で却下」の怪奇を毎日再演（false は却下試行の射影内にだけ存在）。単独提案なら受理されて実 state を汚せた（authored 機構の静かな妨害）。**解**: adjudicate の SetFlag 検証に `authored_only_flags` 検査を追加 — 専権フラグへの LLM set_flag は **true/false とも却下**（FlagNotAllowed で usable 語彙を還流）。トリガー効果は apply_ops 直行で不変＝grant_skill/set_attribute の op 専権と同じ二層が**フラグにも**揃った。一般化: **閉世界の防衛線は「見せない (prompt)」と「通さない (engine)」の二層で初めて閉じる／gate 検査を true 方向に限定すると false 方向が認可の死角になる**。PoC: `llm_cannot_set_authored_only_flags_either_direction`（Red→Green、self-sabotage 再現＝却下理由から MoveGateUnmet が消え FlagNotAllowed が真因を名指し）。`failures.md #50`。
- ✅ **未知フィールド lint — 「静かな罠」の防衛線 (2026-07-12, ユーザーFB)**: serde が未知キーを黙って無視する罠を **2 日で実測 3 件**（friday_lemmon の Location 直下 `gate:` / 1ldk の challenge 入れ子(インデントずれ) / `entry:` typo ×5）踏んだため機構化。`gm_core::unknown_key_lints(yaml) -> Vec<String>`（純粋・[lint.rs](crates/gm_core/src/lint.rs)）が生 YAML を文脈付きで歩き（Scenario/Location/Trigger/Challenge/Gate/StateOp… 18 文脈）、未知キーをパス付きで名指し + **編集距離 ≤2 の近い既知キーを提案**（「entiity」→「entity の誤り？」）。**既知キー集合は手書きしない** — 構造体は「最小 YAML で parse→serialize→全フィールド名」で型から導出（フィールド追加に自動追従）、enum（Gate/StateOp）は全バリアント標本の和集合 + **網羅 match の番人**（バリアント追加時にコンパイルエラーで標本更新を強制）。**非 fatal**（前方互換: 新 content を古い Kataribe で読んでも警告どまり） — `load_package` が `LoadedPackage.warnings` に載せ、app の開幕 ⚠（`GameView.warnings`、flag_hint lint と同経路）へ。実 content (1ldk) で偽陽性ゼロ + 意図的 typo 全捕捉を使い捨てテストで実証。PoC 4 本: `lints_entry_typo_with_suggestion` / `lints_location_stray_gate_and_nested_challenge` / `no_false_positives_on_kitchen_sink` / `broken_yaml_returns_empty`。**✅package.yaml へ適用 (2026-07-21、spec 20 改名の副産物を回収)**: manifest だけ lint されておらず、キー改名（`memo_policy`→`acts_policy`→`facts_policy`）を追えていない配布 package.yaml が serde に黙殺され既定 `locked`（タブ非表示）へ落ちる実害が出ていた。**gm_core は配布レイアウトを知らない**（純粋層）ので、汎用部品だけを pub 化（`gm_core::struct_keys`＝型から既知キーを導出／`gm_core::unknown_keys`＝mapping 1 段の未知キー検査＋提案）し、**型を持つ harness 側が組む** `harness::manifest_lints(src)`（PackageManifest / player / globals の 3 文脈、既知キーは型から導出＝手書きしない規律を維持）。`read_manifest_text` で生テキストを取り二度読みを避け、`load_package`／`load_campaign_package` の両方が `warnings` に載せる（campaign 側は `LoadedCampaignPackage.warnings` を新設）→ app 開幕 ⚠ へ。**残**: campaign 各モジュールの scenario 本体 / characters（どちらも loader が生テキストを返さない）。PoC 3 本 Red→Green（改名前キー＋player/globals typo の名指し＋提案 / 同梱パッケージ全 manifest で偽陽性ゼロ / `load_package` 結線＝Red は `warnings: []` で実害を再現）。**✅バリアント別の既知キーへ (2026-07-27、ユーザー質問「`{op: move, to: pit, entity: aaa}` とやると aaa が移動する？」から)**: enum（Gate/StateOp）の既知キーを**全バリアントの和集合**で持っていたため、`entity` は「他の op にとって正しいキー」ゆえ `move` に書いても素通り＝**op/gate ごとのフィールド typo が全部見えていなかった**（構造は在るのに射程が一段浅い＝機構の穴でなく解像度の穴）。`variant_keys(samples, tag)` でタグ（`op`/`kind`）の値 → そのバリアントのキー集合の表を作り、`Tables::known(ctx, value)` が値を見て引く。**タグが無い/未知の値なら和集合へ退避**＝判別できないものは警告しない（前方互換: 新しい content の未知バリアント名を古い Kataribe が誤警告しない）。既知キーを型から導出する規律・網羅 match の番人は不変。**初回実行で同梱 content の実バグを 1 件検出**: friday_lemmon の `{ op: add_item, entity: akari, item: 銀の指輪 }` — `AddItem` は player 専用（NPC へ渡すのは `give_item`）なので `entity` は黙殺され**指輪はプレイヤーの所持品に入っていた**（UI に見える誤り）。akari の profile が「銀の指輪を無意識に身につけている」＝最初から持っている設定なので `CharacterDef.inventory` へ移し、死んだ op をトリガーから撤去（reiko は既に同じ書き方）。PoC 2 本 Red→Green（バリアント別 typo の名指し / タグ不明時は黙る）。
- ✅ **scenario lint — 専権フラグへの死んだ flag_hint (2026-07-12, ユーザーFB)**: 「GM に立てさせたい」意図の `flag_hints` を、トリガー/challenge の effects が書く**専権フラグ**に付けると GM の usable 一覧に出ずヒントが死ぬ（二重所有の罠、実プレイで作者が踏んだ）。**`Scenario::lints()`（警告・非 fatal）を新設** — 当初 validate（load 拒否）に入れたが、**プレイを壊さない書き方を fatal にすると配布済み content が受領側で死ぬ**（書庫アップロード済みシナリオの大半が該当、ユーザー報告）ため lint に降格。重大度の線引き: **validate=整合性の破れ（幻フラグ等、閉世界が壊れる）→ load 拒否／lints=意図どおり動かない書き方 → 警告表示のみ**。app は `GameView.warnings`（new_game/resume 両方）→ 開幕ログに ⚠ 表示（作者が新規ゲームで気づける）。版ゲート（0.2.x 免除）案は「版×ルールの表」を永続に背負う割に新規でも死ぬため棄却。`ScenarioError::FlagHintOnAuthoredOnly`。PoC: `flag_hint_on_authored_only_is_lint_not_fatal`（validate は通り lints が名指しする）。
- ✅ **challenge の直接効果 `effects` (2026-07-03, ユーザーFB)**: `ChallengeOutcome.effects`/`TierDef.effects: [StateOp]` — 判定の帰結で stat/attribute/スキル等を**フラグ+トリガーの2点セット無しで直接**動かせる（同一 apply 内に原子適用）。authored 専権＝trigger effects と同じ信頼モデル（LLM は challenge を「選ぶ」だけで帰結を持てない閉世界は不変）。通常成否と極の effects は併存（フラグと同じ）。守り2つ: `attempt_challenge` の入れ子は validate `ChallengeEffectRecursive` で遮断（A→A 無限再帰の芽、連鎖は flag→トリガー経由）／`set_attribute` の幻キーは `AttributeKeyUndeclared`（trigger 欄=`challenge:{id}`）で遮断。従来の定石（flag→repeatable トリガー+リセット）は複数トリガー連鎖・latch 制御が要る場面用に併存。PoC: `challenge_effects_apply_stats_and_attributes_atomically` / `validate_rejects_attempt_challenge_inside_challenge_effects`（Red→Green）。
- ✅ **場所の表示名 + 本人未知の秘匿属性 (2026-07-08, ユーザーFB)**: ①`Location.title`＝GUI 現在地の**表示名**（id=機械用セレクタ/title=表示名 の三層思想、`GoalDef.title`/`flag_titles` と同類。非検証・serde default＝既存 YAML 無改修）。app `StateView.location_title` を新設し frontend は `title || id` で表示（hover で id）。GM prompt は従来どおり id（move の宛先=id、語りの素材は description）。ドッグフード: houkago(放課後の教室)/sealed_shrine(祠の入口・祭壇の間)。②`Scenario.hidden_attributes`＝**当人にも見えない**属性キー（自覚のない正体・呪い等）。`secret_attributes`（本人分は見える=人狼の自役職）より一段強い秘匿: プレイヤー UI は**本人分ごと** DTO 段階で落とす（app `state_view`、全 entity 分除外）／GM は `state_brief` の**〔秘匿:本人未知〕**注記で全員分見る＋GM_SYSTEM「当人にも明かすな（自覚・独白・気配としても描くな）・効果は原因を伏せて現象だけ描け」。validate `HiddenAttributeUndeclared`（宣言必須は secret と同じ）。PoC: `hidden_attributes_parse_and_validate_declaration`/`location_title_parses_and_defaults_empty`（gm_core）+ `state_brief_marks_hidden_attributes_and_gm_system_grounds_self_unknown`（harness）+ `state_view_drops_hidden_attributes_even_for_player_and_surfaces_location_title`（app、Red→Green）。**outcast 側 `package_spec.md` 追従済み**（作者向け仕様の写し）。ついで: MSRV 1.80 違反 `is_none_or`×2 を `map_or` へ（clippy clean 復帰）。
- ✅ **chronicle の retrieval 化 / spec 08 Phase A+B (2026-07-08)**: 長編で破れる「忘れない GM」の穴（予算 2400・新しい方優先 → 40 ターン超で序盤が「省略」に潰れる）を塞いだ。**A. 二層注入**: `history_note(history, HistoryQuery{action,location,present})` — 全量が予算内なら全文（従来同一）、溢れる長編では**直近 60% 無条件**（連続性）+ **古い経緯から関連 40%・上限 10 件を想起**（`(関連) T3 …` として再掲）。関連度 = memoria の文字 bigram TF-IDF cosine（`LoreStore::scores` を pub(crate) 共用化、決定論・追加 API ゼロ）× **タグ増幅（location 一致 ×2.0 / present 重なり ×1.5）**、足切り 0.05。関連 0 件は直近が全予算へ拡張＝旧挙動。クエリは `run_turn` が組む（呼び出し側無改修）。**B. 機械タグ**: `TurnLog` に engine 事実（`location`/`present`/`flags_set`=flag_turns 差分/`checks`/`items`=inventory 前後差分）を **`run_turn` が apply 前後から機械計上**（`ChronicleTags`、`TurnOutcome::Accepted.tags` で運ぶ）— 検索が LLM の summary 品質に依存しない。**全フィールド serde default＝spec 07 旧セーブ互換**。文書にタグも含めるので弱モデル fallback でも id の bigram 重なりで接地。正本境界不変（chronicle は確定した語り素材、可変状態は GameState 専有）。C（LLM 章要約圧縮）は見送り＝関連想起で拾えない伏線が定量確認できたら Phase 2（`specs/08_chronicle_retrieval.md`）。PoC: `history_note_two_layer_recalls_relevant_early_entry`（**60 ターンで T3 の銀の鍵入手が終盤の「鍵を使う」で想起される**＝ユーザー指定の測定ケース）/ `chronicle_tags_are_stamped_from_engine_facts` / `turnlog_without_tags_deserializes_for_old_saves`（Red→Green）。実 LLM での効き・ノイズ率は実プレイ計測が残。
- ✅ **判定結末文の GM 還流 (2026-07-08, 実プレイ発見 #41)**: authored 結末文つき判定（「見事に仕留めた」）はプレイヤーに表示されるが、`check_outcome_note` の二重語り回避（2026-06-26）が「再描写させない」と「**結果を知らせない**」を混同していて **GM がどのチャネルからも結末を知らなかった**（言語チャネル接地漏れの 5 例目）。ビート還流（2026-07-03）と同型の二経路で修正: `carryover_narration` が「（直後に判定の結末が確定した）」として継続文脈へ連結 + `chronicle_entry` が summary に「／判定の結末:」併記。`check_outcome_note` の除外は維持（あちらは「語れ」の要求経路＝役割分離）。一般化: **抑止したいのは再生成か認知かを区別する**。PoC: `check_outcome_narration_flows_into_carryover_and_chronicle`。`failures.md #41`。
- ✅ **move 回避学習の遮断 (2026-07-08, 実プレイ発見 #42)**: move が一度却下されると LLM が **move op を出すこと自体をやめ**、narration で「移動した気になる」（回避学習 → 語りと現在地の乖離が chronicle で確定事実化）。三点セットで修正: ①`RejectReason` の gate 未達系 4 種（Move/Flag/Item/ChallengeLocked）に **`requirement: Gate`** を載せ localize が「必要: …。満たせば move は通る」と条件そのものを語る（reason.rs に gate_ja/gate_en=却下理由の言語層。#31 の診断可能性の一般化）②`state_brief` が**いま通れる出口**を毎ターン動的 surface（「いま移動できる: corridor (move op を出せば必ず受理される)」、エンジンが gate 評価＝#37「現在形の事実+固有名」の移動版）③GM_SYSTEM に移動の正本規律（「移動は move op 受理時にだけ起きる／現在地の行が唯一の真実／語りと食い違うなら語りが誤り／語りだけで移動した事にしない」）。一般化: **却下は op クラスへの信頼を毀損する — 理由は「何がダメか」でなく「何をすれば通るか」を語る**。PoC: `gate_unmet_reasons_carry_requirement`（gm_core）/ `state_brief_surfaces_passable_exits_and_gm_system_grounds_move_truth`（harness、Red→Green）。`failures.md #42`。
- ✅ **逐次射影裁定 / spec 09 (2026-07-09 起草→rev2 査読→同日実装 = Done)**: 「拾って使う」が両方向で却下される catch-22（mujinto 実測 #43: 未所持時は束が `ChallengeLocked`＝requires がターン開始時点評価で同 delta の拾得を見ない／所持時は念のため再拾得が `ItemAlreadyHeld` で全体却下）を根絶。**A. 逐次射影**: `validate_ops` が op を**書かれた順**に検証し、受理した決定論 op（add/remove/give/set_flag/move/adjust/scale/cast_vote）を射影クローンへ仮適用してから次を検証＝**裁定は apply のドライラン**（受理 delta は必ずそのまま実行可能）。適用ロジックは新設 `apply_deterministic_op` を実 apply と**共有**（射影と実適用の乖離を構造的に防ぐ。apply の once 持ち去り記録も start_loc→逐次現在地へ）。**ダイス op（roll/check/attempt）は検証のみ・帰結非射影**（出目は apply 時確定・純粋裁定は RNG 非消費）→ 判定結果依存の後続手は次ターン。順序は提案者の責任（[組む,拾う] は却下）。**B. 既所持 add_item の no-op 受理**（`ItemAlreadyHeld` 撤去。複製穴の守りは `taken_items` で不変）。**C. prompt**: GM_SYSTEM「ops は書いた順に適用される／段取りは束ねてよい／判定依存は次ターン」+ `state_brief`「**この場でいま拾える**」動的 surface（#37 投票/#42 出口に続く現在形接地の第三例＝語りだけの拾得 #23 型の抑止）。**掟の改訂**: 原子性の核（全か無か・嘘の状態は作れない）は不変、「開始時点の一括評価」→「適用と同じ逐次評価」（2026-06-23 の「束ね却下＝勝ち筋」は合法な束への摩擦だったと再解釈）。ペーシングは authored 設計の責務＝**ダイスを置けば連鎖は必ずターンで割れる**（data_contract `adjudication.pacing`）。ユーザー決定の根拠: 束ねはトークン経済（1 ターン=フルプロンプト 1 往復、3 手 3 ターンは約 3 倍の入力）。PoC 5 本 Red→Green（射影束ね受理/順序却下/重複 no-op/ダイス帰結非射影/拾える surface）。`failures.md #43`（一般化: **認可と実行の意味論は一致させる — 認可は実行のドライランであるべき**）。実 LLM での束ね採用率・却下率低下は実プレイ計測が残。
- ✅ **あらすじ / spec 10 (2026-07-14 起草→rev2 査読 7+4 点→同日 Phase A〜D 実装 = 実測残)**: 「忘れない GM」の**第四層 = 長期の物語記憶** + ユーザー可視化（spec 08 Phase C の実体化）。chronicle の古い経緯を LLM で「章あらすじ」segment（`SynopsisEntry{upto_turn,title,text}`、**append-only**＝書き直しの複利ドリフトを構造的に断つ）へ圧縮し、`synopsis_note`「# これまでのあらすじ」を history_note の**前**に注入（予算 2000 字・新しい章優先・6 章目以降は最古から省略＝省略章の個別事実は spec 08 retrieval が全量 chronicle から補完）。**契機 2 つ**: ①モジュール遷移（章替わりマーカー前・遷移元 title・3 ターン未満は機械 join＝summary+location/items タグ併記）②あふれ（未圧縮 20 超→直近 10 温存、inclusive Range `[prev_upto+1, end]` で接続保証）。**遷移失敗は `pending_transition` へ範囲凍結・同一リトライ・拡張禁止**（前章の要約に次章が混入する事故の遮断。二重 pending は機械 join で強制消化）。**#47 防衛**: 要約入力を summary+機械タグ（08-B engine 事実）に接地し「発明禁止・表記固定・tail は文体接続のみ」、可視タブ自体が観測装置。`Summarizer` trait（DeltaProposer 同型の依存性逆転）＝実装は `LlmClient::generate`（プレーン生成・no-tools 両対応・15 秒タイムアウト・CoT 除去 #30 同型）。**要約モデル別指定** `SUMMARY_LLM_*`（model/base_url どちらか指定で有効・未指定フィールドは GM 設定継承・provider は実効 url から再判定。app=app_data/.env 正・CLI=`--summary-model` 引数>repo .env>GM 共用、app_data は探さない）。セーブ: `SessionSave.synopsis`（serde default=旧セーブ互換）。UI: 右ペイン**第 3 縦タブ「あらすじ」**（Ctrl+3・Ctrl+Tab 3 枚巡回・book アイコン）＝章列+「最近の出来事」（未圧縮 tail `T{n}: summary`＝GM の見る記憶とユーザーの見るものが一致）、差分 DTO（`GameView.synopsis` 全量 / `TurnView.new_synopsis+new_log` 追記差分＝frontend は push のみ、章確定時は 📖 system 行 + tail から呑まれた行を除去）、圧縮中は `synopsis-compacting` イベントで「あらすじをまとめています……」、設定 AI モデルタブに「あらすじ要約用」プロファイル選択（選択=即保存、空=GM 共用）。PoC 12 本 Red→Green（境界接続オフバイワンなし/凍結リトライ非混入/機械 join タグ/二重 pending/接地文言/400 字カット/注入位置=あらすじ<経緯/予算省略/リクエスト形状/セーブ roundtrip+旧互換/summary_overrides 継承）。**✅Phase E 実測 (2026-07-18 ユーザー実プレイ)**: 章圧縮後にあらすじ限定の出来事をプローブ — Anthropic/Gemini とも整合した想起で発明なし（GM が古い経緯を踏まえる）、Grok は想起整合だが過去章の**過強調** → `synopsis_note` に salience 規律（抑止+保護の対、failures #60、spec 14 未決2 と同時決着）。**あらすじ生成失敗の GUI 通知 (✅2026-07-18, ユーザーFB)**: 要約 LLM の失敗（規約違反等）は従来 `eprintln` のみ＝リリースビルド（コンソール無し）の一般ユーザーは**あらすじが永遠に作られないまま気づけない** → `run_synopsis_job` 失敗時に `synopsis-failed` イベント（エラー文字列 payload）→ frontend がトースト「⚠ あらすじの生成に失敗しました（プレイは続行・次のターンで自動再試行）」（`logToast` 既存経路、ja/en）。失敗は毎ターン高々 1 回なのでトースト再表示が「まだ失敗中」のシグナルを兼ねる。**Gemini ブロック理由の surface (✅同日, failures #61)**: Gemini は安全フィルタ/規約ブロックも **200+空応答**で返し、従来 decode は promptFeedback.blockReason を deserialize せず候補段 finishReason (SAFETY 等) も Other に潰していた＝全ブロックが「空の応答」に縮退し診断不能 → `gemini::block_reason`（二段判定の純関数）+ `LlmError::Blocked{reason}`（**非一過性**＝内容起因ゆえ再送で回復しない、EmptyResponse の一過性再抽選と分離）を新設、トーストに理由がそのまま載る。残課題: ブロックが恒久なら凍結リトライ（同一範囲）は永遠に失敗 → K 回失敗で mechanical_join フォールバック案（起票候補）。**✅要約タイムアウトの連鎖失敗を修正 (2026-08-26、ユーザー実機報告「長くなると要約の失敗が多くなる。一度失敗するとどんどんログが溜まっていく」、failures #87)**: **真因が二つ重なっていた** — ①`SYNOPSIS_TIMEOUT_SECS` 15 秒が遅いモデルに短く、**毎ターン 15 秒払って必ず失敗する**最悪の形になっていた (待ち時間の節約が目的なのに 一度も成功しないので全部捨てている。エピローグ 30 / 挿絵のプロンプト書き 60 から見ても 15 だけが外れ値) ②**あふれ契機の失敗が範囲を凍結していなかった** (`abandon` は遷移契機だけ凍結し、あふれは「次の受理ターンで再計算」) — `next_job` の end は `unc[len-1-KEEP_RECENT].turn` なので未圧縮が 1 本増えるたび範囲が 1 つ伸び、**失敗するほど 次のリトライが重くなる**正のフィードバック (使い捨て probe で実測: 5 ターンで end=11→16)。処方三点: (a) 既定 60 秒 + **`SUMMARY_LLM_TIMEOUT_SECS`** で上書き (b) **契機を問わず範囲を凍結** (フィールドを `pending` へ改名・旧セーブは serde alias で読む) (c) **同一範囲を 3 回失敗したら機械 join で確定して先へ進む** (`SYNOPSIS_MAX_RETRIES`) — (b) だけだと恒久的な失敗条件 (規約ブロック #61・恒常タイムアウト) で永久に詰まり毎ターン待たされる。**質の劣化より 停止の回避を採る**。一般化: **リトライはやり直す仕事の大きさを変えてはいけない** — 範囲をそのつど再計算する設計は 正常時は自己修復的だが、対象が時間とともに増える場では失敗が次の失敗を確実にする。凍結と逃げ道は対で入れる。PoC 3 本 (あふれ凍結で範囲が伸びない / K 回で機械 join へ落ちて先へ進む / 旧セーブ alias)。**✅待ち時間の上限を UI から選べるように (2026-09-01、ユーザー報告「60 秒でも足りない」)**: 機構は 2026-08-26 の `SUMMARY_LLM_TIMEOUT_SECS` で既にあったが**env を手で書くしか道が無かった** (配布ビルドの受領者には実質存在しない設定)。設定「AIモデル」タブのあらすじ節に select (既定 60 / 90 / 120 / 180 / 240 / 300 / 600 秒)。**command はプロファイル選択と分けた** (`set_summary_timeout`) — 「GM と同じ」に戻す保存は base_url/model/api_key を**全部空**で書くので、同じ command に相乗りさせると待ち時間の指定が巻き添えで消え、**画面には何も出ないまま次の長い章でまた落ちるまで気づけない**沈黙の後退になる (spec 26 の「写像を凍結する」と同じ線引き)。**0 = 空 = harness の既定へ** (既定値を app 側に焼かない = 画像生成の `timeout_secs: None` と同流儀)。UI 文言は代償を明示する — この秒数だけ次の語りが待たされうること・失敗時は毎回その時間を払うこと・3 回で機械 join へ落ちること。PoC 1 本 (app: 待ち時間とプロファイルが互いを消さない両向き + 0 は空)。**残**: 圧縮は今も `play_turn` を `.await` で塞ぐので、上限を上げるほど成功時の待ちも伸びる (バックグラウンド化は spec 10 rev2 の見送り判断ゆえ覆すなら別 spec。**上限を選べるようにしたことでこの見送りの代償が可視化された** — 600 秒を選べる以上、次に効くのは非同期化)。設計は `specs/10_synopsis.md`、契約は data_contract `Synopsis` 節。
- ✅ **エピローグ / spec 11 (2026-07-14 起草→rev3 査読 (重大3+不整合4+軽微3+追補3)→同日 Phase A〜C 実装 = 実測残)**: goal 到達の即ブツ切り（不完全燃焼）に**このプレイスルー固有の回想と余韻**を足す。**「いつ」は engine・「何を」は LLM** — 到達判定は `reached_goal()` の専権で GM に「終わったと思ったら」を判断させない（ユーザー原案の「エピローグフラグを GM が立てる」は弱モデル不発=ソフトロック・全作者への配線負担で棄却、機構化を採用）。`GoalDef.epilogue_prompt: Option<String>`（authored **生成指示**・本文ではない・serde default。旧形式 `goal: Gate` への記述は serde 黙殺 + unknown_key_lints が警告）。**narration（結末文）は土台** — 生成失敗時のフォールバック + 結末の意味の接地素材。指示あり+結末文なし（`trim().is_empty()` 基準）は lint `EpilogueWithoutNarration`（非 fatal）。発火 = 到達 + **終端**（単発 / campaign の advance 辺なし＝判定は呼び出し側 app/CLI の責務。遷移 goal では発火しない=遷移時は追加演出なしが v1 判断）。生成 = `build_epilogue_request`（素材 = goal 結末文 + synopsis 予算 2000 字 + 未圧縮 chronicle tail 予算 2400 字 + last_narration + world/protagonist ＝通常ターンの注入量と同等でタイムアウト率を悪化させない）→ `generate_epilogue`（**GM の client**・30 秒・CoT 除去・失敗 skip=非致命）。規律は synopsis と線引きが違う: **「起きたことは記録のとおりに・これから起きること（後日談）は自由に」**（終幕ゆえ #47 汚染経路なし、後日談こそエピローグの価値）。表示順 = **結末文 → 🎉 バナー → ―― エピローグ ――（エピローグで幕）**。会話ログにのみ積む（chronicle/SessionSave には積まない・生成は autosave の**後**）。`TurnView.epilogue` + `epilogue-writing` イベント「エピローグを紡いでいます……」+ CLI 両終端 println。PoC 4 本 Red→Green（parse+lint 空基準 / 旧形式 lint 回帰 / リクエスト形状+規律文言+圧縮済み範囲除外 / 予算+空節省略）。**Phase D 残**: epilogue_prompt 付き盤面のドッグフード + 実 LLM で記録矛盾ゼロ・後日談の質・生成時間 + GUI/CLI 目視。設計は `specs/11_epilogue.md`、契約は data_contract `Epilogue` 節。outcast package_spec.md は ✅追従済み（2026-07-20 確認: epilogue_prompt 欄あり）。
- ✅ **d100 ロールアンダー判定 / spec 16 (2026-07-19 起草→CoC7 原典査読 (Must3+Should4+Nit2 全反映)→同日 Phase 0〜E 実装→2026-07-20 実 LLM 実測 4 周 = Done)**: CoC 系「1d100 ≤ 技能値」の判定様式。**技能 = 数値 stat**（目星 60 を stat で持つ＝新概念不要）+ 既存ロールハイは不変・別モード併存。①**`StateOp::CheckUnder{entity,key}`**（LLM 提案可の即興 d100 ロールアンダー、目標値=stat 現在値、帰結なし）+ **成功度 degree** を engine が決定論計算（critical=01 常成功・先勝ち判定/extreme≤値÷5/hard≤値÷2/regular/failure/fumble=目標値<50 なら 96-100・≥50 なら 100。整数除算=原典どおり、`percentile_degree` 純関数）。②**`ChallengeDef.resolution: percentile`**（stat 必須・sides/dc 禁止・tiers 併用禁止を validate 3 種で保証、sides/dc は serde default 化し additive の必須性も `ChallengeShapeInvalid` へ移管）+ **degree 別帰結スロット** `on_critical/on_extreme/on_hard/on_fumble`（フォールバック連鎖 critical→extreme→hard→on_success / fumble→on_failure、`resolve_degree_slot` を apply と射影が共有）。modifiers は**目標値加算**（原典ボーナスダイスは分布の異なる近似と明文化、将来は ChallengeMod 側に dice を足す）。spec 09 の全帰結共通射影は 6 degree の解決先で厳密交差。③**`StateOp::RollStat`**（可変量ダイス `count×d(sides)+bonus` を stat へ clamp 適用=SAN 1d6 減少。**AUTHORED_ONLY_OPS 第 6 例** `StatRollNotAllowed`、監査は `StatRollOutcome`→`TurnView.stat_rolls`→「🎲 SAN -4 (1d6=4)」表示）。④**`Scenario.check_style: percentile`**（opt-in の提示/語彙スイッチ: schema の check↔check_under 入替=`LlmClient::set_excluded_ops`+`state_delta_schema_excluding`、app/CLI が new_game/遷移で設定。scenario_brief「## 判定様式」で「低いほど良い・DC を自分で決めない」接地。**engine は却下しない**=様式は規約であって整合性ではない）。degree の ja 表示はカタカナ表（クリティカル/イクストリーム成功/ハード成功/成功/失敗/ファンブル、内部 id 英語=提示層 3 経路で変換）。check_outcome_note は degree 書式「d100=23 ≤ 目標値60 → ハード成功」に分岐。ドッグフード: `packages/lakeside_manor/`「湖畔の洋館」（SAN 1/1d6・真相/発狂 goal・epilogue、動線は使い捨てテストで Green）。PoC 9 本 Red→Green。**✅実 LLM 実測（2026-07-20 ユーザー実プレイ 4 周・核心的未知 3 点とも陽性）**: 様式混同 0/4 周（DC 発明・「高い方が良い」回帰・却下とも皆無=schema 入替+様式節が機能）／degree の語り良好（ハード/イクストリーム成功の劇的翻訳・失敗の因果後付けが毎回別内容）／🎯 d100・🎲 SAN 表示・エピローグの SAN 残量条件付けも目視 Green。**実測 FB 2 件を同日修正**: (A) 発狂エンド到達不能 — altar_gaze の全帰結スロットが同じフラグを立てており初回の覗きで必ず真相到達（**帰結スロットが全て同一フラグだと分岐が死ぬ=authoring 定石**）→ on_failure の truth_witnessed を撤去し「素の失敗=像を結ばない・SAN 消耗のみ・再挑戦ループ」へ（使い捨て PoC Red→Green）。再プレイで目標値 60→54→52 と生きて下がる消耗スパイラルを実証（SAN<50 でファンブル帯 96-100 拡大も既存ルールで創発）。(B) 即興 check_under の再挑戦スパム — 同一目星を 3 連発できた（CoC7 RAW は同条件再ロール不可・プッシュのみ、failures #62）→ 判定様式節に**抑止+保護の対**（「同じやり方の振り直しは判定を出さず『結果は変わらない』と接地」+「別の技能・手段・状況の変化なら新規判定」）。対照検証: 規律前=同一目星 3 連発／規律後=振らずに接地→新手段（聞き耳）には新規判定、authored challenge の再挑戦（SAN ループ）は目標値変化=状況の変化で規律と共存。**寝かせ**: 成功側 SAN -1（challenge effects の adjust_stat）が会話ログ不可視（状態パネルでは見える・優先度低）／check_under へのプッシュ拡張は CoC7 v2。**✅v2 の設計課題が 1 つ立った (2026-08-29、外部実装 OpenCOCKeeper の調査から)**: 対抗・プッシュ・幸運消費は「振り方」の話だが、**狂気規則だけは別の欠落に当たる** — 一時的狂気 (1 回の判定で SAN を 5 以上失う) も不定の狂気 (開始 SAN の 1/5 を失う) も、**stat をリテラルでない何かと比べる**ことを要求する。`Gate::StatAtLeast/AtMost` の比較相手は `value: i64` のリテラルで式を置けず (spec 19 の `expr` は `ChallengeDef` と `RollSpec` の 2 箇所のみ)、`RollStat` の変化量は `StatRollOutcome` として提示層と次ターンの prompt には出るが **`GameState` のどこにも残らない**ので gate から問えない (`RecordTurn` はターン番号を刻む op で、stat の値を写す op は無い)。取りうる形は (a) gate の比較相手を式へ広げる (spec 19 の評価器がそのまま使える・不定の狂気は解けるが一時的狂気は解けない) / (b) 変化量を帳簿 stat へ残す (`internal_stats` が受け皿・両方解けるが帳簿が増える) の 2 通りで、選択は v2 の判断。詳細は specs/16「狂気規則が要求する『比較の相手』」節。副産物: 湖畔 4 周のエピローグ全て記録矛盾ゼロ+SAN 条件付け反映=spec 11 Phase D の実測も事実上 Green。設計は `specs/16_percentile_checks.md`、契約は data_contract（StateOp/ChallengeDef/check_style/adjudication 節）。
- 🟡 **プレイヤーが振るダイス / spec 18 (2026-07-20 起草→同日査読 rev2→Phase A 実装済。B/C は Draft)**: TRPG の緊張感の欠如（正本が振ってしまう）をユーザーFB から三段で解く。**接地の核心 = seeded RNG ゆえ出目はいつ振っても同じ／ガチャも結果はサーバ確定済みで演出は開帳**（サンプル HTML 自身が「物理の面は読まずデコード演出で数字を注入」と自認 = 正本が決め提示層が開帳する設計の証明）。**✅Phase A 開帳 (frontend のみ・engine 無改修)**: ダイス系 3 行（rolls/checks/stat_rolls）に `revealed` カウンタ + 伏せカード `DiceReveal.vue`（クリック→数字スクランブル 1.1s 減速→確定出目に着地→本物の行へ差し替え）。1 クリック 1 判定の直列（`revealTargetIndex`）。**漏洩防止が実装の過半**: ダイスより後ろの行（ビート/goal バナー/遷移/エピローグ）+ ビート SE + イベント CG/BGM を `pendingTail`/`pendingSe`/`pendingVisual` に保留し全開帳で flush（発火 CG は判定の帰結でありうる）。結末 SE は開帳と同時。未開帳中は入力欄ロック（`hasUnrevealedDice`）、設定グラフィックにオン/オフ（既定 on・オフ切替=revealAll=脱出口）、新規/再開/ロードで保留破棄（揮発・リロード=自動開帳は許容と明記）。**ダイスを振る音 (✅2026-07-23、ユーザー提供 `doubledice3.ogg`)**: 開帳クリックでスクランブルと同時に one-shot（出目より先に音が出ても帰結は漏れない）。**アプリ同梱の UI 音**として `app/src/assets/` に置き Vite が emit（CSP は既存の `media-src 'self'` で足りる）— パッケージのアセットにしなかったのは、開帳カードがどの盤面でも出る提示層の仕掛けだからで、authored にすると「音を入れていない配布物だけ無音」になり汎用音を全パッケージが複製する（アセットの WebP/Ogg 作法＝配布サイズの反省と同じ向き）。再生は `store.playSe` 経由＝サウンド設定の音量/ミュートがそのまま効く。作者による差し替えは未実装（要れば `Scenario` 宣言で上書きする経路を足せる）。既知の残漏洩=右ペイン状態パネルの即時更新（実プレイで測る）。**✅Phase B 選択 (同日実装・実 LLM プレイ残)**: CoC7 プッシュロール+差分買い=**第三の権能「プレイヤー op」**（LLM 提案でも authored 専権でもなく、プレイヤーが UI から直接 engine に入れる決断。LLM 非関与=トークンゼロ）。**中枢=帰結の確定遅延**: pushable/spendable challenge の素の失敗（fumble/tier 該当は final・player 主体のみ・実行可能な選択肢が在る時のみ）は帰結を適用せず `GameState.pending_decisions` に凍結（raw_roll 必須=差分計算の基準・セーブ対象）、`resolve_decision(Accept/Push/Buy)` が確定時にスロット適用→settle→flag_turns。プッシュ=1 度だけ・final・失敗連鎖 `on_push_failure`→`on_fumble`→`on_failure`（代償の本線。`push_cost` は任意の上乗せ・既定 None=原典準拠）。買い=percentile 段階制 regular/hard/extreme（閾値 dc, dc/2, dc/5）/additive 一段、払える段だけ提示（上限=現在値−宣言 min・払って死ぬ設計も可）、critical/fumble 不可・プッシュと排他。支払い元は authored（`spend_rules.from`=自由な stat キー、LUCK 固定変数を作らない・ユーザー決定）。**pushable 既定は false へ実装時改訂**（rev2 の true から: 既存 content の挙動と spec 09 共通効果射影を黙って変えない+一発勝負の事故防止。決断つき challenge は射影から除外=安全側）。**副産物 2 件**: authored_only_flags が degree スロットのフラグを見ない #50 バックストップの穴+lint の degree スロット内盲点を `all_outcomes()` 集約で閉鎖。GM 還流=check_outcome_note「押して振り直された/代償を支払って買い取られた」（決断は物語の素材・pending は還流しない）。app=`TurnView/GameView.decision`+`resolve_dice_decision`（確定後 autosave・chronicle 併記・play_turn は決断待ち中拒否）/frontend=DecisionPanel（全開帳後に表示・**プッシュの振り直しはもう一度伏せカードで開く**=Phase A 機構の使い回し）/CLI=自動 Accept。v1 制限=決断経由の goal で campaign 自動遷移・エピローグ生成はしない。PoC 8 本、workspace 277 green・app 22 green・build green。ドッグフード=dice_trial（spend_rules 幸運 30+全 challenge pushable+on_push_failure）。**CoC7 完全準拠は謳わない**（ボーナスダイスは目標値加算近似のまま）。**✅Phase C 交互振り (同日実装・GUI/実 LLM 目視残)**: 一括型 cadence＝`ContestDef`（opponent/player_roll・opponent_roll=RollSpec inline or `CharacterDef.rolls` テンプレート/on_win・lose・tie=ChallengeOutcome 再利用/until/max_rounds 既定 1）+ `AttemptContest`（LLM は開くだけ）+ `contest_round`（1 交換=双方振り→比較〔additive=合計/percentile=degree 順位→同位は目標値高い側=CoC7 準拠〕→帰結原子適用→settle。決着=until/max_rounds/goal 安全弁）。**様式は contest 単位で双方適用**（spec の「テンプレート単位」から単純化＝様式跨ぎ対抗が構造的に不存在）。digest 1 行だけを carryover+chronicle へ（現在値は state_brief が運ぶ＝重複させない）。ContestPanel ⚔＝player の振りは伏せカード・相手は開帳後即開示・ラウンド毎 autosave・入力は決着まで三層ロック・CLI 自動回し。帰結フラグ authored_only+validate 3 種。ドッグフード=dice_trial gravel_brawl。v1 外=逃走/乱戦/技選択（選択が欲しい戦闘は逐次型 challenge で）。PoC 4 本、workspace 281 green。**cadence は作者専権**: 逐次型（毎交換 LLM）=challenge/一括型（トークンゼロ）=contest。設計は `specs/18_player_dice.md`。
- ✅ **主人公 stat の境界つき宣言 (2026-07-20, ユーザー指摘)**: 「なぜ player の stat は上限をつけられない？」→ `initial_stats` が StatDecl より古い素の i64 マップだった**歴史的非対称** (NPC は境界宣言可・player は不可、台帳にも「境界が要れば後続で」と先送り明記) を解消。`StatInit` (untagged) で**素の数値と `{ initial, min, max }` の両受け** — 従来形 `hp: 10` は無改修で挙動不変 (後方互換=ユーザー条件)、境界つきは SAN 上限 99・負の min (借金) が書ける。`stat_bounds(player)` が宣言境界を読み clamp は adjust/scale/roll_stat 全経路 + spec 18 差分買い上限に自動で効く。package `player.stats` も同型。lint は mapping 値だけ StatDecl 検査 (StatMap 意味論)。PoC: `player_stat_bounds_via_initial_stats_decl` (上限 clamp/負 min/従来形に上限は生えない)。workspace 286 green。
- ✅ **複数ダイス×乗数 (2026-07-20, ユーザー要望「3D20×5 とかできる？」)**: challenge/contest の出目を `{count}d{sides}×times` に一般化 (`count`/`times` 既定 1=完全後方互換)。**乗算は素の合計だけ**に掛かり修正は後から加算 (3D6×5 の CoC 系)。tier は素の合計で判定 (min=全部 1・threshold 範囲 1..=count×sides)、percentile は 1d100 固定 (count/times 指定は load 拒否)、プッシュの振り直しも同じ式 (PendingDecision に素性を持たせた・serde default=旧セーブ互換)。表示は 3 経路 (prompt note/CLI/GUI) とも「3d6(合計11)×5」、既定は従来書式のまま。PoC: `challenge_multi_dice_with_multiplier`。workspace 287 green。**✅即興判定との線引き (2026-07-23, ユーザー質問「3D6 の最低値は 1 になってしまわないか」)**: 正本は無実 — authored 経路 (challenge/contest/roll_stat) は `count` 個を個別に振って合計するので 3d6 は 3〜18、tier の `Min` も `roll == count`（全部 1）で判定する。**穴は即興側**: LLM が提案する `check`/`request_roll` op に `count` が無い（1d{sides} 固定）ため、GM が 3D6 相当を出そうとすると `sides: 18` と書くしかなく、engine は忠実に振って**一様分布 1〜18**になる（最低値も平均も分散も別物・様式は整合性ではないので engine は却下しない）。**解は prompt 接地のみ（engine 不変・schema 不変＝キャッシュの安定プレフィックスも動かさない）**: GM_SYSTEM に「即興 check は 1 個のダイスだけ／合計ダイスが要る判定は attempt_challenge を選べ／**d100 は 1d100 と書く**」を追加。d100 が 1d でよい根拠は原典側にある — CoC7 の 10 面体 2 個は**十の位と一の位の位取りであって合計ではない**ので結果は 1〜100 の一様分布＝1d100 と同値（ユーザーが原典で再接地）。一般化: **「2 個振る」には合計と位取りの二種類があり、混同すると分布が静かに入れ替わる**。ゆえに `count` の使いどころは合計の分布そのものを難易度設計に使う系（2d6+修正・3d6 ロールアンダー）だけで、d100 は含まない。**同時に台帳ドリフトを回収**: 2026-07-20 の count/times 追加が本節と package_spec は更新したのに、`Natural`/`TierDef`/`ScenarioError` の doc comment と validate のコメント（`roll == 1`・`1..=sides`）を取りこぼしていた＝掟の「追加側の穴」の実例（実装が台帳に触れない作業では追加でも落ちる）。PoC: `gm_system_limits_improvised_checks_to_a_single_die`（Red→Green）。**既知の残穴（優先度最低）**: `validate` の threshold 範囲は `1..=count×sides` だが到達可能帯は `count..=count×sides` なので、3d6 に `at_most: 2`（永久に不発火）や `at_least: 3`（常時発火）を書いても load を通る。package_spec に作者向け注意として明記して据え置き。
- ✅ **式修正 / spec 19 (2026-07-20 起草→同日実装 = Done)**: CoC7 記事を受けたユーザー要望「割り振りは YAML 手書き・**ダイスのときの演算**が欲しい (CON+SIZ/2 で補正)」。`ChallengeDef.expr`/`RollSpec.expr`（stat と排他）に `(CON + SIZ) / 2` の整数式を書くと、engine が**判定のたびに現在値で評価**して additive=修正値/percentile=目標値にする（CON が削られれば補正も落ちる=生きた派生値。手書きシートとの本質差）。文法=整数・stat 名 (日本語可)・四則・括弧・単項マイナス（全角 × ÷ （） 受理）、除算は切り捨て (CoC 準拠)、リテラル /0 は load 拒否・実行時ゼロ除算は 0。**authored 専権**（LLM は式を持てない=閉世界の線）+ 参照 stat は主体の宣言済みキー必須（load 時 `ChallengeExprInvalid`/`ContestRollInvalid` + 裁定時 UnknownStat の二層）。実装=`gm_core::expr`（依存ゼロ純関数パーサ）+ `stat_or_expr()` 3 適用点。prompt/schema 不変=キャッシュ影響ゼロ。スコープ外=DB 帯テーブル（modifiers の Gate 帯で書ける）・派生 stat 初期化（作者手書き=ユーザー決定）。PoC 4 本 Red→Green、workspace 285 green。設計は `specs/19_stat_expressions.md`。
- ✅ **否定 gate `not { of: Gate }` (2026-07-24, ユーザー要望「アイテムを持っていないときの gate は?」)**: `has_item`/`has_skill`/`attribute_is` には否定が無く、汎用 NOT も無かったので「〜を持っていない」は**フラグでの二重管理**（拾得トリガーで `set_flag` して `flag_is false` を見る）でしか書けなかった — 渡した/使った時にフラグを戻し忘れる**ズレやすい**回避策で、CoC 系の「お守りを持っていなければ襲われる」に耐えない。`all`(AND)/`any`(OR) に `not`(NOT) を足して**ブール代数を閉じ**、任意の gate を否定できるようにした（`Gate::Not { of: Box<Gate> }` + `eval = !of.eval`）。部分的な逆（`flag_is value:false` / `stat_at_most`）は既に在ったが、それらは**個別 gate に生えた双対**であって組み合わせ子ではない＝一般化がこれ。`unmet` は catch-all がそのまま拾い（All 以外は自身を返す＝not は葉として名指しされる）、`scan_gate`（幻 location lint）は not の中へ再帰、lint 標本+網羅 match の番人・`reason.rs`(ja/en)・harness `gate_brief` も追従。PoC 2 本 Red→Green（`all + not + has_item` のユーザー実ケース「player_room にいて、かつライブチケットを持っていない」/ not の中の幻 location を lint が拾う）。app backend は Gate を網羅 match していないので無改修。
- ✅ **トリガーからの判定起動 + authored effects の死んだ参照 lint (2026-07-25, ユーザー要望「trigger からチャレンジ実行させる」)**: 要望された機能は**元から動いていた** — トリガー効果に `attempt_challenge` を書くと `fire_triggers` → `apply_ops` 直行でダイスが振られ、帰結スロット・degree・effects・pending decision まで通常経路と同一に効き、`CheckOutcome` も同じ `checks` に積まれて提示層と還流に載る（PoC 実測: seed 7 で `1d20(8)+敏捷3=11 vs dc10 成功`、帰結フラグ着弾、`fired` にトリガーも載る）。「罠が発動して回避判定」「時間経過で襲撃判定」を筋書き側から起こせる＝**engine 改修ゼロ**、台帳に無かっただけ。**停止性は既存の二層**（settle 内 `fired_this_settle` で各トリガー高々 1 回 / challenge→challenge の入れ子は `ChallengeEffectRecursive` が禁止）。**ただし `requires` は評価されない** — トリガー効果は authored 信頼済で検証を通らないので、同じ challenge を LLM が選べば `ChallengeLocked` で却下されるのにトリガーからは通る（authored 専権の一貫した帰結だが、作者には「`requires` を書いたのに効かない」に見えるので明文化した）。**同時に穴を一つ塞いだ**: 検証を通らないということは **id の typo も沈黙する**ということで、challenge 版は「トリガーは発火して narration だけ出て判定が起きない」（作者には『たまに判定が出ない』としか映らない）、**contest 版はより重く幻の対決が `pending_contest` に居座り以後の対決が全部 `ContestInProgress` で却下される**（沈黙でなく盤面の停止）。未知フィールド lint の射程外（キーも値も文字列として正しく参照先が無いだけ＝撤去した enum 値が lint に届かないのと同じ機序）。`ScenarioError::UnknownChallengeInEffects` / `UnknownContestInEffects` を **lint（非 fatal）** で新設し trigger effects / challenge の全帰結スロット・tier / contest の on_win|lose|tie を走査、app が作者向け文面で開幕 ⚠ に出す。**fatal にしないのは `FlagHintOnAuthoredOnly` の降格と同じ判断** — この typo を含む配布済み content が受領側で「開けなくなる」方が沈黙より悪い。PoC 2 本 Red→Green（Red は使い捨てテストで `validate errs = []` / `lints = []` / `checks = []` を実測）。
- ✅ **トリガーからの場所移動 + 死んだ行き先 lint (2026-07-27, ユーザー質問「trigger でロケーション移動はできますか？」)**: これも**元から動いていた** — トリガー効果に `move` を書くと `apply_ops` 直行で `location` が変わる（使い捨て PoC で実測: フラグ 1 つ立てただけで `cell` → `pit`）。落とし穴・転移・気絶して運ばれる・場面転換を、出口を偽装せずに書ける。**判定起動（2026-07-25）と同じ「機構は在るのに台帳に無かった」類型**で engine 改修ゼロ。**LLM の move と違う点が二つ**: ①**出口も gate も見ない**（実測: `pit` は `cell` から出口が無いのにトリガーは運んだ／同じ `move` を LLM が提案すれば `NoExit` で却下）＝authored 専権の一貫した帰結 ②揮発 presence の破棄は `Move` の適用点そのものなので、誰が動かしても同じく効く。**同時に三つ目の穴を塞いだ**: 行き先の typo（`to: piit`）は `validate` も `lints` も拾わず、主人公は**宣言されていない場所に立つ**（出口ゼロ・説明文なし＝ソフトロック、エラーも警告も出ない）。`ScenarioError::UnknownLocationInEffects` を **lint（非 fatal）** で新設し、2026-07-25 の `scan_refs`（trigger effects / challenge の全帰結スロット・tier / contest の on_win|lose|tie）に相乗り。**同じ日に同じ形で二つ塞いだのに三つ目が残っていた** — 死んだ参照の一族は「authored effects は検証を通らない」という一つの根から生えるので、**op の種類ごとに一つずつ気づくのでなく、`StateOp` のうち id/名前で外部を参照するもの全部を一度に洗う**のが正しい形（残りの参照 op に穴が無いかは `scan_refs` の match を見れば分かる）。PoC 1 本 Red→Green（トリガー効果と challenge 帰結効果の両方で名指し＋宣言済みの行き先には出ない偽陽性なし）。bundled_packages テストが全同梱パッケージで `warnings` 空を保証（実 content で偽陽性ゼロ）。
- ✅ **揮発 presence / 来訪者と同行者 (2026-07-25, ユーザーFB「set_presence にさせちゃうとずっと着いてきてしまう」)**: spec 04 が「スコープ外: per-location override（現状 override はモジュール内全 location に効く）」と先送りした穴が実プレイで顔を出した。**真因は層の非対称** — `Location.present` は場所ごとの authored 宣言なのに、それを上書きする `present_overrides` だけが**場所を持たない**ので、一度登場させるとモジュール中どこへ行っても付いてくる（＝「同行者」しか表現できない層だった）。その瞬間だけ登場させたい来訪者には、退場条件のトリガーを別途書く運用負担が要り、書き忘れれば永久同行。**解**: `StateOp::SetPresence` に `volatile: bool`（serde default false ＝既存 YAML 無改修）を足し、`present_overrides` の値を **untagged 両受け**の `PresenceOverride`（`Persistent(bool)` ＝旧形式がそのまま読める／`Volatile { present, at: LocationId }`）へ。揮発は**適用時の現在地に紐づき、そこを離れた瞬間に破棄**され、実効 presence は `Location.present` の土台へ戻る（再訪しても蘇らない）。作者の語彙が「**同行者**（仲間になった・永続）／**来訪者**（この場限り・自動で消える）」の二つになった。**破棄は `apply_deterministic_op` の `Move` 適用点**＝spec 09 の逐次射影と実適用が共有するので、裁定のドライランと実行が構造的に一致する。`transition` は永続分だけ持ち越し（揮発は前モジュールの場所に紐づくので捨てる）。**`ResolveVote` の死亡投影は永続で書く**（揮発だと場所を変えた瞬間に処刑された者が蘇る＝実装時に気づいた分岐）。旧セーブ互換は `LocationItem` の旧/新形式両受け・`StatInit` の素の数値/境界つき宣言と同じパターンで、`present_overrides: { alice: true }` が `Persistent(true)` として読める。harness/app/frontend は `present_at` を呼ぶだけなので**無改修**。**期限を「場所を離れたら」にした根拠**: 「そのターンだけ」はトリガーが GM の語りの**後**に発火するため来訪者が喋る前に消える／「N ターン」は `record_turn` + `turns_since` で今日書けるので engine に置く価値が薄い。PoC 2 本（揮発の失効と土台への復帰＋同一テスト内に永続の対照群／旧セーブの untagged 互換）。既存 320 本は**回帰ゼロ**で通過。
- ✅ **ジオラマ template のための `"*"` sentinel (2026-07-28, ユーザー発案「簡単なジオラマみたいな、あとはキャラファイルを足せば動く環境シナリオ」)**: 敷居の話から出た形 — 場（`scenarios/`）と人（`characters/`）が別ファイルという**既にある継ぎ目**を、作者に見える単位にする。ただし「キャラファイルを足せば動く」は文字どおりには成立していなかった（`cast` と `Location.present` の**二箇所**を編集する必要があり、しかも **`present` の typo は `present_at` の retain で黙って落ちる**）。`gm_core::WILDCARD = "*"` を新設し二箇所を埋めた: **`cast: ["*"]`**（`harness::inject_cast` が `characters/` の全ファイルを注入。明示 id と併記可＝typo った明示 id は従来どおり load エラーで大きな声で落ちる／inline 優先は不変）/ **`present: ["*"]`**（`present_at` の土台を「このシナリオが知る登場人物全員」に。**override は上に重なる**ので「全員 − 一人」が書ける）。**2026-07-02 に廃した「空なら全 characters」の暗黙フォールバックとは別物** — あれは無人の場所を作るのに全キャラを `set_presence false` する羽目になったから消した。**明示宣言なら書かない場所は今までどおり無人**なので、あの問題は戻らない（これが「復活」でなく「新設」である根拠）。キャラ id の `"*"` は `validate` が `ReservedWildcardEntity` で禁じる（sentinel と衝突すると到達不能なキャラになる）。**goal は機構でなくテンプレートの書き方で解く** — 当初「終わらない盤面を書けるように goal を任意化」を提案したが、**ユーザーがエピローグ（spec 11）を惜しんで止まった**（goal 到達が epilogue の発火点なので、goal を捨てると余韻ごと捨てる）。要るのは「終わりが無いこと」でなく「終わりたくなった時に幕が引けること」で、`終幕` フラグ＋`flag_hints`＋`goals[].epilogue_prompt` で書ける（専権フラグでないので GM の語彙に出る／早すぎる幕引きが怖ければ `flag_rules: { 終幕: turns_since(未記録キー, N) }` が実質「N ターン目以降」の下限になり、未達なら 2026-07-25 の「いまはまだ立てられないフラグ」行が毎ターン出る）。PoC 2 本 Red→Green（cast の全員注入＋sentinel 自身は登場人物にならない / present の全員＋未宣言は無人のまま＋override が上に重なる＋予約語）。**残**: ジオラマ package のドッグフード一枚（書けば四つ目の小石が出る想定）。
- ✅ **`presence_is` gate — 「この場にいるか」を条件にする (2026-07-28, ユーザー要望「いないキャラを『〇〇が帰った』と出したくない」)**: presence は spec 04 で可変状態へ昇格していたのに、**gate から問う口だけが無かった**（`Location.present` ± `present_overrides` の合成を UI と prompt は読むが、条件式は読めない）。用途の核心は**筋書きのビートを不在のキャラに出させない**こと — 退場の語り・別れの挨拶を、その場にいない相手に発火させない。譲渡や会話の前提にも効く。`Gate::PresenceIs { entity, present(既定 true) }`（`present: false` で不在を直接問える＝`not` で包まなくてよい／`entity` は必須＝主人公は常に居るので既定に意味がない）。**構造上の壁と判断**: 実効 presence の土台は scenario 側（`Location.present`）なので `Gate::eval(&GameState)` からは半分しか見えない。party gate は「ロスターを GameState に置く」で signature 改修を避けたが、**presence では採らなかった** — 場所→present の表を state に写すとセーブに複製が残り、**spec 17 のパッケージ更新で作者が `present` を直しても古いセーブは古い顔ぶれのまま**になる（`party` が既に抱える小さなドリフトを増やす）。よって `Gate::eval(&self, s, sc)` / `unmet(&self, s, sc)` へ**シグネチャを変えた**（呼び出し 25 箇所・全部コンパイラが名指し・既存 gate は `sc` を読まないので挙動不変）。**同梱 lint `UnknownEntityInGate`**: `present_at` は cast 注入されていない entity を落とすので、幻のキャラを問う gate は永久に偽（`present: false` なら永久に真）＝どちらも死んだ条件。`scan_gate` の走査に相乗り（非 fatal・`UnknownLocationInGate` と同じ一族）。**揮発 presence（来訪者）にも追従**する — 場所を離れれば gate も偽へ戻る。PoC 1 本 Red→Green（土台/override/揮発の失効/トリガー when での発火/幻 entity の lint）。
- ✅ **フラグ前提の現在形接地 (2026-07-25, 実プレイ却下から)**: `flag_rules` の gate 未達で `set_flag` が却下される（実例: 「ライブでユイファンを盛り上げる」が `all(location_is stage, flag_is ネットを見る)` 未達で 1 回目却下 → GM は 2 回目で筋を通したが 1 往復ぶんの無駄）。**真因は語彙節の静的性**: 使えるフラグを列挙する `scenario_brief` は**静的 system ブロック＝キャッシュの安定プレフィックス**なので state 依存にできず、機の熟していないフラグも常に見える。ユーザー案の「rules 未達なら GM に見せない」は**採らなかった** — そのフラグの `flag_hints`（どうすれば立つか）も道連れに消え、**前提を満たしに行かせるための情報が前提未達だから消える**という循環になる（加えて隠すと幻フラグ発明側に倒れうる＝2026-07-03 に語彙提示を入れた理由）。代わりに **#42 の処方をそのまま適用**: 「何がダメか」でなく「何をすれば通るか」を、**可変側の `state_brief`** で毎ターン言う — 「いまはまだ立てられないフラグ: {id}（表示名）→ 必要: {gate_brief}」。gate が真になれば行から消える＝立ててよい合図。**投票 (#37) / 出口 (#42) / 拾得 (spec 09-C) に続く現在形接地の第四例**で、engine 無改修・静的プレフィックス不変＝キャッシュ影響ゼロ。除外は 既 true（立てる話でない）/ `flag_rules` 無し（常に立つ＝ノイズ）/ `hidden_flags`・`internal_flags`（語彙節と同じ）。**即効の代替も併存**（コード変更なしで `flag_hints` に前提を書く＝静的側なのでキャッシュも壊れない。盤面 1 件ならこちらが安い）。PoC 1 本 Red→Green。**却下率の実低下は次のプレイで計測が残る**。
- ✅ **パッケージ一覧に「フォルダを開く」(2026-07-28, ユーザー要望「そのほうが改変しやすい」)**: ローカルタブの各行、削除の左に「フォルダ」。中身を直接いじって遊ぶ（改変・自作の下敷き）ための導線。`open_package_folder` は `list_packages` と**同じ `resolve_pkg_dir`** を通すので一覧の行と開く場所が必ず一致（相対パス登録でも実際に読んでいるフォルダが開く）、実在するフォルダのときだけ開く。`tauri-plugin-shell` を足さず既存 `os_open` で完結（custom command ゆえ追加権限ゼロ）。**留意**: 書庫取得物を手で書き換えると spec 17 の `tree_hash` が変わり更新時に「編集されています」警告（改変が上書きで消えるのを防ぐ守り）。改変を保つならフォルダごとコピーし、`.kataribe_source.json` を消せば手動配置扱いで更新機構の対象外になる。
- ✅ **AIモデル設定の焼き込み既定を撤去 (2026-08-20, ユーザーFB)**: 未設定時に設定画面へ `gpt-4o-mini` / `api.openai.com` が出ており、受領者が「このモデルが (無料で) 使える」と誤解して苦情になった — 一般の受領者は AI を無料と思っていることが多い。**二層とも撤去**: ①`get_llm_config` の UI 初期値は空欄 (プレースホルダの灰色例示は残る = 値と例示の区別) ②`LlmConfig::from_env` の黙示既定も撤去し、`LLM_BASE_URL`/`LLM_MODEL` 未設定は **Config エラー「設定 → AIモデル で〜」** (UI だけ空にすると空で保存→from_env が黙って gpt-4o-mini を選ぶ幽霊が残るため。黙って特定モデルを選ぶ利益は無く、誤解と意図しない課金の芽だけ)。CLI (.env 運用) は .env.example が両キーを明記済みなので実影響なし。
- ✅ **AIモデル選択リストの横はみ出し (2026-07-24, ユーザー報告)**: 設定→AIモデルの登録モデル `<select>` が右ペインを押し広げ横スクロールバーが出ていた。真因は **flex アイテムの既定 `min-width: auto` ＝中身より細くなれない**、かつ **select の「中身」は最長 option で決まる**こと（`Claude-sonnet-5（claude-sonnet-5）` のような長い登録名で発火）。`min-w-0`（縮められる）+ `truncate`（長名は省略記号）を付与、隣の表示名 input も同じ罠なので `min-w-0`。**他タブの select は無事**＝`block w-full` はブロック要素で、この縮まない挙動は flex/grid アイテム限定。**input と select で挙動が違う**のも要点で、`<input>` の幅は中身でなく `size` 属性（既定 20 文字相当）で決まるため長い値を打っても広がらない → PackageDialog の `flex-1` な input 群は同じ書き方でも症状が出ない（確認済み・無改修）。frontend のみ。
- ✅ **パッケージ更新 / spec 17 (2026-07-19 起草→rev2 査読 (A5+B5+C 全反映)→Phase 0+A+B+C 実装→2026-07-20 Phase D e2e ユーザー確認 = Done)**: 書庫取得物の**更新検知と上書き取得**（VSCode 拡張モデル）。検知の一次ソースは **content hash**（ユーザー決定・作者の semver は表示専用 = 申告の上げ忘れという穴を作らない）。接地済みの前提: サーバは sha256 を DB 保存済みだが API 未露出（outcast 残作業 = SELECT+1 列）/ manifest 不 parse 原則ゆえサーバに版番号は無い（事前表示は `file_updated_at`・版差分は更新後トースト）/ セーブはパス FNV キーゆえ同一フォルダ上書きで無傷。**機構**: ①出所メタ `.kataribe_source.json`（`SourceMeta{site_url,id,version,content_hash,tree_hash,installed_at_unix}`、メタ無し=手動配置の聖域）②サーバ sha256 露出（一覧+詳細、配信は DB sha256 対応バイナリをそのまま返す契約）③`check_package_updates`（**site_url==現在設定 siteUrl の時のみ照会=SSRF 遮断**、失敗はバッジ非破壊の沈黙、タブを開くたび自動）④`update_site_package`（プレイ中ガード/tree_hash 再計算で編集検知警告/`.update_tmp` 直下へトップ剥がし展開=二重構造排除/旧→.bak→スワップ→復旧=全か無か・パス不変/メタ更新は成功後のみ/排他）。**✅Phase 0+A (2026-07-19)**: data_contract `package_updates` 凍結 + app `update.rs`（sha256_file/tree_hash 凍結式/cleanup_leftovers 3 分岐=tmp 削除・bak 単独復旧・両在 bak 破棄）+ install の expected sha256 一致検証（不一致=DL 破損で中止）+ メタ書き込み + 混入メタの展開 skip + `RemotePackage.sha256: Option`（None 耐性）+ frontend 受け渡し。PoC 5 本 Red→Green、app backend 19 green。**✅Phase B (outcast 側・2026-07-19 デプロイ)**: 実サーバの一覧・詳細とも `sha256`+`file_updated_at` を露出済み（curl で接地確認）。**✅Phase C (結線・2026-07-19)**: command 3 つ — `check_package_updates`（メタ有り∧`site_url`==現在設定 siteUrl の時だけ並列照会。**失敗は全て沈黙**＝オフライン/404/5xx はバッジを消さずトーストも出さない）/ `package_is_locally_edited`（tree_hash 再計算）/ `update_site_package`（プレイ中ガードは session の `package_root` 照合＝frontend の disable と二層／編集検知は `force` 無しなら backend が拒否／DL+sha256 検証は新規取得と同一経路／`site::extract_package_zip_to` で `.update_tmp_{id}` **自身**を package root にして展開＝二重構造の構造的排除／`update::swap_in_place` で 旧→.bak→据え替え→.bak 削除、**失敗時は .bak を戻す**＝旧フォルダが必ず生き残る・**パスとフォルダ名は不変**＝セーブの FNV キーと packagePaths が安定／**メタ更新はスワップ成功後のみ**＝失敗した hash を書いて「更新あり」を永久に消さない／`UpdateGuard`(AtomicBool RAII) で排他し update 中は check もスキップ）。frontend: ローカルタブに ember の「更新あり」チップ（hover=サイト差し替え日時 ↔ 手元の取得日時+版）+「更新」ボタン（プレイ中/更新中は disable）、開くたび自動チェック、完了トースト「v{旧}→v{新}」。`cleanup_leftovers` に中断 DL の `.zip.part` 掃除を追加。**二重取得の封鎖 (同日・実プレイで発覚)**: サイトタブの取得は `unique_dir` で毎回 `名前_2` に増えていた（spec 05 の衝突回避の名残＝spec 17 が潰しに来た問題の片割れ）。Phase A で出所メタが入り判定材料が揃ったので、`list_packages` が `PackageEntry.source_site`/`source_id` を返し、**現在の siteUrl かつ同じ書庫 id が手元に在ればサイトタブのボタンを「取得済み」で disable**（更新はローカルタブの役割＝役割分担の確定。手動配置・別サイト由来は対象外で従来どおり取得可）。**編集検知の件数表示は撤回**（ユーザー決定＝件数には per-file hash が要り Phase 0 で凍結した「tree_hash 一つ」を崩すため。spec 17 決定 2 を改訂）。PoC 4 本追加（スワップ原子性の失敗注入 / 展開先固定 / siteUrl 不一致メタは照会に出ない / `.zip.part` 掃除）、app backend 22 green・clippy clean・frontend build green。**✅Phase D (2026-07-20 ユーザー実機確認)**: 実書庫で 差し替え→バッジ点灯→上書き更新→セーブ生存 の通しを確認。Windows rename 失敗は未観測 → **リトライ追加は見送り**（.bak 復旧+起動時掃除で十分、実運用で報告されたら再検討）。設計は `specs/17_package_updates.md`、契約は data_contract `package_updates` 節。
- ✅ **既成事実 / spec 20 (2026-07-21 起草→rev2 参照スコア制 (ユーザー発案)→rev3 査読反映 (重大4+中軽微8)→churn 許可の逆提案をユーザー承認→同日 Phase 0〜C 実装→Phase E 権限+「メモ」→「約束事」→「既成事実」/`facts` 全面改名→Phase D 実測 = 書き側撤去・読み側 Green)**: プレイヤーと GM が共有する確定事項のリスト = **MemoriaAeterna の最小プロトタイプ** (LLM が書きユーザーがキュレーションする可視の記憶)。既存の記憶 4 層 (state/recent_narration/chronicle/synopsis) に無い「常に見える・選別されない・固定表示」のピン留め層 + **ユーザーが GM の記憶を直接修正できる初のレバー**。**正本の外** (gm_core の変更は `StateDelta.facts: Vec<String>` 1 フィールドのみ・非検証・schemars 露出=description で 60 字を LLM に事前伝達)。**参照スコア制** (Memoria weight/feedback の移植): GM 新規=1 / ユーザー追加=4 / 編集=+3 (origin→user) / dedup ヒット=既存行 +1 (**skip でなく強化に昇格**・origin 不問=user 既成事実と同文の GM 提案は user 既成事実を強化)。score は harness 機構の帳簿 (GM もユーザーも直接指定不可)。満杯 20 件の規則は 2 つに分離: (a) 犠牲者選定=最低スコア帯の最古 / (b) GM 新規の入場判定=現最低以上なら最古退場で入場 (**同点は新規が勝つ=churn 許可** — 既存保護だと score 1×20 件が化石化する)。ユーザー操作 (add/edit) は入場判定免除で常に成立+押し出し行をトースト。60 字 (Unicode スカラー) 機械カット・**NFKC dedup**・編集の同文衝突は削除して既存へ +3 統合。**注入は user 可変側**「# 既成事実」節 (state_brief 直後=キャッシュ不変) + 従属規律の抑止/保護対 (#47 信頼チャネル防衛: 「state と矛盾したら state が正しい・付与禁止」+「呼称/約束/意図の一貫性には従う」)、GM_SYSTEM に読み書き規律 (60 字 1 行・毎ターン書くな・繰り返しは強化・既成事実由来の付与禁止=語りの確定は ops で弾けない唯一の防衛線)。並びは **UI/注入ともスコア降順 (同点 id 昇順)** = LLM の先頭重視と表示の見え方を一致・消えかけが下に集まる退場予告。`FactsDigest{accepted,reinforced}` → 📝 採用 / 📝⁺ 強化を会話ログ表示 (silent なスコア変化を作らない・開帳中は pushLog で保留=spec 18 漏洩防止に同乗)。`SessionSave.facts` (serde default=旧セーブ互換)・**GUI 編集は即時 autosave** (眺めるだけで消えない)・GM 分は受理ターン autosave 同乗・campaign 遷移で持ち越し。UI=右ペイン**第 5 縦タブ「既成事実」** (Ctrl+5・pencil アイコン・出所バッジ・スコア小表示・n/20)。commands: `facts_add/facts_edit/facts_delete` (ユーザー専権)。CLI は 📝/📝⁺ print のみ (編集は GUI 専用)。**✅Phase E `facts_policy` (2026-07-21 実プレイ FB「TRPG では約束事が強すぎる」＝当時の表示名)**: ユーザーの書き込み権限を盤面が縛る（注入ヘッダが GM に「一貫性には従え」と指示する＝**ユーザーの既成事実は GM への指示**で、engine 無傷のまま作者の設計した発見の順序を語りで迂回できる。誰が虚構を所有するかは盤面で違う＝三権分立の「シナリオが縛る」脚）。`Scenario.facts_policy` + package.yaml 宣言（注入で全モジュールを支配＝既成事実はセッション単位）。二値: **locked (既定)**=一切不可 + **タブごと非表示**（書き手がユーザーだけになった今、locked は実質「機能オフ」＝既成事実が一件も生まれず注入する節も出ない。ゲーム性＝作者が設計した発見の順序を守るための既定）/ **open**=追加・編集・削除（キャラ RP 向け）。当初は**加算と減算の非対称**（追加/編集は虚構を注入できるが削除は捏造不能）から中間値 **prune**（見せるが削除のみ）を置いたが、あれは **GM も書く**前提の設計で、GM 書き込み経路の撤去に伴い enum ごと撤去した（commit 3a18c1a。`facts_policy: prune` は今や manifest の parse ごと失敗する）。**既定 locked の根拠（2026-07-21 ユーザー決定・当初案 prune から強化）**: 危険は書き込みだけでない — **読めること自体が漏れる**（GM は scenario_brief 越しに challenge の前提・フラグの意味を知るので、プレイヤー未到達の仕掛けを既成事実に書きうる。パネルがそれを晒す）。宣言を持たない配布物＝書庫の既刊すべてを安全側に置く（spec 18 `pushable: false` と同じ保守判断）。**注入自体は policy を見ない**（`facts_note` はリストを受け取るだけで空なら節を出さない）＝ locked で節が出ないのは「書き手が居ないのでリストが空」という帰結であって policy 分岐ではない。二層防衛（UI で隠す + commands で拒否・制限中は理由を一行表示）。作者テストの開放は既存の開発者モードで足りる。PoC: `facts_policy_is_binary_locked_or_open`。PoC 8 本 Red→Green (schema 露出+旧デルタ互換 / 60字カット / dedup強化 / churn 入場判定 / 編集+3+同文統合 / user add 免除+犠牲者返却 / 注入文言・位置・空節なし / 旧セーブ互換)。**✅ 表示名の二段改名「メモ」→「約束事」→「既成事実」/ `facts` 全面改名 (2026-07-21 ユーザーFB ×2)**: **一段目**「メモ」は**暫定性**を符号化しているのに機構は**確定性**を扱っており、注入ヘッダが GM に「一貫性には従え」と指示する以上、プレイヤーが推理途中の仮説を書くと GM が尊重すべき設定として扱ってしまう（`hidden_*` の命名事故 2026-07-19 と同型＝**名前が符号化する軸を間違えると必ず混乱する**）。**二段目「約束事」→「既成事実」**（同日、GM 書き込み経路の撤去後）: 「約束事」は**相互の取り決め＝交渉の余地**を符号化するが、撤去によりこの欄は**ユーザーの一方的宣言**になった（GM は読むだけ）。「既成事実」はその非対称＝動かせなさを符号化する。弱い/ローカルモデルにも「取り決め」より「確定事項」の方が曖昧さなく伝わる（北極星に整合）。**同じ軸の判断が三度出た**（暫定性→確定性／相互性→一方性）＝命名は機構の意味論が変わるたびに再点検する対象。**二面を同時に直すのが要点** — 表示名（タブ/パネル/注入見出し＝「既成事実」）だけでなく **LLM が見る schema フィールド名**（`StateDelta.memo`→**`facts`**、description も「以後の語りで守るべき確定事項」へ）を変えないと GM の書きっぷりが変わらない（`memo` という語自体が走り書きを誘発。`facts` は弱い/ローカルモデルにも曖昧さなく伝わる＝北極星に整合）。内部も一貫改名（`memo.rs`→`facts.rs` / `MemoEntry`→`FactEntry` / `MemoPolicy`→`FactsPolicy` / `MemoPanel.vue`→`FactsPanel.vue`。`memoria`＝伏線の semantic recall は無関係の別概念ゆえ語境界つき置換で保護）。**タイミングが効いた**: spec 20 は v0.5.0 の後のコミットで未配布ゆえ、セーブキー・package キー・commands を互換の重荷ゼロで改名できた（リリース後なら移行負債）。**🟡Phase D 実測 1 周目 (2026-07-21): 「GM がほとんど書かない」→ 記述義務を再設計**。真因は LLM の理解不足ではなく**指示が構造的に実行不可能**だった — 同じ GM_SYSTEM で確実に書かれる summary (毎ターン・無条件 + 「書かなければ忘れられる」+ 例) と、書かれない旧 acts (「**忘れそうなら**」+ 帰結なし + 例なし + 「毎ターン書くな」の抑止) の差は文の形にあった。**「忘れそうなら書け」は LLM に判定できない** — ステートレスな LLM に忘却の体験は無く、文脈にある情報は常に「今まさに知っている」(忘却は次ターンの messages 再構築時に外部で起きる)。自分の忘却を予測させる条件は評価不能で実質「書かない」に倒れる＝**#32「権利≠義務」の再演**。加えて分類漏れ (「目標・志望」が無く、ユーザーの実例「主人公は医大受験を志している」が該当しなかった)。**修正 (prompt 層のみ)**: ①契機を観測可能な出来事へ (「新しく判明・確定したとき」) ②帰結の明示 (「書かなければ次のターンには失われる」= summary の効いている形の写し) ③分類の具体化+例 ④**コールドスタート対策** (空でも節を出す — 従来はトークン節約で空なら省いており、**一件も無い状態＝最も書き始めてほしい瞬間に可変側からチャネルが消えていた**)。副産物: 改名 (memo_policy→acts_policy) を告知せず進めたため配布側 package.yaml が旧キーのままで serde に黙って無視され既定 locked=タブ非表示に (**キー改名は serde の未知キー無視と組むと静かに死ぬ**。→ ✅2026-07-21 に `harness::manifest_lints` で package.yaml へ lint を適用し回収済み＝上の「未知フィールド lint」節)。**その後**: 書き側は三度目の書き直しでも 0/20 で撤去 (下の収縮節)、**読み側は実測 Green** (同節)。残る未測定は弱い/ローカルモデルでの読み側の効きだけ。設計は `specs/20_shared_facts.md`、契約は data_contract `shared_facts` 節。
- ✅ **確定行動 / spec 21 (2026-07-21 ユーザーが真因を特定→同日起草+実装 = Done)**: `ChallengeDef.resolution: none` — **ダイスを振らない challenge**。**動機は機構の穴**（Gemini が `sides:1/dc:1` の偽ダイスを書く理由）: **LLM が authored 効果を起動する経路は `attempt_challenge` しかない**（トリガーは engine が発火させるもので LLM から触れない）。「`set_flag` → トリガーが効果を出して**書き戻す**」定石は、その書き戻しで起点フラグが `authored_only_flags` に落ち、#50 バックストップが**真偽どちらの向きも**却下する → **LLM は二度と起動できない**（語彙提示からも消えるので静かに死ぬ）。「セットして戻す」は engine 駆動ループ（カウンタ→効果→リセット）用の定石で、**起点が LLM の時だけリセットが起点の権利を食い潰す**。装備・使用・切替のような**繰り返せる確定行動**はこれで書く。従来の回避策 `sides:1/dc:1` は偽のダイス（🎯 行・spec 18 伏せカードの誤発火）かつ **percentile 盤面では validate 拒否**ゆえ様式依存で壊れていた。**仕様**: 判定せず `on_success` を必ず適用・**RNG 非消費**（確定行動の有無でダイス列が変わらない=決定論）・`CheckOutcome` を積まない（判定していないものを判定に見せない）・結末文は**発火ビート経路**で提示/還流（authored narration を捨てない）・判定用フィールド（sides/dc/count/times/stat/expr/modifiers/tiers/degree スロット/on_failure/pushable）は `CertainActionShape` で load 時に名指し拒否・`requires` は使える（本命＝「条件を満たすと選べる行動」を宣言でき【前提】が prompt に出る）・`scenario_brief` は「判定なし・確定」表示。**spec 09 逐次射影に完全に乗る**（「装備する + 移動する」を 1 ターンで束ねられる＝ダイス challenge が必ずターンを割るのとの対比）。**同梱 lint `UnknownLocationInGate`**: Gate の `location_is` が未宣言の場所を指すと永久に false（requires なら一度も選べない・出口なら通れない）なのに従来は無警告だった → 非 fatal 警告で名指し（実例＝LLM 生成の `at: inventory`＝所持品を場所と誤認。challenge requires/modifiers・trigger when・flag_rules・出口 gate を入れ子の葉まで走査）。PoC 5 本 Red→Green（確定適用+RNG 非消費+ビート載せ / **専権フラグを LLM が起動**（直接 set_flag は却下のまま）/ 形の検査 / spec 09 束ね受理 / lint 偽陽性なし）。ドッグフード: `dice_trial` の `hold_torch`（松明を掲げる→トリガーが専権 `torch_bonus` を書き戻す→碑文判定の目標値 +15＝「準備が勝率を変える」の実演）。設計は `specs/21_certain_actions.md`、契約は data_contract `certain_actions` 節。
- ✅ **spec 20 の収縮 — GM の書き込み経路を撤去 (2026-07-21, 実測 3 周の結論)**: 既成事実の記述義務を三度書き直しても **0/45・0/20 の絶対ゼロ**（failures.md #65/#66）。①「忘れそうなら」＝ステートレスな LLM に忘却の体験は無く**判定不能** ②「新しく判明・確定した設定」＝GM はこれを「シナリオ側が明かした情報」と読み「flag/item で state に載るから対象外」と**正しく推論して**書かない ③「あなたが即興で足した事柄」＋提出直前の Yes/No 手続き＝**認識は完全に届いた**（開発者モードで問うと「東大理三」「妹の雫と 3 万円」を自分で列挙し、書くべきだったと認める）のに提出前の確認が毎ターン脱落。**GM 自身の分析が真因を言い当てた**:「条件付き・自己申告型の作業は、語りの生成に注意を奪われると最初に脱落する」— `summary` が守られるのは**無条件**だから。**語り手に記録係を兼ねさせるのが構造的に無理**であって文言の問題ではなかった。**撤去**: `StateDelta.facts` / `apply_gm_facts` / `FactsDigest` / 📝・📝⁺ ログ / `TurnView.new_facts`・`reinforced_facts` / GM_SYSTEM の記述義務と提出直前チェック / 空節のコールドスタート促し（守れない規律を積むと他の規律の重みまで下がる）。**残す**: ユーザーの add/edit/delete・60 字カット・NFKC dedup・スコア（編集 +3＝手を触れた行が守られる）・満杯退場とトースト・`facts_policy`・注入節の従属規律・UI タブ・セーブ。既成事実は**「ユーザーが宣言し、GM が守る設定」**になった（`prune` は書き手がユーザーだけになり実質無意味＝作者の選択は locked/open の二択）。**✅読み側は実測 Green（2026-07-21 ユーザー実プレイ・claude-opus-4-8）**＝撤去後に残った半分に価値があることの確認。「〇〇したら怒る」と宣言 → 実際にその行動の後に GM が怒り、しかも**長く根に持った**。単なる呼称の一貫性ではなく**条件つきの未来規則**が保持され条件成立時に発火した＝注入ヘッダの保護対象（呼称・設定・関係・約束の一貫性）より広く効いている。**接地の限界**: 強モデル n=1 で、弱い/ローカルモデルは未測定（配布の北極星ではそちらが本番）。**「根に持つ」は両刃**＝仕様どおりだが既成事実には減衰経路が無い（退場は満杯 20 件時のみ）ので、和解しても怒り続けうる。**減衰の代わりは手動削除**で運用は閉じる（`open` なら消せる／`locked` は消せないが書けもしない）。**負の側面（state と矛盾する行での捏造）は許容と判断（ユーザー決定）**＝この欄は**シナリオの薄いキャラ RP 用に限定**する意図であり、重厚なストーリーもので使えば壊れるのは仕方ない。この限定は既に機構になっている（`facts_policy` 既定 `locked` ＝作者が open と宣言した盤面でしか触れない）ので追加の守りは要らない。**一般化**: LLM に副次的な記録作業をさせるなら主作業と同じ出力に混ぜない — 混ぜるなら無条件にする（summary が実例）、条件付きにしたいなら**別の呼び出し・別の役割**へ出す。**この失敗が spec 22（あらすじ圧縮時の意味記憶抽出）の設計根拠になる**＝記録係の問いを記録係の瞬間に投げれば主作業と競合しない。
- ✅ **読み上げ / TTS (2026-07-22、v0.5.2。**←2026-08-31 に作者宣言 `use_tts` を撤去**)**: **撤去の改訂 (2026-08-31 ユーザー決定)**: `Scenario.use_tts` / package.yaml の `use_tts` (作者宣言ゲート) を撤去し、可否は**プレイヤー側の設定一本**へ — 設定サウンドタブのチェックボックス「読み上げ機能を使う」(`kataribe.tts.feature`、**既定 OFF** = 有効化した人だけに会話ペインの操作列が出る。imageGen `enabled` と完全同型)。挿絵で確立した「作者ゲートは作らない (鑑賞物で押すのもプレイヤー)」の線に読み上げも揃えた。ホバーのクイックトグル (`kataribe.tts.enabled`、未設定 ON) は機能スイッチの内側で効く (#68 の「宣言の下流では隠さない」は新形でも維持 — 「設定で有効化」が宣言に当たる)。**既存 content の `use_tts:` は parse を通り (serde が未知キー無視 = 配布済みが死なない) 未知キー lint が名指しで警告** — 同梱 friday_lemmon の宣言が bundled_packages テストを Red にする形で機械網が効いた (Red→Green)。DTO (`GameView`/`TurnView`) からも撤去、語彙フィクスチャ再生成。PoC: harness 1 本 (`removed_use_tts_parses_but_is_named_by_unknown_key_lints` — scenario 直書き/package.yaml とも lint が名指し)。**✅実機 Green (2026-08-31 ユーザー実測)**: チェックボックスの ON/OFF と、`use_tts` 宣言つき盤面での未知キー警告の表示を確認。**v0.5.19 で配布済み**。**outcast package_spec.md「読み上げ」節の追従が要る (ユーザー作業)**。以下は撤去前の記録 (再生機構の実測・罠は現行でも有効): TRPG/キャラ RP コミュニティで話題が多いため試作。**提示層だけ**で成立し正本・prompt・語りは一切変わらない（読むのは受理ターンの narration のみ＝判定結果やビートは読まない＝spec 18 の開帳前に出目を音声で漏らさない）。`Scenario.use_tts`(serde default false) + package.yaml 宣言（セッション単位で全モジュールを支配）＝engine 非使用・非検証の提示層宣言（`hidden_stats` と同類）で、**`use_tts` を true にしても prompt は 1 バイトも変わらない**（PoC で固定）。意味は「音声で読めるか」でなく**作者が音声を想定して書いたか** — 既定 false は宣言なき配布物（書庫の既刊すべて）を作者の意図どおり無音に置く（`facts_policy`/spec 18 `pushable` と同じ保守側の既定）。**文体は決めない**（TTS の ON/OFF で語りが変わると chronicle/synopsis の記録まで再生設定で食い違う＝音声前提の会話文体は `world` に書く）。実装: `@aituber-onair/voice` 0.19.0 固定（MIT・ランタイム依存ゼロ）、**値の import は動的だけ**（静的に一つでも値を取ると未使用セッションでも起動時ロード＝実測で dist の preload/静的 import 不在を確認）、長文は `splitSentence` で文割り（Chrome 系の長い発話が切れる癖）、世代カウンタで前の読み上げを必ず打ち切り（ターンが進んだのに前の語りが喋り続けるのは「矛盾しない GM」の音声版の破れ）。操作は会話ペイン右下に**ホバーで浮き出る**（ON/OFF＋スキップ、スキップは音を切るだけで物語は進めない）。設定サウンドタブにエンジン選択（ブラウザ内蔵/VOICEVOX/AivisSpeech/**OpenAI 互換**）+ サーバー URL + 話者一覧（localStorage キャッシュ、鍵=エンジン+実効 URL）+ 速度/高さ/音量。**共通 3 パラメータをエンジン別キーへ写す**（速度は全て倍率でそのまま、高さは webSpeech 0〜2 / ローカル 2 種 ±0.15 と意味が違うので −1〜+1 から変換）。CSP は `connect-src` を localhost に限定して開け（クラウド TTS は API キーが WebView に載るので開けない）`media-src blob:` を追加。**←この「限定して開け」た形が後に spec 23 の WebSocket を落とした（failures #73、2026-07-23）**: `connect-src` は明示した時点で `default-src` に落ちないので、後から足した外部接続は射程外になる。しかも **CSP は dev で効かない**（`devUrl` の間は Vite が画面を配るので、CSP は Tauri が配る本番ビルドにだけ乗る）＝**破れは配布物でしか出ない**。以後 `connect-src` は `wss: ws://localhost:* ws://127.0.0.1:*` も含む（ノックサーバーはユーザーが差し替えられるので単一ホストの allowlist にできない）。字面は unit テスト `csp_allows_websocket_to_a_user_configurable_knock_server` で固定した。**実測（AivisSpeech Engine 1.1.0-dev を curl）**: CORS は Origin 反映で `http://tauri.localhost` を許可＝WebView から直接叩ける／audio_query→synthesis で WAV 取得／`speedScale 0.5〜2.0`・`pitchScale ±0.15`・`volumeScale 0.0〜2.0` と写像が一致。**罠**: AivisSpeech は公式説明に「VOICEVOX と異なり pitchScale を 0.0 から変えると音質が劣化する」とあり UI で警告する。**❗作者本人の実測（WebSpeech）では没入感が「下がった」** — 声への違和感が勝った。良い音声モデルなら変わりうるが未検証。ゆえに **`use_tts` は既定 false・任意の上乗せ**という設計判断が実測で裏付けられた形（音声がないと成立しない盤面は作らない）。**残**: 開幕描写は読まない（`speak` は受理ターンのみ）／多声（キャラ別の声）は narration が単一文字列ゆえ未対応（案A=「」の正規表現、案B=StateDelta に話者つき台詞＝LLM 契約の変更でキャッシュ・弱モデル・#47 汚染リスクを背負う）。**読み上げ対象 (2026-07-22 追補)**: GM の即興 (`turn.narration`) と**エピローグ** — どちらも LLM が書いた本文 (spec 11 の `epilogue_prompt` は作者の生成指示であって本文ではない)。作者の書いた文 (場所の説明・ビート ✦・結末文・判定の結末) は読まない＝**声で流れるのは LLM の文だけ**（作者文はシナリオの進む方向を示す指針で、GM が次の語りをその方向へ書く＝読み上げると二重になる）。エピローグは**結末を語るので開帳待ちなら `pendingSpeech` に保留**し flush で解き放つ（伏せたままだと帰結を音声で漏らす。`pendingTail`/`pendingSe`/`pendingVisual` と同じ契機）。`speak(text, {queue:true})` は前を打ち切らず**続けて足す** — 同じターンの語りを途中で切らずにエピローグへ繋ぐため。投入は**自前の直列鎖 (`chain`)**: まとめてアダプタへ投入すると**ネットワーク型エンジン (VOICEVOX/AivisSpeech) で全文の合成が一斉に並列発射**され、後続がライブラリ既定の 30 秒タイムアウトで reject → 失敗は握り潰す設計ゆえ**その文が黙って飛ぶ**（実測: 読み上げが途中で切れる。AivisSpeech 側の文字数制限ではない — 320 字 1 リクエストが 4.4MB WAV で正常に返るのを curl で確認済み）。かといって 1 文ずつ await して積むと隙間に後発の queue が割り込んで順序が崩れる。鎖なら直列と順序保存を同時に満たす。一般化: **「まとめて投入」は自前が直列のつもりでも下流が並列に解釈することがあり、失敗を握り潰す経路と組むと過負荷が「エラー」でなく「静かな欠落」として現れる**（例外もログも出ず文だけ消えた）。**実機確認 (2026-07-22)**: AivisSpeech でエピローグ直前のセーブから通し、語り→エピローグを最後まで読み切ることを確認。あわせて WebSpeech の発話・エンジン切替・話者一覧・シードリセット後の出目変化も実機で確認済み。**❗評価はモデルで反転した (2026-07-22、作者実測 n=1)**: WebSpeech (Windows SAPI) では「イントネーションへの違和感で没入感はむしろ下がった」→ **AivisSpeech に替えると「朗読みたいで良い」へ反転**。仮説「良い音声モデルなら変わる」は肯定側に落ちた＝**この機能は畳まず育てる**判断材料。ただし作者自身が慣れ (habituation) の可能性を挙げており未分離 — **分離は安い**（エンジン選択で WebSpeech へ戻して聴き直す。慣れなら WebSpeech も許容に転じ、モデル差なら違和感が戻る）。**「朗読みたい」という評価は設計前提を揺らす**: 音声の価値をキャラ RP (台詞中心) に見込んで「音声前提なら `world` に会話中心の文体を書く」を作法にしたが、朗読として効くなら**地の文の厚い物語ものにこそ向く**ことになり、想定した受け手と作法が逆を向く。追試で固まるまで作法は据え置き。**未測定**: 弱い・ローカルモデル環境での挙動。**語彙の注意**: コードの `narration` は **LLM の文**を指すが、UI とユーザーの語彙では「ナレーション/システム文」は**作者の文**を指す（`StateDelta.narration` は LLM に見せる schema 名なので改名しない＝prompt とキャッシュの安定プレフィックスが動く）。**OpenAI 互換エンジン (2026-07-22、⚠実サーバー未検証)**: `POST /v1/audio/speech` を喋る任意のローカルサーバーに繋ぐ口。**Irodori 個別対応でなく汎用の口として足した** — 次に来るモデルにも同じ口で刺さり、エンジン固有実装を増やさない。ライブラリの `openaiCompatible` に写す（URL は**エンドポイント全体**・`model` は**必須**（空だと例外）・`speed` 0.25〜4.0 は速度スライダ 0.5〜2.0 を内包）。**高さ/音量は API に無い**ので UI でスライダを無効化（実装の手抜きでなく仕様なので誤解を作らない）。**話者一覧はライブラリ非対応**（voice の語彙がサーバー実装ごとに違い共通の列挙 API が無い）→ 自由入力に切替。既定 URL は Irodori-TTS-Server の `http://localhost:8088/v1/audio/speech`（CSP の localhost 許可の射程内）。API キー欄は作っていない（CSP を localhost に限っているのでクラウド用途は元々通らず、キーを WebView に置く問題も避ける）。**サーバーを立てずに口だけ作った状態** — CORS を返すか・実際に鳴るかは実サーバーで要確認。参考: Irodori-TTS はコード MIT だが**モデル重みは別ライセンス**で、生成音声にソニーの SilentCipher 電子透かしが入る（配布で薦める前に要確認）。作者向け仕様は outcast `package_spec.md` の「読み上げ」節。**❗中断したら二度と喋らない (2026-07-23、ユーザー報告「AivisSpeech でスキップすると次のターンにボイスが出ない気がする」→ 実測は 1 ターンでなく**そのセッション恒久**)**: ライブラリの `BrowserAudioPlayer.stop()` は `audioElement.pause()` するだけで `play()` の Promise を settle しない（pause は `ended` も `error` も発火しない）→ `VoiceEngineAdapter.processQueue` が `await playPreparedSpeech` で永久に止まり **`isProcessingQueue` が true のまま残る** → 以後の `speakText` は `if (this.isProcessingQueue) return;` で捨てられる。`stop()` 自身は待機中 request を reject するので**呼び出し側には正常な中断に見え**、握り潰し設計（#69）ゆえ症状は例外でなく**無音**。**スキップ限定でない** — `speak()` は次ターン到来時にも同じ `stop()` を呼ぶので、語りの最中にターンが確定すれば同じ罠（原因はボタンでなく「再生中に止めたか」）。**webSpeech では出ない**（self-playing ＝ `speechSynthesis.cancel()` が発話を settle させループが巻き戻る）＝**ネットワーク型 3 種だけ**が踏む（評価を WebSpeech→AivisSpeech へ移した直後に露見したのは経路が変わったから）。修正: **再生中に止めたときだけ**アダプタを捨てて次の speak で組み直す（`isPlaying()` は `stop()` を呼ぶ**前**に見る＝畳まれる前の状態で判定。合成待ち/停止済みならキューは正常に巻き戻るので使い回す）＋ `speak()` の割り込みを `stop()` へ寄せる（アダプタを取る**前**に呼ぶ＝後だと今から使うアダプタを捨てる）＋日本語音声の選択をキャッシュ（組み直しのたびに `voiceschanged` を最大 1 秒待たない）。PoC: 実ライブラリを使った使い捨て再現（再生を「立ててから永久に pending」に差し替え、`isProcessingQueue=true` の残留と作り直しで鳴ることを実測、Red→Green）。一般化: **「止める」API が、呼び出し側への約束（待機中の reject）と内部の後始末（再生ループの巻き戻し）の両方を果たすとは限らない — 前者だけ守られると中断は成功して見え、壊れた状態は次の要求まで観測されない。外部ライブラリの `stop()` を打ち切りの正常系として扱うときは「その後に同じインスタンスがまだ使えるか」を疑い、使い回しをやめて作り直すのが内部状態を読まずに済む最も安い回復手段**。`failures.md #72`。
- ✅ **シードリセット (2026-07-22, ユーザー要望)**: 決定論の裏返し — セーブ地点からやり直しても出目が同じで別の進み方を見られない、への逃げ道。`GameState::reseed(seed)` が rng を `{seed, cursor: 0}` へ差し替える。**save/load と同じ層のプレイヤー meta 操作**で、LLM の op にも authored トリガーにも経路が無い（語りの内側から運命を振り直すことは構造的に不可能）。**cursor も 0 に戻す**のが要点 — 位置だけずらすと「前回の続きの出目」になり得て『別の筋』にならない。**押した時点では保存しない**（次に保存される時＝受理ターンの autosave / 手動スロットに新 seed が書かれる。押した直後にやめればセーブは元の seed のまま＝既存セーブを塗り替えない線引き）。開始時シードのまま・ロードでも元シードが残る従来挙動は不変。種は OS 時刻ナノ秒 ^ 現在 seed の回転（gm_core は純粋ゆえ乱数源を持たない＝上位の責務、同一ナノ秒の二度押しでも別種）。**波及**: 投票の同数抽選（VOTE_RNG）は評価時に `rng.seed` から派生するのでリセットが効く／配役（ROLE_RNG）は `initial_state` 時のみゆえ不変（途中で役職が入れ替わらないのが正しい）。UI は右ペイン「状態」タブ右下の小ボタン＋確認ダイアログ（**誤爆すると「この先の運命」が黙って変わるのに画面は何も動かない**＝不可視で不可逆な操作なので確認を挟む）＋確定時に会話ログへ system 行（いつ筋が分岐したか後から辿れる）。PoC: `reseed_replaces_the_dice_stream_and_rewinds_the_cursor`（Red→Green。別 seed で列が変わる／**同 seed へ戻せば完全再現**＝リセットは決定論そのものを壊さない・監査可能性は不変／他の正本に触れない）。実機で出目の変化を確認済み。
- ✅ **UI 設定ミラー — localStorage → app_data/settings.json (2026-08-31、改名の設定消失事故の根治策)**: WebView の localStorage は bundle identifier 別プロファイルに住み、identifier 変更 (2026-08-28 改名で実機発生)・プロファイル破損で設定 (登録モデル・packagePaths・画像生成設定・テーマ等 `kataribe.*` 約 30 キー) が丸ごと消える。**ミラー方式** (ユーザー決定 — 完全移行は提示層 ~100 箇所改修 = 「ユーザーが検出器になる」層の回帰リスク大で見送り): **localStorage が正本のまま** (読み取り経路 1 バイト不変)、`kataribe.*` 全キーのスナップショットを `app_data/settings.json` へ write-through (debounce 800ms・**全量 dump** = removeItem も自然収束・beforeunload で flush・**起動時にも 1 回** = 既存ユーザーの file を最初の設定変更を待たず作る)。**書き込みの捕捉は `Storage.prototype` フック 1 箇所** — 書き込み ~50 箇所を列挙しない (#84 の列挙 1 本化と同じ向き)。**`localStorage` インスタンスへの代入は Storage の named setter に化けて効かない** (「setItem」という名前の item が書かれるだけ) ので prototype に張り `this === localStorage` で絞る。復元 = 起動時 (mount 前)「localStorage に kataribe.* ゼロ (新プロファイル) ∧ file 非空」のときだけ全量書き戻し → **reload 1 回** (import 時の同期読み群に値を行き渡らせる最も安い形。通常起動では reload しない・sessionStorage ガードで storage が書けない環境の無限 reload を封鎖)。backend = `settings_store.rs` (検証 = object・`kataribe.` 接頭辞・文字列値のみ = localStorage の写し以外は壊れたデータで復元に乗せない〔壊れは eprintln + None〕/ tmp→rename 原子書き込み = save_session 同流儀 / 上限 4MB) + command 2 つ `load_ui_settings`/`save_ui_settings`。**「移行後に localStorage を削除するボタン」はユーザー提案を対話で撤回** — ミラーでは localStorage が作業コピーなので削除は無意味 (次回起動で復元される) か有害 (file 書き込みが失敗していた場合に本当に消える)。削除ボタンが意味を持つのは完全移行の意味論。API キー (aiModelProfiles) は従来から localStorage 平文 (BYO-key ローカル app) で file も app_data/.env と同じ信頼水準。PoC: backend 2 本 (検証則 / 原子性+読み戻し+壊れ拒否) + frontend vitest 6 本 (採取の接頭辞限定 / 検証則の backend 一致 / 復元判定と一度きり性 / debounce 束ね / 全量 dump 収束+flush)。**✅実機 Green (2026-08-31 ユーザー実測)**: settings.json 生成 (Roaming/jp.lorekeel.app、16KB) + プロファイル退避 → 起動 → 復元 → reload で設定が戻る動線を通しで確認。rename.rs の LevelDB コピーはこの機構が育てば将来不要になるが撤去はしない (= 退路)。**v0.5.19 で配布済み**。
- ✅ **本文の文字サイズを UI と分離 (2026-08-31, ユーザーFB「UI とメッセージウィンドウの文字がまとめて大きくなるのを分けたい」)**: 会話ログに独立の文字サイズ設定 `kataribe.msgSize`（表示タブ・14〜28px の select・**既定 0 = UI に合わせる** = 従来挙動不変）。適用は会話ログ container の inline fontSize **1 箇所** — 語り系の段落はサイズクラスを持たず container を継承し、メタラベル（text-xs 等）は rem = UI 基準フォント（`kataribe.fontScale` = root font-size）側に残るので、本文だけが独立して動く。`messageFontFamily` getter は `messageAreaStyle`（フォント+サイズ）へ置き換え（使用 2 箇所 = ConversationLog container / 設定プレビュー、grep で残存ゼロ確認）。frontend のみ。**v0.5.19 で配布済み**。
- ✅ **救済が診断を握り潰していた / 開帳カードのインスタンス再利用 (2026-09-01、実プレイ 2 件)**: どちらも**正本は無実**で、壊れていたのは「失敗したときに何が言えるか」と提示層の部品同一性。 **(a) failures #93** — 実プレイで GM が `give_meat_to_momoka` / `eat_meat` という幻の op を発明し、画面には `data did not match any variant of untagged enum OneOrMany` だけが出てターンが落ちた。真因は三層で、①LLM の幻 op（正常な失敗）②`StateDelta.ops` の #64 救済 `one_or_many` が **untagged enum** で書かれており、serde が全バリアント失敗時に**内側のエラーを捨てる**ため ``unknown variant `give_meat_to_momoka`, expected one of `add_item`, …``（有効 op 全 19 個の列挙）が総称文言に潰れていた ③`turn.rs` の self-repair がその潰れた文言を再生成へ載せたうえ「JSON として壊れていて読めなかった」「**ops は要素が 1 つでも必ず配列**」と続け、**構文は正しく ops も既に配列**＝LLM が正しくやっていることを指示して真因に一言も触れず、同じ出力を繰り返して `max_attempts` を使い切った。処方は `one_or_many` を手書き Visitor へ（`visit_seq` は要素の失敗をそのまま伝播 / `visit_map` が #64 の単一オブジェクト救済）— **受理する形は untagged 版と同一**（配列・単一オブジェクトのみ、null・文字列・入れ子配列は従来どおり拒否＝probe で前後比較）で、**変わるのは診断だけ**。あわせて self-repair の文言を「理由を先頭に立てる」形へ（断定と「必ず配列」の注意書きは撤去＝受け側が救済するのでもう要らない）。依存追加ゼロ・schema 不変＝キャッシュの安定プレフィックス不動。**幻 op に限らず有効 op のフィールド欠落も同じ文言に潰れていた**＝`ops` の診断チャネルは #64 以降まるごと死んでいた。**一般化は #78 の再演＝緩さが安全なのは情報を捨てないときだけ。寛容化を入れるときは受理集合だけでなく『失敗したときに何が言えるか』を前後で比較する**（見ていなかったので今日まで気づかなかった）。 **(b) failures #94** — 1 ターンに判定が 2 件積まれると 2 個目の開帳カードが押せず、入力欄が `hasUnrevealedDice` で締まったままデッドロックした。`DiceReveal.vue` の `state` は `idle → rolling` へ一方向にしか進まないのに、2 件が**同じログ行**に積まれると `v-if` が真のままなので Vue が**同じインスタンスを使い回す** → props だけ差し替わり `state` は `rolling` のままでクリックが弾かれる。プッシュの振り直しで露見しなかったのは、あちらが一度 v-if 偽（unmount）を通るから＝**唯一 unmount を通らない経路が「同一行に 2 個以上」だった**。処方は呼び出し側の `:key`（rolls / checks / statrolls の 3 箇所）で、**key は値でなく位置（`revealed`）で切る** — props の watch では同じ出目が 2 回出たときに区別できない。#92 と同じ **Vue のインスタンス再利用**の一族で 2 件目。**接地の限界**: frontend の vitest は DOM を持たない（`@vue/test-utils` / `happy-dom` 未導入）ので**この修正に自動テストは無い** — マウント用ハーネスの導入は起票の価値がある（未決）。
- ✅ **challenge の回数上限 `max_per_turn` (2026-09-01、ユーザー観察「muse-spark は 1 ターンに 2 つのチャレンジを束ねる」から)**: 束ねること自体は**合法で望ましい** (spec 09 の「ダイスはターンを割る」は**帰結に依存する後続手**を割る規律で、独立した 2 判定は射程外。1 ターン = フルプロンプト 1 往復なので束ねる方がトークン経済に合う。Claude 系が 1 つずつに誘導するのは 2026-06-27 のダイス規律を強く読んでいるから＝**どちらのモデルも接地どおり**)。**だが隣に穴があった** — `attempt_challenge` は **LLM が選べる唯一の「authored 効果を持つ op」**で、同じ challenge を 1 デルタに N 回並べると帰結効果が N 倍になる (probe 実測: `on_success` に `adjust_stat hp +5` を持つ challenge 5 回で **hp 10 → 25 が受理**)。LLM は効果**量**を書けない (#23/#24 の閉世界) のに**回数で実質的に量を決められる**抜けで、却下されないので沈黙する。**`requires` では塞げない**ことも実測: ダイス op の帰結は非射影 (spec 09) ゆえ `requires: not(flag_is rested)` は同一デルタ 3 回を全部通し次ターンだけ却下した ＝**作者の持つ「一度きり」の粒度はターンで、その一段内側にちょうどバッチ 1 回ぶんの穴が開いていた**。**処方** `ChallengeDef.max_per_turn: Option<u32>`（既定 `None` = 無制限＝既存 content の挙動不変）。**数えるのは (challenge, 主体) の組** — 多人数卓 (spec 23) で仲間 2 人が同じ challenge に 1 回ずつ挑むのは正当なので challenge 単位で数えると偽の却下が生まれる (主体の解決は判定と同じ)。超過は `ChallengeExhausted` で却下し、文面は #42 の規律どおり**「次のターンなら挑める」= 何をすれば通るかを語る**。`scenario_brief` は【1 ターンに N 回まで】を**先に**出す (越えてから叱ると op クラスへの信頼を毀損し回避学習が起きる)。`0` は lint `ChallengeMaxPerTurnZero` (一度も挑めない = 提示には出るのに必ず却下される死んだ挑戦。閉世界は壊れないので load 拒否でなく警告)。**射程は attempt_challenge だけ** — `check`/`check_under` は帰結を持たない (成否と degree の surface のみ) ので繰り返しても無害、`roll_stat` は authored 専権、`attempt_contest` は `ContestInProgress` で既に 1 つに制限済み。**副産物**: 新フィールドで `editorVocabulary.fixture.json` が古くなり `vocabulary_fixture_is_fresh` が落ちた＝2026-08-29 に張った機械の網が想定どおり働いた (`UPDATE_VOCAB_FIXTURE=1` で再生成)。PoC 3 本 (回数上限＋別主体は通る＋既定は無制限＋上限内 apply / 0 は lint で load は通る / brief に先出し・未指定は文言不変)。契約は data_contract `ChallengeDef.max_per_turn`。**outcast `package_spec.md` の challenge 節に追従が要る**。
- ✅ **エディタの検索・置換パネルを表示言語に追従 (2026-09-01, ユーザーFB「日本語表示でも英語になっている」)**: `@codemirror/search` のパネルは自前の英語ラベルを `state.phrase("Find")` のように引くので、**`EditorState.phrases` ファセット**で差し替える (Compartment = テーマと同じく言語切替に即追従・再読込不要)。表は `src/editorPhrases.ts` に置き `messages.ts` には入れない — **キーがライブラリ側の英語文字列そのもの**で、アプリの文言ではなく第三者コンポーネントの引き当て表だから (置くとキーと値が 2 ファイルに割れる)。**英語は空表を返す**＝上書きしない (キー自身が英語なので写すとライブラリの原文更新から取り残される)。**網羅は手書きの一覧で確かめない** — テストが `@codemirror/search` の dist を `?raw` で読み込んで `phrase()` に渡される文字列を正規表現で抽出し、過不足を両方向で照合する (ライブラリを上げて文言が増えたら落ちる＝app backend の `vocabulary_fixture_is_fresh` と同じ流儀。`node:fs` を使わないのは vue-tsc がテストも型検査するため＝`@types/node` を足さずに済む)。**「半分動く形」を潰した** — `SearchPanel` はラベルをコンストラクタで 1 度だけ組み `update()` は検索語しか同期しないので、ファセットを替えただけでは**開いているパネルが英語のまま残る**。locale 変更時に開いていれば閉じて開き直す (検索語とオプションは state 側にあるので失われない)。#92 の「半分動く形を疑う」の適用。PoC 4 本 (frontend vitest: 網羅 / 綴り違いの検出 / `$` 差し込み位置の保存 / 英語は空)。
- ✅ **自壊する束の名指し `broken_by` (2026-09-03、ユーザー報告「所持品は 10 個までしか正本が認識しないのでは」起点 = failures #95)**: 焼き魚を持っているのに `eat_fish` が 4 回却下された。**上限説は probe で棄却** (15 個受理・実セーブでも合計 11 個が受理済み)。真因は spec 09 の逐次射影が作る**時点のずれ** — GM が「食べる」の意味論で `remove_item 焼き魚` を `attempt_challenge` の前に並べ (challenge の帰結を自分で二重に書く)、射影の中で魚が消えて `requires` が偽になる。却下理由は「必要: 焼き魚を所持」としか言わず、画面でも state_brief でも持っているので LLM は同じ形を繰り返した = **#50 の「画面は true なのに『true が必要』」の item 版**で、gate 未達 5 種にはあの backstop が無かった。処方は engine + reason 層のみ: 5 変種に `broken_by: Option<StateOp>` (serde default)、`validate_ops` が gate を**ターン開始時の state** で評価し直し、真なら射影に載せた op 列を再生して最初に偽へ落とした op を載せる (共通効果が壊した責めは attempt_challenge 自身)。文面は「この条件はターン開始時には満たされていた。先行 op〔op: remove_item, item: 焼き魚〕がそれを壊している。その op を消すか順序を入れ替えれば通る」= #42 の規律。prompt/schema 不変 = キャッシュ無風。**一般化: 逐次射影は裁定の時点を op ごとにずらすので、gate 未達の理由は「どの時点の評価か」と「そこまで誰が状態を動かしたか」を必ず載せる**。PoC 1 本 Red→Green (ChallengeLocked と MoveGateUnmet の両方で名指し / 最初から未達は名指し無し / 順序を入れ替えれば受理)。接地の限界: ユーザーの画面の却下文言は未確認 (autosave は却下ターンを記録しない)、「remove_item 先置き」は実セーブに対して却下される唯一の自然な形として同定した推論。
- ✅ **挿絵のスタイル写し落とし (2026-09-03、ユーザー報告「スタイル指定が『画像に送るプロンプト』に二重で出る」= failures #96)**: 設定の `userPrefix` は 1 回分で無実、二重を作っていたのは**合成** — プロンプト書きは素材節の「前置きされるので重複して書くな」を確率的に守らず、タグ列のスタイルを画風タグとして出力の先頭へ写す。`compose_image_prompt` に `strip_leading_style_echo` — **写しは verbatim ではない** (ユーザー実データ: スタイルの誤字 `hyper-rea` を `hyper-real` に**直して**写す) ので、一致は**タグ単位** (`,` `、` `.` `。` `;` 改行で区切る) で正規化 (小文字・空白畳み) のうえ 5 文字以上で編集距離 `1 + 長さ/10` を許す (短いタグは完全一致のみ・語の途中では切らない・override は verbatim 不変)。**LLM の写しは正規化を伴う** — 写しを検出する機械は完全一致でなく「人が同じと読む」粒度で比べる。**規律 (見せる側) と合成 (通さない側) の二層** = 閉世界の防衛線 #50 と同じ形を提示層の文字列合成にも。PoC 2 本 Red→Green (2 本目はユーザー実データそのものを Red に)。接地の限界: 編集距離の閾値は実データ 1 件に合わせた値で、誤字 2 箇所以上のタグは剥がれない。
- 🟡 **winget-pkgs へ登録 (`Outcasts.Lorekeel`、2026-09-01 提出 = マージ待ち)**: [PR #427244](https://github.com/microsoft/winget-pkgs/pull/427244) を v0.5.19 で提出。**CI は 1 行も変えていない** — winget は版ごとに URL と SHA256 で固定する側で、**版を含むファイル名だからこそ自動化できる**。手順と罠は **Fuseforks の実績 (2026-08-17 提出 → 08-29 マージ = 12 日) をそのまま継承**した (`D:/Github/Fuseforks` の CLAUDE.md が一次資料)。**判断 3 つ**: ①**MSI のみ提出・NSIS の `-setup.exe` は出さない** (MSI は `ProductCode`/`UpgradeCode` を持つので更新・削除の検出が構造で決まる。NSIS は ARP の表示名と発行者の照合に頼るが Fuseforks の実測で exe の `CompanyName` が空 = 最も壊れやすい) ②**`InstallerLocale` を書かない** (Fuseforks が実機で踏んだ罠 — en-US を宣言すると日本語環境のインストール記録 ja-JP と突き合わされ `winget upgrade` が `UPDATE_NOT_APPLICABLE` で落ちる。宣言が無ければ winget-cli の preference 枝が空ロケールを素通しする。**live の Fuseforks 0.1.13 にも無いことを `wingetcreate show` で確認**してから同じ形にした = ユーザーの記憶と実データが一致) ③`Scope: machine` (MSI の `ALLUSERS=1` を実読して確定)。**識別子は実質永続** — 改名には move PR が要り既存利用者の `winget install` が壊れる。**`Outcasts.` 名前空間の根拠は Publisher 欄と既存の `Outcasts.Fuseforks` の先例**で、MSI の `Manufacturer` は `lorekeel` (Tauri が `jp.lorekeel.app` から導出。Fuseforks は `jp.outcasts.fuseforks` から `outcasts` だったので**そこは揃っていない** — モデレータの質問に備えて記録)。**版ごとの値**: `ProductCode` は版ごとに変わり `UpgradeCode` は不変 (0.5.19 = `{32111BC6-AB44-45B3-9189-D9838CD05895}` / `{6C3F3B38-1537-5822-9108-11532C4DFDC0}`) — **次の版は必ず新しい MSI から読み直す** (古い値を写すと更新検出が壊れる)。**SHA256 は DL 不要で取れる** (`gh release view --json assets` の `digest`)。今回は `ProductCode` のために MSI を落としたので、**手元の hash が digest と一致することも確認**した (二経路の突合)。**提出前にフォークを upstream と同期**する (Fuseforks の罠 = 古いと「同期できませんでした」で失敗。今回は SHA 一致を実測してから提出)。**次の版の手順**: `wingetcreate update Outcasts.Lorekeel --version <V> --urls <MSI URL>` → **`--submit` せず一度ローカル生成** → `InstallerLocale` の行が入っていたら削除 → `wingetcreate submit`。版上げは New-Package と桁が違う (Fuseforks 実測で 1 時間足らずでマージ・ただし**マージとカタログ配信は別**で索引再構築に 2 時間弱)。マニフェストの正本は winget-pkgs 側なので `manifests/` は `.gitignore` (Fuseforks と同じ流儀)。**未起票の副作用**: MSI と NSIS は**同じ端末に並んで入り identifier が同じなので `app_data` を共有する** — Fuseforks はこれで事故った (新しい欄を知らない旧版が保存ファイルを黙って削って書き戻す)。Lorekeel も `SessionSave` が `serde(default)` なので同じ形の risk がある。**winget が作る問題ではない** (Releases から両方入れれば今日も起きる) が、入手経路が 1 本増えるぶん遭遇確率は上がる。**`UpgradeCode` の導出を実測した (2026-09-01)**: `uuid_v5(NAMESPACE_DNS, "{product_name}.exe.app.x64")` — tauri-bundler のソース (`msi/mod.rs`) を読み、**両製品の実 MSI と照合して一致**させた (`Lorekeel` → `6c3f3b38-…` / `fuseforks` → `b2dde00e-…`。Fuseforks が小文字なのは `productName` が `fuseforks` だから)。**入力は `product_name` だけで bundle identifier は入っていない** = 非対称がある: **`productName` を変えると `UpgradeCode` が変わり、新版が旧版を差し替えられず二重に入る** (winget の更新検出が壊れる。**触ってはいけない**) / **bundle identifier を変えても MSI の更新は壊れない** (壊れるのは `app_data_dir` と WebView 保存領域 = データの置き場だけ)。`tauri.conf.json` の `bundle.windows.wix.upgradeCode` で固定もできる (今は未指定 = 導出のまま)。**この実測が 2026-09-01 の裁定を支えた** — 「identifier を `jp.outcasts.lorekeel` にすべきだった」(ユーザー) に対し、MSI 側は壊れないと確定したうえで**変更は却下** (ユーザー裁定): `jp.kataribe.app` → `jp.lorekeel.app` → `jp.outcasts.lorekeel` の**三重移行**になり、`rename.rs` の移行は「旧が在り新が空のとき」だけ発火する 1 ホップ設計なので、**中途半端に更新した人のデータが 3 箇所に散る経路が生まれる** (8/28 の 1 回目で実際に設定が消えている)。得るものは名前空間の見た目だけで、`jp.lorekeel.app` は第三者が見ても「lorekeel」としか読めない = 誤解を生む名前ではない (`jp.kataribe.app` が残るのが問題だったのとは質が違う — あれは**他人の名前**だった)。やるなら v1.0 のような区切りで移行を 1 回に束ねる。**提出中に新版が出ても PR は差し替えない (2026-09-01 裁定)** — 同日中に v0.5.20 を出したので 「PR を 0.5.20 へ差し替えるか」を提示したが**却下** (ユーザー): **通るかどうか未確定のものを追いかけても、通らなければ全部無駄**。そもそも差し替えても「レビュー中に新版が出る」問題自体は解けない (2 週間あれば次の版も出る) ので、根治は「マージされたら追う」運用の方。Fuseforks も 0.1.8 で載った後に 0.1.9〜0.1.11 が出ていて、**古い版は壊れずに止まる** (エラーが出ないので気づく契機が無い) = 版上げ PR が唯一の追従経路 (実測 1 時間足らずでマージ)。**残**: マージ待ち (New-Package の中央値 15.2 日) → マージ後に `wingetcreate update` で最新版へ追従 → `winget search lorekeel` で実機確認 → README 3 言語のインストール節へ winget を追記。
- 合計 **370/370 green**（gm_core 163 + harness 138 + 統合 2〔bundled_packages / data_contract_parses〕+ llm_client 67。live 3 本〔llm_client Perplexity / Meta / harness 挿絵プロンプト〕は ignored）、clippy clean。app backend **80 green**（ignored 7 本＝画像 live 5 + 書庫サーバ 2）。**frontend 53 green**（`cd app && npm test` = vitest。2026-08-29 spec 28 v2 で新設した三本目の数え場所）。〔**2026-09-03 実測で更新** — broken_by で gm_core +1、スタイル写し落としで harness +2 (367→370)。2026-09-01: #93 で llm_client +1 / 要約タイムアウト設定で app +1 / max_per_turn で gm_core +2・harness +1 / エディタ文言の和訳で frontend +4。**この行は実測を正とする**: 2026-08-21 に「41 green / live 4 本」を、2026-07-24 に「312」を、それぞれ数え落としで訂正した前科がある＝**テストを足した回に count 行へ触れない**という追加側の穴 (`mandate_removal_requires_ledger_grep` の補足そのもの)。数える手順は `cargo test --workspace` / `cd app/src-tauri && cargo test` / `cd app && npm test` の 3 本。〕
- ✅ **セーブ/ロード / spec 07 (2026-07-04 起草 → 同日 Phase A+B+C 完了 = Done)**: 動機=実害（Gemini 長セッション ~1h が provider エラーで打ち切られ全消失、`#34` と同根）。**`SessionSave`（YAML 1 file）** = 正本（`GameState` 丸ごと＝rng/votes/flag_turns/present_overrides 込み＝**出目まで再現**）+ `campaign_memory`（spec 02 の save 永続化積み残しを解決）+ **語りの継続性**（chronicle 全量/last_narration/pending_checks/pending_lore＝state だけでは再開時に「経緯を忘れる GM」に戻る #27 系の第二チャネル）。**骨格は保存しない**（`content: {kind: package|campaign|scenario, path}` 参照で再ロード＝単一真実源・content 修正が生きる、package 版不一致は警告のみ・形式版不一致はロード拒否=v1 実験的）。`save_session` は tmp→rename **原子書き込み**（受理ターン毎上書きで書きかけに耐える）。**CLI 結線**: 受理ターン+遷移が確定したループ先頭で autosave（`--save <path>` 既定 `kataribe_autosave.yaml`、turn 0 は書かない=起動しただけで既存セーブを潰さない）・`--resume <file>` で再開（campaign は module+campaign_memory も復元、開幕に「前回までの語り」表示）。**実機検証**: 霧の村 3T→--resume→GM が経緯（前ターンの口ごもり）を踏まえて続行・帳簿タイマー（議論T）もセーブを跨いで正しく発火。PoC: `session_save_roundtrips_state_and_carryovers`（roundtrip 同値+版不一致拒否）。**✅Phase C（GUI, 2026-07-04, 査読3点ユーザー確定）**: 置き場=**app data dir**（`saves/<パッケージパスの FNV-1a ハッシュ>.yaml`＝パッケージ別 1 autosave・配布 zip と分離）／`play_turn` 受理+遷移確定後に上書き（却下では書かない・失敗は警告のみ）／`list_packages` が `autosave_turn` を返し**選択中パッケージにセーブが在ればヘッダーに「続きから (turn N)」**（主ボタン化・「新しいゲーム」は控えめ配色へ＝誤上書き防止）／`resume_game` command（`open_package`=new_game と共通化、campaign はセーブの module を注入込みで開く）が正本+継続性を復元し `GameView.resumed` で「── 続きから ──」+前回までの語り+版警告を開幕ログへ。**✅Phase D 手動セーブスロット (2026-07-16, ユーザー要望=気に入ったシーン(ロマンス等)を何度もロールプレイ)**: `saves/<hash>_slot{1..5}.yaml`（autosave と同じ器 `SessionSave` を別名で書くだけ＝形式追加ゼロ）。ヘッダーの「続きから」と「新しいゲーム」の間に「セーブ」「ロード」ボタン → 5 スロットのダイアログ（`SaveSlotDialog`、メタ= turn + mtime 日時 + 直前の語り冒頭 60 字）。command 3つ: `list_save_slots`（省略時=プレイ中 session のパッケージ）/ `save_slot`（`session_save_of`=autosave と共通スナップショット）/ `load_slot`（`restore_session`=resume_game と共通化、**GameSession 丸ごと差し替え**＝LLM は毎ターン messages を再構築するので前のプレイの記憶は構造的に残らない）。ロード後の autosave 書き先は常に autosave パス（スロットは凍結点のまま・眺めるだけロードで最新進捗を壊さない=ロード時に autosave は書かない）。`delete_autosave` はスロットも一括処分（`has_slots`）。PoC 2 本（ファイル名非衝突/snippet の char 境界）。**spec 07 は Phase A〜D で Done**。設計は `specs/07_save_load.md`、契約は data_contract `SessionSave` 節。留意: セーブは正本のスナップショットゆえ**秘匿役職も平文で入る**（ローカル file・暗号化はスコープ外宣言済）。GUI 実機の再開動線は起動して目視確認。
- ✅ **narration の正本接地 (2026-06-24, 実プレイ発見→修正)**: 「正本>文章力」は **ops にしか効かない** — narration は engine 非検証なので、所持品に無い「行商ネックレス」を LLM が op 無しで贈与する捏造が素通りした（`failures.md #23`）。op には engine のバックストップが在るが narration には原理的に無く、**prompt 層が唯一の防衛線**（#19 との非対称）。GM_SYSTEM に「プレイヤー行動文は意図であって事実でない/未所持物は存在しない/narration は非検証ゆえ GM 自身が矛盾を防げ」を刷り込み、実 LLM で「手元に無い」と接地・状態変化ゼロを確認。`gm_system_grounds_unowned_items` で固定。
- ✅ `harness` bin `play`: 実クラウド通しプレイ CLI。`LlmClient` を `DeltaProposer` に配線、`.env` 実キーで密室脱出を回す（`cargo run -p harness --bin play`、stdin 対話 or 台本流し込み）。
- ✅ **核心的未知の測定 (2026-06-23, claude-opus-4-8 @ Anthropic 互換)**: 密室脱出を実 LLM で通しプレイし **goal 到達（turn 4, 4/4 一発合格）**。LLM がエンジンの制約内で構造化出力を出し続けられることを実証。schemars 生成スキーマも Anthropic 互換層が受理（`failures.md #3` 解決）。実 API で判明した罠は `failures.md #12-13`（temperature 非対応 / dotenv 副作用）。
- ✅ **敵対プレイ実測 (2026-06-23)**: 「正本 > 文章力」を実 API で実証。LLM は scenario_brief の gate を読み不可能な単独行動（解錠前 move・幻 master_key）を自ら拒否（prompt 層接地）。複数ステップを束ねた行動では原子性違反デルタを提案 → エンジン却下 → 理由還流 → LLM が合法な部分手に修正（attempts=2）。`failures.md #15`（再生成のプレーン echo 形式が実 API で通る）を実証、`#14`/`#16` は watch。
- ✅ **memoria_bridge 実 LLM 実測 (2026-06-24, claude-opus-4-8)**: demo 盤面で発火→cascade(recall_promise→renew_vow)→goal を端から端まで実機実証。曖昧 cue で **TF-IDF cosine 経路**が伏線を正しく recall(┊ で surface)。**神経 embedding は不要を接地で確定**（authored cue は exact 保証＋語彙重なりの TF-IDF で実用十分、`failures.md #21`）。発見: ①数値閾値は LLM の自然増分(好感度 +1〜3/ターン)に較正要(`#20`) ②強モデルは profile だけで伏線を自発的に語るので bridge の固有価値は「発火保証」と「大規模 lore の on-demand recall」に絞られる(`#22`)。
- ✅ `app/` **デスクトップ殻 (Tauri2 + Vue3)**: LocalAI prompt 殻の足場を移植（chat/project/MCP/SSE は捨て GM プレイ画面を新規）。`app/src-tauri`(Rust, workspace 外の独立 project で重い tauri 依存を core から隔離) が `harness::run_turn` を Tauri command `new_game`/`play_turn` で叩く。`GameSession{state,scenario,lore,client,pending_lore,lang}` を `Mutex<Option<_>>` で manage（正本は backend が握り、frontend は view DTO を描画するだけ＝CLI `play` の GUI 版）。narration の literal `\n` 正規化(`#16`)・却下理由 localize(`KATARIBE_LANG`)は提示層で実施。`app/src`(Vue3+Pinia) は会話ログ(narration/✦beat/┊recalled/rolls/reject) + 行動入力 + 状態パネル。**検証**: backend `cargo check` green（harness 結線が型検査通過）/ frontend `vue-tsc`+`vite build` green。GUI 実機起動（`npm run tauri dev`, WebView2 必須）は **✅Windows 機で起動確認済 (2026-06-24)**。**アイコンは Kataribe オリジナルへ差し替え済 (✅2026-07-02)**: 原画 `kataribe.png`（repo 直下、587×583）→ 透明パディングで正方形化 → `tauri icon` で全サイズ生成（デスクトップのみ、android/ios 出力は破棄）。`bundle.icon` を tauri.conf.json に宣言（従来未宣言）、exe 埋め込みは cargo build + exe からのアイコン抽出で目視検証済。**←✅2026-08-31 Lorekeel 新アイコンへ再生成**: 原画はユーザー作成の `images/lorekeel.png`（655×655 正方形 RGBA、パディング不要）→ 同じ `tauri icon` 手順で 17 ファイル全差し替え（android/ios 破棄も同じ）。この原画は README 3 言語のヘッダロゴでもあるので差し替えが README にも効く。
- ✅ **GUI 実機起動 (2026-06-24)**: Windows 機で `npm run tauri dev` 起動を確認（WebView2）。app 殻が実機で動く接地完了。
- 🟡 **設計方針: キャンペーン化 — 密室脱出からフィールドTRPGへ (2026-06-24 設計収束・gm_core+harness 着地済、app 残)**: 単一シナリオ（1 goal + 内部場所グラフ）を、**状態を持ち越したまま authored モジュールを繋ぐ**キャンペーンに一般化する。北極星「忘れない GM」を場所移動に適用。**結線は PoC-2c で着地**（上記 orchestration bullet、`campaigns/escape.yaml` + `harness::advance_campaign`）。詳細は `data_contract.yaml` の `Campaign` / `CampaignFile` / `Orchestration` 節。要点:
  - **語り部 = 自己完結 file の純粋実行器**。各モジュール = `{scenario 骨格 + GameState + RngState}` の1 file（`GameState`/`RngState` は既に `Serialize` 派生済＝save-state 化が型レベルで可能、出目まで再現）。生きた可変コピーが2つ無い → **split-brain が構造的に起きない**。二エンジン（SomniumTextor 等）同期は**棄却**（正本が2つ＝矛盾する GM）。
  - **チェックポイントは authored、間は GM が narration で即興**（TRPG 流）。flag/gate/goal/trigger は人が書く骨格＝エンジンが正本である対象。**LLM にシナリオ/フラグを生成させる案は棄却** — 拘束が流動したら一貫性保証が崩れる（→ `validate_scenario` も scenario 生成も不要）。
  - **`transition(state, 次の骨格)`**: state を持ち越し（リセットしない）骨格だけ差し替える純粋関数。フラグの持続性は **per-flag の authored 属性**（捨てる/世界に持つ/その場所に持つ）— 二値（global/局所）でハードコードすると**再訪で詰む**（"宝箱はもう開けた" は局所だが捨てられない＝復活すると矛盾する GM）。
  - **`goal: Gate → goals: {GoalId: Gate}` + `reached(state)->Option<GoalId>`**（authored 順で最初の一致＝決定論）。条件付き勝利（「flag 立てば A、無ければ B」）は既存合成 Gate（`All`/`Any`/`FlagIs{bool}`）で**今すぐ書ける**が、**どのエンディングか**を知るには named goals が要る。発火 GoalId が次モジュールの**分岐セレクタ**＝自由度の出所。
  - Memoria はフィールド規模で初めて働く（密室は小さすぎた。NPC/伏線/土地 lore が積もる＝大規模 lore の on-demand recall が効く高度）。
  - 最小着地順: `transition`（状態の器・per-flag 持続性込み） → `reached()`（分岐の出口） → **`advance_campaign`（地図駆動の結線、✅着地）**。app の複数モジュール表示も ✅2026-06-26（campaign-entry パッケージが GUI で playable、goal 到達で次モジュールへ遷移）。per-flag 三値持続性（捨てる/世界に持つ/その場所に持つ）も ✅spec 02 (2026-06-27)（`persistent_flags` + `CampaignMemory` で再訪「宝箱はもう開けた」を解決、双方向グラフ対応）。残: モジュール内の単一 location 粒度 / `CampaignMemory` の save 永続化。
- ✅ **GUI 目視確認 (2026-06-29)**: 入力 UI（Claude Code 風・送信マーク ↵ 内蔵・自動伸長）/ presence 顔アイコンの入退場入れ替え（spec 04）/ 音声の実再生（BGM ループ・SE、spec 01 Phase 3）を実機で目視確認済み。spec 01（没入アセット）と spec 04（presence）は実描画/実再生まで接地完了。
- ✅ **目標（goal）一覧の可視化 + `GoalDef.title`/`hint` (2026-07-02, ユーザーFB)**: 「TRPG で何を目指せばいいか最初にわからない」→ 右ペイン presence 行の上に「目標」一覧を表示。`StateView` に `goals: [{id, title, hint}]`（authored 順）+ `reached_goal`（到達 id、`Scenario::reached` 直結）。**gm_core 新設（どちらも serde default・非検証の提示素材・engine 不解釈）**: `GoalDef.title`＝人間向け表示名（id はスペース等を避ける機械用の分岐セレクタゆえ、**id=セレクタ / title=表示名 / hint=道しるべ の三層**。空なら提示層が id へフォールバック）、`GoalDef.hint`＝「何をすればだいたい行けるか」の**プレイヤー向け道しるべ**（`when` の条件はネタバレゆえ出さない設計に対し作者が意図的に開示、prompt 非注入＝GM に見せるとレールロード圧）。到達メッセージ（`TurnView.goal_title`、結末「モカの告白」に到達）も title 優先。到達済みは ember ハイライト + ✓。単一 goal（無名・後方互換）シナリオでは一覧非表示。Icon に `target` 追加。ドッグフード: houkago(her_love_me=モカの告白)/sealed_shrine(3 goal=冒険の終わり/封印エンド/討伐エンド) に title+hint 記述。**`GoalDef.visible`（既定 true、同日追加）**: false で**隠しゴール** — 到達するまで目標一覧に出ない（app `state_view` が DTO 段階で落とす＝ネタバレ衛生、到達で開示され ✓ 付きで現れる）。到達判定 `reached`/transition 分岐は不変で効く＝engine 非解釈の提示層宣言（`hidden_stats` と同類）。「???」で存在だけ匂わせたい時は visible のまま title を「???」にする流儀。PoC: `goal_title_hint_visible_parse_and_default`（Red→Green、省略時 title/hint 空・visible true＝既存 YAML 無改修）。
- ✅ **アイテム取得様式 `take: once/infinite/fixed` (2026-07-02, ユーザーFB)**: 場所アイテムに取得様式を宣言できる。`Location.items` の値を `Gate` 直書き（旧形式＝`once`）と `{when, take}`（新形式）の **untagged 両受け**にし既存 YAML 無改修。**`once`（既定）**: 一度取ったら場所から無くなる — 新設 `GameState.taken_items`（場所別の持ち去り記録・セーブ対象）で再取得＝**複製を却下**（従来は鍵を NPC に渡して戻ると同じ鍵がもう一本取れた＝正本の複製穴、今回封鎖）。**`infinite`**: 自販機のジュース等、何度でも取れる。**`fixed`**: シャワー/リモコン等の**備え付け・取得不可** — 却下理由 `ItemFixed`「備え付けで持ち運べない（取得せず、その場で使える）」が self-repair で LLM に語り直しを誘導。二層: `scenario_brief` も備え付け/何度でもを先回り接地（見出しは「アイテム」に改名）。`RejectReason` に `ItemFixed`/`ItemAlreadyTaken` 追加（Ja/En）。`taken_items` は transition 非持ち越し（LocationId はモジュール内スコープ、campaign 再訪の持続は spec 02 と同じ問いで後続）。PoC: `item_take_modes_once_infinite_fixed`（gm_core、Red→Green）/ `scenario_brief_surfaces_item_take_modes`（harness）。
- ✅ **右ペインの縦タブ化 (2026-07-02, ユーザーFB)**: 1枚に全部積むと全体スクロールになる→縦タブ 2 枚に分割。**進行**（ターン/目標/goal 到達バナー/この場にいる）と**状態**（現在地/所持品/フラグ）。左端 rail（w-9、アイコン + `writing-mode: vertical-rl` の縦書きラベル、active は ember 左ボーダー + glow）。旧 h2「正本の状態」はタブが意味を担うため撤去。frontend のみ（`activeTab` ref、DTO 無改修）。**タブ切替ショートカット (✅2026-07-08, ユーザーFB)**: **Ctrl+Tab=トグル**（2 枚なので往復が最速）+ **Ctrl+1/2=直接選択**（タブ増加時の拡張枠）。PageUp/PageDown 案は棄却 — ①チャット UI の「ログ遡り」慣習と衝突（将来ログスクロールに割り当てる枠として温存）②日本語 IME の変換候補ページ送りと衝突。`isComposing` ガードで変換中は発火させない。タブ button の title にヒント表示。
- ✅ **AI モデル名の常時表示 (2026-07-11, ユーザーFB)**: TitleBar のパッケージ一覧アイコンの右に**使用中モデル名のバッジ**（`get_llm_config` の model、hover で「設定 → AIモデル で変更」）+ OS ウィンドウタイトル（タスクバー/Alt+Tab）にも `Kataribe — 語り部 (モデル名)`。store `llmModel` + `refreshLlmModel()`（起動時 App.vue onMounted / AIモデル設定の保存直後に呼ぶ＝即時反映）。#46（dev で repo .env が GUI 設定を隠し「再起動で gemini に戻る」）の再発時もどのモデルで動いているかが一目で分かる可視化。frontend のみ、build green。
- ✅ **会話ログのテキスト保存 (2026-07-09, ユーザーFB)**: タイトルバーに記録アイコン（Document+↓）を追加、クリックで現在の会話ログを「`{日時}_{パッケージ名}.txt`」で保存。**backend command 3 つ**: `save_log_file(folder, file_name, content)`（**ファイル名はパス要素禁止＝トラバーサル遮断**、フォルダ不在は作成、書いた絶対パスを返す）/ `open_log_folder(folder)`（OS ファイルマネージャで開く＝`std::process::Command` で explorer/open/xdg-open、tauri-plugin-shell 不要＝custom command ゆえ追加権限ゼロ）/ `get_default_log_dir`（設定の placeholder 用）。**保存先は設定「ログ」タブ**でフォルダ指定（`localStorage["kataribe.logDir"]`、空なら既定 `app_data_dir/logs`＝spec 07 saves と同じ流儀・repo を汚さない）+「フォルダを開く」「既定に戻す」ボタン。frontend: store `formatLog()`（ConversationLog の見た目に沿ってプレーンテキスト化：narration/player/beat/rolls/checks/reject/retries/system 各 kind）+ `saveLog()`（日時 stamp は JS `Date` で locale-aware、title は `[\/:*?"<>|]` 除去＋40 字切り）、App.vue に右下トースト（`logToast` を watch して 4 秒表示）。frontend build green + app backend cargo check/clippy/test green。実保存/フォルダ open は GUI 目視。
- ✅ **開発者モード / メタ質問 (2026-07-11, ユーザーFB)**: シナリオ作者のテストプレイ中に、行動入力へ `<meta: なぜ○○した？>` の形でメタ質問を挟むと、GM が物語を止めて判断根拠を開発者に率直に説明する（盤面のどこを見たか／なぜ ops をそう組んだか／なぜ間違えたか）接地の可視化装置。#47/#48 のような破れをプレイ中に直接診断できる。**env `KATARIBE_DEV_MODE`（truthy）で有効**（`LLM_DEBUG` と同流儀の env 直読み＝app/CLI/run_turn の signature 不変、app は app_data/.env・CLI は repo .env/環境変数）。**GUI 設定「開発者」タブのトグルからも切替可能**（`set_dev_mode`/`get_dev_mode` command、#46 と同経路で env 即時反映 + app_data/.env 永続化＝再起動後も維持。Kataribe はユーザー＝制作者ゆえシナリオ制作中に使う想定、ユーザーFB）。実装は prompt 層のみ（engine 不変）: `prompt::DEV_META`（メタ質問の応答規律＝物語を進めず ops 空を刷り込む先頭ブロック）+ `gm_system_prompt(scenario, dev)`（純粋関数、dev で DEV_META を system 先頭に。dev/非 dev で安定プレフィックスが分かれるだけで prompt caching は不変）+ `dev_mode_enabled()`（env 判定、`is_truthy` は純粋でテスト可）。turn.rs の system 構築を差し替え。通常プレイには DEV_META は一切漏れない。PoC: `dev_mode_injects_meta_block_only_when_enabled`。app/CLI 無改修（実プレイでの `<meta:>` 応答は GUI/CLI で目視）。
- ✅ **マップパネル / spec 15 (2026-07-16, ユーザー要望)**: 右ペイン第4タブ「マップ」（Ctrl+4）に**訪問済み+1歩先の有向グラフ**を SVG 描画。動機=プレイヤーが「どこ行ける?」を GM に尋ねる往復（=1ターン=フルプロンプト1往復）を減らす（GM prompt は #42 で既に出口を知るので **prompt/engine は不変**、効くのはプレイヤー側の探り行動の削減=間接トークン節約）。**engine 無改修の派生表示**: `Scenario.exits`（有向グラフ）+ `GameState`（現在地・`Gate::eval`）+ chronicle（`TurnLog.location`=訪問済み）から `map_view` が導く（可変状態を増やさない=訪問済みは history 由来）。**可視範囲=霧（ユーザー確定）**: visited={start,現在地}∪{history の location}を現 scenario に限定（campaign 遷移で前モジュール location を除外）／frontier=visited の各出口先（1歩先・未訪問でも名前を出す）／奥は霧=出さない／辺は visited ノードの exits のみ。gate 未達出口は🔒（破線・行き先名は出すが条件文は出さない=ネタバレ回避）、現在地からの通れる辺は ember 強調。frontend `MapPanel.vue`=現在地起点 BFS 距離のランク列レイアウト（辺=SVG、ノード=HTML absolute div のハイブリッド）。DTO=`MapView{nodes:[{id,title,current,visited}],edges:[{from,to,locked}]}` を GameView/TurnView に載せる（却下は現状スナップショット）。PoC 2本（霧の範囲=訪問済み+1歩先で奥を隠す/gate ロック+現在地）。app backend 16 green・clippy clean、vue-tsc/vite build green。**Phase C=GUI 目視が残**（レイアウト・現在地強調・🔒・移動での更新・campaign 遷移で遷移先グラフへ差し替え）。設計は `specs/15_map_panel.md`、契約は data_contract `Map` 節。
- ✅ **パッケージ検分 `play lint` (2026-08-09、Meta Muse Spark でのシナリオ生成が良かったという実測から)**: **LLM 呼び出しゼロ・API キー不要**の静的検査 CLI。動機は**生成の検証ループ** — `package_spec.md` を渡して LLM にパッケージを書かせる流儀（配布サイトの `/guide` が案内している道）では「作らせる → 検める」を何度も回すのに、従来これを見る唯一の経路が**アプリに入れて新しいゲームを始める**（開幕の ⚠）で、キーが要りターンを 1 回消費した。**新しい検査規則はゼロ** — `unknown_key_lints` / `Scenario::validate` / `Scenario::lints` / `manifest_lints` / `character_lints` を 1 つの入口へ集めるだけ（`harness::inspect`、フォルダ=パッケージ / `.yaml`=シナリオ単体の下書き）。終了コードは errors があるときだけ 1（スクリプトで束ねられる）。**ついでに射程の穴を 2 つ塞いだ**: ①**`characters/*.yaml` が完全に無検査だった** → `harness::character_lints`（`manifest_lints` と同型・既知キーは型から導出＝手書きしない。`stats` を `stat` と書くとそのキャラは数値を 1 つも持たないまま静かに動く。`stats` の値が mapping のときは `StatDecl` の中まで見る）。**人は同梱の他ファイルを見ながら書くが、モデルは仕様書だけを見て書くので欄名の揺れがここに集まる** ②**campaign の各モジュールが未知キー lint の射程外だった**（`load_module_injected` が生テキストを返さないため。`LoadedCampaignPackage.warnings` の注記どおり）→ 検分側は自分で読むので全モジュールを歩いて塞いだ。**検査順は「壊れていても見えるもの」から**（characters → manifest → entry/モジュール）＝シナリオが load 失敗してもキャラの typo は見える（生成物では両方同時に壊れているのが普通）。**app の作者向け文面を harness へ移設**（`scenario_lint_messages`）— 受け手は GUI の開幕 ⚠ でも `play lint` でも同じ「作者」で、別々に持つと片方だけ直したときに必ずずれる（収縮・app は薄い包みに）。**毎回言う注記**: 「静的検査が通っても遊べるとは限らない — 各ゴールへの到達経路は手でたどれ」。`validate` も `lints` も**各機構を単独で**検査するので、独立に正しい機構どうしの相互作用は原理的に捕まらない（2026-07-09 の実例: 同じフラグへの `flag_hints` + `hidden_flags` で真エンドが到達不能、validate 無反応、発見は手トレースのみ）。**緑は「読める」であって「遊べる」ではない**。PoC 4 本（同梱 4 パッケージで偽陽性ゼロ / campaign 全モジュール走査 / キャラ欄名 typo の名指し+提案 / シナリオ単体）。実機で確認: 正常パッケージ ✓・typo 注入で ✗1 ⚠1 + exit 1。
- ✅ **macOS の署名と公証 + Homebrew tap (2026-08-27 = Done。`v0.5.16` が署名・公証された最初のリリース)**: 起点は調査で出た前提 — Kataribe の `.app` は **リンカのアドホック署名だけ**で Developer ID も公証も無く (v0.5.15 の成果物を解析して確定: 署名スーパーブロブに CodeDirectory しか無く CMS スロット不在・`flags = 0x00020002` = adhoc + linkerSigned・`_CodeSignature/` 不在)、Homebrew は検疫属性を付けて配るので **cask にしてもインストールは成功してアプリが開かない**状態だった。**実装は Fuseforks からの移植** (あちらは v0.1.9 = 2026-08-17 から公証済みで、手順・罠とも `D:/Github/Fuseforks` の CLAUDE.md「macOS の署名と公証」が一次資料): job env `HAS_APPLE_SIGNING` で**秘密 5 つを全部数え**、揃うときだけ `$GITHUB_ENV` へ生やす + 未署名時に明示ログ。**空文字を渡してはいけない** (Tauri の `macos/sign.rs` は `var_os` で見るので空文字も「指定された」と読み macOS だけ落ちる)。**`verify-notary.yml`** (手動) も移植 = 公証の資格情報だけを `xcrun notarytool history` で 1 分検証する。**Team ID `6XU323VJZN`・証明書は G2 Sub-CA で 2031-08-18 まで**、p12 は Windows の openssl で G2 中間証明書と束ねて作成済み (`~/.apple-signing`)。**`p12-password.txt` は末尾 CR LF なので `tr -d` で剥がす** (台帳が警告する「1 文字ずれ」の罠を値を出さずに検出できた = 長さとバイトだけ見る)。**署名すると配布物に実名が恒久的に載る** (個人登録ゆえ CN が個人名) — Fuseforks で承知のうえ選んだ判断がそのまま適用。**実測 (2026-08-27)**: `verify-notary` 緑 (`APPLE_PASSWORD` 19 文字 = ハイフン込み・`notarytool history` が Accepted 履歴を返す) → v0.5.16 のビルドで `Apple signing: enabled` → **成果物を解析して確認** (`flags = 0x00010000` = hardened runtime で adhoc ビット消滅 / CMS 9058 バイトに `Developer ID Application: kosei takahashi (6XU323VJZN)` → `Developer ID Certification Authority` → `Apple Root CA` / `Contents/CodeResources` 1653 バイト先頭 `s8ch` = 公証チケット綴じ込み済み) → **tap の `verify-cask` を実機 macOS で緑** (`spctl` = `accepted` / `source=Notarized Developer ID`)。**`releaseDraft: true` へ (ユーザー決定 A)**: 3 OS matrix (`fail-fast: false`) は**1 OS 通過だけで Release を公開**してしまい、署名で macOS の工程が伸びた分そこを踏む確率が上がる (Fuseforks は同じ形で 2 回踏んで true にした)。**タグは push した時点で残る**ので publish しないことは「その版が存在しなかった」を意味しない。**Homebrew tap (`betyourluck/homebrew-tap`)**: `Casks/kataribe.rb` を Fuseforks と同居 (tap は cask を何本でも置ける)。`auto_updates` は付けない (Kataribe に updater は無く、付けると `brew upgrade` の対象から外れて新版が届かない) / `depends_on macos:` も書かない (OS 下限を未計測) / `zap` が消すのは**セーブデータ・書庫の取得物・参照画像・挿絵・ログ・API キーを持つ `.env`** (全部 `jp.kataribe.app` の下)。**`verify-cask.yml` を汎用化** — `.app` 名は `brew info --json=v2` の `app` スタンザから引く。**トークンからは導けない** (`fuseforks` → `fuseforks.app` だが `kataribe` → `Kataribe.app` で大小が違う = Tauri の productName がそのまま出る) ので、cask が増えてもこの workflow は触らなくてよくなった。**順序の学び**: cask の検証には**アセットが公開 URL に出ている必要がある** — draft のままだと Homebrew の素の curl が 404 を受ける (認証つきの `gh release download` では落とせるので、こちらの確認手段では気づけない)。ゆえに 「3 OS 揃ったのを見て publish」→「cask 検証」の順にしかできない。README 3 言語に導線 (完全修飾名が必須・Apple Silicon 専用・`uninstall` はセーブと API キーを残す)。**✅macOS 実機で起動確認 (2026-08-28、ユーザーが MacInCloud で実測)**: v0.5.17 の dmg を curl で取得 (= **検疫属性が付いたまま**の受領者と同じ条件) → `~/Applications` へコピー → `spctl` が `accepted` / `source=Notarized Developer ID` / `origin=Developer ID Application: kosei takahashi (6XU323VJZN)` → **起動しウィンドウが描画された**。「Gatekeeper が受け入れる」と「アプリとして動く」は別の主張で、後者がここで初めて埋まった。**借りた Mac では Homebrew が使えなかった** (`/opt/homebrew` が別ユーザー所有・sudo なし = 共有プランの常態) が、cask 自体は GitHub の macOS ランナーの `verify-cask` で証明済みなので、実機では dmg 直で測れば足りる (**検証したい主張ごとに、それが測れる最小の場が違う**)。**✅v0.5.19 リリース (2026-08-31)**: 中身 = spec 28 v2 (構文木) + 設定ミラー + use_tts 撤去 + 本文サイズ分離 + 新アイコン。draft が「Kataribe v0.5.19」で作られ **build.yml の `releaseName` の改名取りこぼしが発覚** → draft は gh で修正・build.yml も修正済み (次のタグから正しい名前)。リリースノートは英日 2 言語 (v0.5.18 の体裁)。cask 0.5.19 更新 (sha256 は**公開 URL から実 DL で計算** = 404 でないことの同時確認)・verify-cask 実機 Green (`Lorekeel.app: accepted`)。**verify-cask は cask 入力必須** — 無指定は既定 fuseforks に落ち、**緑が出ても lorekeel の証明にならない** (実際に一度踏んで log の grep で気づいた。緑を読むときは「どの主張の緑か」を確認する)。 **✅v0.5.20 リリース (2026-09-01)**: 中身 = failures #93 (パース失敗の診断を握り潰していた) + #94 (1 ターン 2 個以上のダイスで開帳が詰まる) + 要約タイムアウトの UI 選択 + `max_per_turn` + エディタ検索パネルの和訳 + 書庫既定の `lorekeel.outcasts.jp` 切替 (別名表つき)。**前版の `releaseName` 修正が効き、draft が最初から 「Outcasts Lorekeel v0.5.20」で作られた** (取りこぼし回収の実地確認)。リリースノートは英日 2 言語で v0.5.19 の体裁を継承 — **利用者から見える変化だけを書き、台帳・調査系のコミット (UpgradeCode の導出・別名表の設計理由・#93/#94 の一般化) は落とす**。**winget は告知しなかった** — PR がマージ待ちで `winget install` が通らない段階で書くと「書いてあるのに入らない」になる。publish と cask 更新はユーザー作業。**cask の sha256 は GitHub release API の `digest` と突合**した (v0.5.19 は実 DL で計算していた — digest が取れると分かったので手数が減る。ただし**実 DL は 404 でないことの同時確認**にもなっていたので、digest 突合に替えるなら公開 URL の生存は別途見る)。テストは workspace 367 / app backend 80 / frontend 53 (3 本とも実走で確認)。
- 🟡 **パッケージエディタ / 編集モード — spec 28 (2026-08-27 起草→同日査読 2 本 (軽微 8+要整理 5+追記 3) 反映 rev2→未決 4 点は両査読とも提案どおり確定→**Phase A〜D + 追補を 2026-08-28 に、v2 (構文木) を 2026-08-29 に実装**。残 = ウィザード / AI にパッケージを直させる)**: パッケージの YAML を**アプリ内で**編集する。動機 = 「書く場所と検める場所が別」「汎用エディタは Kataribe の語彙を知らない」の二摩擦を畳む。**前提の訂正が起点**: ユーザーの「エディタは LSP 準拠」は誤りで、CodeMirror 6 は LSP クライアントを内蔵しない — 持つのは診断 (`@codemirror/lint`) と補完 (`@codemirror/autocomplete`) の**拡張点**で、検査規則は全部 Rust 側に既にある (`inspect` の 5 系統) から **LSP サーバを立てず Tauri command 直結が正しい形** (新しい検査規則ゼロ = `play lint` と同じ収縮)。**形式は 1 バイトも変えない** (エディタは形式を書く道具であって形式ではない = outcast package_spec.md 波及なし)。**✅Phase A (骨格)**: ①鉛筆トグル (**アプリヘッダの「新しいゲーム」の右隣**・ON=ember・卓中/未選択 disable。2026-08-28 ユーザーFB でタイトルバーから移設 — タイトルバーはアプリ全体の操作の場で、編集はパッケージに対する操作) → 会話ペイン+入力欄が `EditorPane` に差し替わる (**表示のみ** — プレイ状態は破棄せず OFF で戻る。動的 import = CodeMirror を main に握らせない chunk 境界維持。2026-08-29 実測で main 368KB / CodeEditor 425KB) ②右ペイン第 6 縦タブ「ファイル」(編集モード中のみ・OFF は**入る直前のタブへ**復帰 = 査読反映) = package.yaml / campaign.yaml / scenarios / characters / memoria を**各フォルダ直下・YAML のみ**列挙 ③**編集ルート登録制** `open_editor`/`close_editor` — 読み書き command は相対パスだけを受ける (保存の瞬間に選択が替わる TOCTOU を構造的に消す = 査読反映) ④`save_package_file(rel, text, fork)` の判定順を凍結: 卓ガード→パス検証 (形 + canonicalize containment = symlink 脱出遮断)→原子書き込み (tmp→rename)→**fork 時のみ出所メタ削除** (書き込み成功の後 = 逆だと「フォーク済みなのに中身は書庫版」の半端。削除失敗は警告のみで次の保存が再試行・メタ不在は冪等成功) ⑤**フォーク確認は初回保存時** (spec 27 の決定「編集したらフォーク」の具体化。事前認知は書庫版バッジ — 置き場は spec の「ファイルタブ上部」でなく**エディタヘッダ** = フォーク確認が発生する保存ボタンの隣) ⑥**卓との排他は入口で塞ぐ** (査読で保存時拒否から強化): 卓中は鉛筆 disable / 編集中は卓開始 disable の両向き + backend 卓ガードの二層 (**backend はホスト側しか見えない** — participants はホストの session にだけあり、GuestAssetRoot は解除経路が無く卓の生死シグナルにならない) ⑦未保存確認 4 契機 (ファイル切替 / OFF / パッケージ切替 = App.vue の packagePath watch で強制 OFF・キャンセルは revert / アプリ終了 = onCloseRequested) ⑧プレイ中の保存は許可 + トースト「次の新規/ロードから反映」(反映は既存仕様が保証 — SessionSave は骨格を持たず content パス参照)。`CodeEditor.vue` に `language=yaml` 追加 (`@codemirror/lang-yaml`、構文色のみ・行番号は text 以外に拡大)。PoC 4 本 (パス検証の形と脱出 / 一覧の直下限定・決定論順・containment / **fork の順序** / 原子書き込みの tmp 不残)。**✅Phase B (診断二層、同日)**: 層 1 = `lint_editor_text(kind, text)` を CodeMirror linter (デバウンス 500ms + lintGutter) へ — parse エラーは serde_yaml `Location` で**正確な行** (severity error・単独報告)、未知キー lint (manifest/scenario/character。campaign・memoria は parse のみ = 専用 lint を新設しない) は**親キー列の範囲絞り込み**で行を近似 (終端キー単純検索は id/name 級で誤爆するので採らない・絞れなければ line: null = **行を偽らない**)。層 2 = `inspect_editor_package` (**入場時 + 保存成功時**) = `inspect_package` + 接頭辞→ファイル帰属 (`package.yaml` / `characters/{name}` / `{entry}` / `modules.{id}`→campaign 地図) → ファイルタブ下部に一覧・クリックでそのファイルへ・「緑は読めるであって遊べるではない」薄字常設。**副産物 = 既存の穴 1 件**: `inspect_package` の単発 entry 分岐が `Scenario::lints` を報告していなかった (campaign 分岐は足していた・app 開幕 ⚠ は別経路で足すので隠れていた = **`play lint` の単発パッケージだけ lint が落ちていた**)。Red→Green で塞いだ結果、**friday_lemmon の flag_hints 全 15 件が真陽性** (全部トリガー専権フラグ宛て = GM の語彙に出ずどこにも表示されない死に荷物) と判明しブロックごと撤去 (挙動変化ゼロ。2026-07-27 の銀の指輪と同じ「lint 強化の初回実行が同梱 content の実弾を出す」類型)。PoC: app `editor_lint` 5 本 + harness 1 本 Red→Green。**✅Phase C (補完、同日)**: `editor_vocabulary` command — **キー名は型から導出** (`struct_keys` + `gate_variant_keys`/`op_variant_keys` を gm_core で pub 化 = **lint と同じ表**を補完が見る。「補完に出るのに lint に叱られる」乖離を構造的に作らない)、**配線 (どの親キーの下がどの型か) だけは手書きの表** = lint.rs `walk` の構造知識の写し (v1 のドリフト許容 — ずれても補完が古いだけで lint は正しい。統合は walk のデータ駆動化 = v2)。id 語彙は**ディスクの保存済みパッケージ**から (単発 = entry / campaign = **全モジュールの和**。読めなければ id 補完が減るだけでキー補完は常に効く)、入場時 + 保存成功時に再取得。frontend (`editorCompletion.ts`) の文脈推定は **行内タグ (`op:`/`kind:`) 最優先** (実 content は flow style 主体でインデント遡りが行内で効かない) → 親キー (実効インデント遡り) → 祖父 (`map_child_keys` = locations 等の名前つき map 容器) → 全キー〔**v2 で撤去** — 平らな表と近似ごと構文木の walk へ〕。値補完はフィールド名 → id カテゴリ写像で **`to:`/`key:`/`from:` の多義を行内タグと doc kind で解決** (op=give_item の to は entities / move は locations。絞れなければ候補カテゴリ併記 = 黙らない)、**値欄は「: 」直後に開く** (id の typo = 死んだ参照 lint と同じ誤りの上流予防が本命)。`harness::Campaign` に Serialize derive (`struct_keys` の要件)。PoC: app `editor_vocab` 3 本 (型導出の表明 / id 収集と壊れ耐性 / campaign 全モジュール和)。**✅Phase C-2 — 説明つき候補と書きかけでも効く語彙 (2026-08-28 ユーザー要望「昔の Visual Studio 並のコード補完」)**: v1 の枠内 (Lezer は v2 のまま) で 4 点。①**候補が説明を持つ** (`VocabItem{name, doc?}`) — doc は `editor_docs` が **crates の doc comment から機械抽出** (`include_str!` で型定義ファイルをビルド時に焼き行走査。key は `Scenario.world` / `StateOp::give_item` / `StateOp::give_item.from` の 3 形)。**説明の表を手で持たない**のが核 = 持てばコードの doc・data_contract・補完表の三箇所が必ずずれる (掟の追従漏れの説明版)。doc の無い欄は説明なしで出す (捏造しない)・`syn` は足さない (対象は自分たちのコードだけで、書式が変われば PoC が落ちる)。最も効くのは **`op:`/`kind:` の値** (「この op が何をするか」がその場で読める)。id 候補の説明は**作者の付けた表示名** (`Location.title`/`CharacterDef.name`/`flag_titles`/`GoalDef.title`/`ChallengeDef.description` — id は機械用キーなので一覧だけでは何の場所か分からない)。②**id 収集が `validate` を通らなくなった** — 旧実装は `load_package` の Ok だけを見ており、`validate` は幻フラグ 1 つで Err を返す = **書きかけの盤面という補完が最も要る状態でだけ id 語彙が全滅していた**。今は parse できたものを全部足す (`scenarios/*.yaml` の和 / campaign のモジュール / **`characters/*.yaml` の直読**= cast 宣言に依らないのでまだ cast に載せていないキャラも候補になる / package.yaml の注入分 = `globals.flags`・`player.*` は load 時注入ゆえ entry を読むだけでは落ちる)。整合性は診断の仕事で補完の仕事ではない。③**アセット欄の写像を追加** (`image`/`icon`→images、`bgm`/`sound`→audios) — 語彙は Phase C で収集済みだったのに frontend の写像が無く**一度も出ていなかった** (収集した語彙が死んでいた)。アセット ID は宣言を持たず死んだ参照 lint の射程外なので、ここは補完だけが予防になる。④**編集中のバッファも id 源** (`bufferIds` = 容器の直下だけを見る素朴な行走査。保存前に打った場所名・フラグ名が出る。深い入れ子と flow style は拾わない = **v2 で構文木の `harvestIds` へ置換済み**)。PoC: app `editor_docs` 2 本 + `editor_vocab` +2 本 + frontend 使い捨て 12 assertion (アセット欄は変更前が候補ゼロ = Red)。**✅Phase D (新規作成、同日 → GUI 目視中のユーザーFB で 4 カテゴリ + 削除へ拡張)**: `create_editor_file(category, stem, fork)` — scenario / character / memoria は stem 入力つき・campaign は**固定名 campaign.yaml** で不在時だけ即作成 (作っただけでは campaign-entry にならない = entry 差し替えは作者の判断)。判定順は保存と同一。stem は保守的文字集合 (英数 + `_` `-`)、character は加えファイル名 = **EntityId になる**ので**予約 id (`player`/`party`/`*`) を名指し拒否** (characters/player.yaml は主人公スロットと静かに衝突する — 実装時に見つけた穴)。雛形は**全カテゴリそのまま parse できる形** (character = `name: {stem}`〔null は String で parse エラー〕/ scenario = **validate も通る**最小盤面 / memoria = tags+text / campaign = modules 1 本)。**削除も v1 へ昇格** (`delete_editor_file` — spec 起草時の「削除は v2」をユーザー決定で上書き。**package.yaml だけ拒否**〔UI 非表示 + backend の二層〕、entry や campaign.yaml の削除は許す = 壊れることは層 2 inspect が報告する〔再構成の途中経過を機械が先回りで禁止しない〕。不可逆の確認 → 書庫由来はフォーク確認。リネームは v2 のまま)。重複は明示エラー・フォルダ不在は作成・返りは更新後一覧込み (ref_stock の 1 往復流儀)。UI は**固定カテゴリ順グループ + 各グループ内の「+ 新しい…」** (ユーザーFB) + 行 hover の ✕ → 作成後そのまま開く + 診断・語彙再取得。fork/未保存の確認は作成の**前**。PoC 2 本 (4 カテゴリの雛形 parse + character 予約 id / 削除の土台保護と fork 順序)。**GUI 目視で出た実装バグ 2 件を同日回収**: 起動 panic (EditorRoot の型エイリアスが GuestAssetRoot と TypeId 衝突 — manage() は TypeId キーでテストは builder を通らない = 実機の起動だけが落ちる検出の死角。newtype へ、failures #89) / エディタ本文がスクロールしない (CodeEditor ルート div に高さが無く height:100% の連鎖切れ — class fallthrough で h-full)。**✅追補 — メディア面 (2026-08-28 ユーザー要望)**: ファイルタブに**テキスト / メディアのラジオ**。起草時の「画像・音声アセットの管理はスコープ外」をユーザー決定で上書きし三つ足した — ①**参照一覧** (`images/` `audios/` 直下。**アセット ID = ファイル名そのもの**。**クリックはプレビュー** = 画像は全画面拡大・音声は再生 (2026-08-28 ユーザーFB で「ID をコピー」から差し替え — **補完が書く手元で ID を出す以上、一覧からコピーして貼る手順は二重**で、一覧の仕事は「どんな絵か・どんな音か」を見せること)。補完にも `images`/`audios` として載せた = **アセット ID は宣言を持たない**〔engine は不透明 ID を運ぶだけ〕ので死んだ参照 lint の射程外で、実名を出す補完が唯一の予防) ②**ドラッグ投入** (`put_editor_media`、raw body。**行き先は中身の嗅ぎ分けで決まる** = 拡張子の申告を信じない〔#78/#88 の規律〕。名前は charset へ潰し衝突は連番で**上書きしない** = 既存アセットを黙って替えると参照している盤面の見た目が変わる。画像は WebView で WebP 変換〔SVG は除く = ラスタ化すると取り柄が消える〕、音声は原本のまま) ③**クロップ** (`CropDialog` = canvas + 角ハンドル + 外側暗幕。**保存先を知らない部品**で切り抜いた bytes を `apply` で返すだけ — 2026-08-28 に**参照ストックが二人目の使い手**になった時点で一般化 (spec 27 の枠のクロップ = 「たくさんキャラのいる設定画集から今の場面に居ない人物を削る」。画集は 1 枚に複数人という spec 25 の形が前提なので場面ごとに全員が写り込む → 作り直さずに削れる経路が要る)。エディタ側は **同じ ID へ上書き** = 投入の連番回避と**逆向き**の判断で、名前が変わると参照している YAML を全部書き直す羽目になるから。サムネイル URL に `mediaRev` = 名前が同じまま中身が替わる failures #86 と同型)。削除もメディアに効く (UI の ✕ は一本の経路)。asset:// スコープは `open_editor` が編集ルートを別途許可 (**編集対象はプレイ中パッケージとは限らない**)。**✅追補 — VS Code 流の行内編集 + リネーム (2026-08-28 ユーザーFB)**: 「+ ボタン → 別の入力欄 + 作成ボタン」を撤去し、**作成も改名も一覧の中でその場に行が生まれる**形へ (契機 = カテゴリ見出しの hover 新規アイコン / 行のダブルクリック / エディタヘッダの F2・ダブルクリック。確定は Enter **または blur** = 打った名前を捨てない・取り消しは Esc・改名の初期選択は拡張子を除いた部分だけ・作成は拡張子つきで打っても通る)。**入力欄は一覧の行だけ**に保つ (ヘッダの F2 は `editorRenameRequest` で StatePanel へ橋渡し — 入力欄が二箇所にあると確定/取り消しの流儀が割れる)。**リネームも v1 へ** (`rename_editor_file`、起草時は v2): 同じフォルダの中だけ (移動はシナリオをキャラにする意味を持つ) / テキストは `.yaml` 必須 + stem charset + characters は予約 id 拒否 / **メディアは拡張子を変えられない** (中身は変わらないので、変えると名前が中身について嘘をつく) / `package.yaml` 不可 / 衝突は明示エラー・同名は no-op で成功。**参照は追随しない** — 壊れることは層 2 inspect が報告する (削除と同じ判断) が、**アセットだけは lint の射程外**なので提示層が title で告げる。PoC +1。**✅GUI 目視 Green (2026-08-28 ユーザー実測)** — A〜D 通しで確認し、実装バグ 6 件を同日回収 (起動 panic #89 / 本文がスクロールしない / CRLF で開いただけで ● / 閉じるボタン無反応 #90 / クロップの canvas 汚染 #91 / 入力欄が閉じない #92)。**このうち 5 件は実機でしか出ない形** (テストは builder を通らない・CodeMirror の LF 正規化・カスタムプロトコルの CORS・v-for 内 ref の型) = **エディタのような提示層は構造的にユーザーが検出器になる**という 2026-08-24 の観察 (`duplicated_discipline_and_test_shape_20260824`) の n+1。**✅v2 — 構文木 (2026-08-29、ユーザー指示で「次の一手」の 1 番を先に着地)**: v1 は「テキスト上の位置 ↔ YAML パス」の写像を持たず、**同じ欠落を三箇所で別々に近似**していた (①補完の文脈推定 = インデント遡り + 行内タグの正規表現 ②診断の行推定 = backend の親キー列の前方検索 ③バッファ id = 素朴な行走査)。三つとも flow style・多行の入れ子・引用キーで破れるので、木で一度に解いた。**着手前に決め手が二つ出た**: (a) **配線は既に gm_core に在った** — `editor_vocab` の手書き表は `lint.rs` の `child_of(ctx, key)` を**平らに潰した写し**で、平らゆえ文脈が落ち frontend が `docKind` で場当たりに補っていた (`to:` が場所かエンティティか・`start:` がモジュールか場所か)。(b) **Lezer は書きかけでも解ける** — 「入力途中は木にならない」という懸念を実測で棄却した (カーソル位置に短い番兵を差し込んで parse し直す形で 9 ケース全て解けた。**文字の無い位置には節点が生えない**のが唯一の壁で、番兵はそれだけを埋める)。**実装**: ①gm_core の `child_of` を match から `WIRING` データ表へ移し `wiring()` で名前へ射影 (**lint と補完が同じ 1 本の表を見る**)。app が持つ手書きは harness 型の 4 本 (`player`/`globals`/`player.stats`/`edges` = `manifest_lints` の入れ子と `Campaign` の形そのもの) だけになった ②ついでに `lint.rs` の `Tables` を名前つきフィールド 22 本 + 22 腕 match から**宣言リスト 1 本**へ畳んだ (動機 = doc の引き key である**型名**を外へ出す必要が生じたこと。キー集合と型名を別リストで持つと必ずずれる = 補完の説明が黙って消える形で) ③診断は行の近似を撤去し `EditorDiagnostic.path` に YAML パスを載せるだけにした — 位置へ解くのは**木を持つ frontend** (`rangeOfPath`) で、返るのは行でなく**キーそのものの範囲**。解けなければ位置なし (行を偽らない規律は不変)。**直ったこと (測ったもの)**: 兄弟タグが**行を跨いで**効く (v1 は `kind:` が同じ行に無いと Gate 全バリアントの和集合へ落ちていた — v1 のコードで実測して確認) が**列の境界は跨がない** (`of:` の新要素が親 gate の `kind: all` を継がない) / 同じキーが親で割れる / **作者の付けた名前の段** (`locations:` の直下) では型のキーを出さない (v1 は祖父キーを引く近似で名前の位置にも `Location` のキーを出していた) / 「別の行から始まる空の値」は構文では**キーか値か決まらない**ので配線で判別する (名前の段の内側 or その欄が構造を持つならキー / 素の文字列欄 `description:` は値のまま)。**検証の場を作った (ユーザー決定)**: frontend に **vitest** を入れた (`npm test`)。位置↔パスと文脈の巻き戻しは DOM に依らない純粋関数で、**提示層で唯一テストできる部分** — 「提示層は構造的にユーザーが検出器になる」(2026-08-28 の観察) に対して、書ける範囲での応答。`editorPath.test.ts` 13 本 + `editorCompletion.test.ts` 14 本。ワイヤの形 (Rust の欄名 ↔ TS の interface) は**型検査が継ぎ目を跨がない**ので Rust 側の `wire_shape_matches_the_frontend_interface` が留める。**✅ユーザー目視 (2026-08-29) で 1 件を追加**: 「多行 gate の narrow が効いていないのでは」の報告に対し、TS 側のテストが**手で組んだ語彙**しか見ていない (= バリアント名の綴り・文脈名の一致という継ぎ目はどちらのテストも測っていない) と気づき、backend の実語彙を `editorVocabulary.fixture.json` へ書き出して frontend のテストが**実データで**合成を測る形にした (`vocabulary_fixture_is_fresh` が古くなれば落ちる・`UPDATE_VOCAB_FIXTURE=1` で更新)。**結果は narrow が効いていた** — 報告のスクリーンショットは `kind:` をまだ書いていない盤面で、出ていた列は Gate の和集合そのもの (= 絞る材料が無いときの正しい出力) だった。ただし**指摘は別の形で正しかった**: 和集合のとき、**最初に書かねばならない `kind` / `op` が名前順の真ん中に埋もれる** (これを書くまで他の欄は決まらないのに)。タグ未確定の文脈でだけ boost で先頭へ押し上げた。**✅エディタのフッタ (2026-08-29 ユーザー要望)**: 「YAML はインデントが意味を持つので**カーソルの列**が見えたほうがいい」→ 本文の下に 行/列・全体行数・文字数・挿入か上書きか。`CodeEditor.vue` は**包みの div が高さを持ち本文とフッタを縦に並べる**形へ (CodeMirror 側は `height:100%`。従来の固定高の呼び出し 2 箇所は `status` 既定 false で見た目不変)。**挿入/上書きは表示するだけでなく実装した** — CodeMirror は挿入しか持たないので `inputHandler` で作り、Insert キーで切替。**切替を受けるのはフッタを出しているときだけ** (モードが見えない場所で打鍵の意味だけ変わるのは事故) + 上書き中はカーソルを太くする (語だけだと本文を見ている目が気づけない)。**純粋な芯は `editorTyping.ts` へ出して固定** — 消す幅は**書記素クラスタ単位** (`findClusterBreak`) で、行末では消さない (次の行を食わない)。サロゲートペアや結合文字を半分だけ消すのは**エラーも警告も出ないまま本文が化ける**類の壊れ方なので、テストの在る場所に置いた。文字数は `.length` (UTF-16 符号単位) でなく**コードポイント数** (絵文字を 2 と数えない)。frontend 43 green。**チャンク境界は不動** — `@lezer/yaml` は `@codemirror/lang-yaml` が既に積んでいるので追加依存ゼロで、main は変更前と同じ 368.40 kB (`git stash` して実測)。**v2 に残したもの**: 値カテゴリの写像 (`(文脈, 欄名) → id カテゴリ`) は**手で持ったまま** — 「その欄が何を指すか」は意味の話で型からは導けない (Rust 側でも `LocationId` は `String` の別名で serde には残らない)。配線と違い、ここに単一の真実源は無い。**残る次の一手** = ウィザード / **AI にパッケージを直させる** (llm_client と harness の prompt は `GameSession` 前提のターンループ用で、プレイの外で動く編集モードからは直接使えない。挿絵のプロンプト書きと同型で harness に generate 経路を足す形を推す)。設計は `specs/28_package_editor.md`、契約は data_contract `package_editor` 節。
- ✅ **製品名を「Lorekeel」へ改名 (2026-08-28, ユーザー決定)**: **同じ分野に同名の先行者が居た** — [kataribe.jp](https://kataribe.jp) は「語り部」という汎用 TRPG ルールとその共創コミュニティ (現役・参加者クレジット約 23 名)。無関係な業種の同名ではなく**同分野の完全一致**で、TRPG の界隈で「語り部」と言ったときどちらを指すか割れる。商標登録の有無は未確認 (「語り部」は普通名詞ゆえ識別力は弱く、法的に止められる筋は薄い) が、**効くのは法律より混同**。冠名「Outcasts」は 2026-08-28 に検索で埋もれる対策として付けたが、**人は短く「Kataribe」と呼ぶ**ので冠だけでは吸われ続ける。**受領者が少ない今が最も安い時点**という判断で改名した。**識別子も替えた** (ユーザー決定 = `jp.kataribe.app` が残るとフォルダ名・cask の zap・クラッシュレポートに相手の名前が居座る)。**替えたもの**: `productName`/ウィンドウ表示 (`Outcasts Lorekeel`)/`index.html` の title/i18n の brand・windowTitle/bundle identifier `jp.lorekeel.app`/Cargo の crate 名 (`lorekeel-desktop`・lib `lorekeel_desktop_lib`)/`app/package.json`/CI の成果物名/README ×3 と同梱画像 (`images/lorekeel*.png`)/env 変数の接頭辞 (`LOREKEEL_*`)/出所メタのファイル名 (`.lorekeel_source.json`)。**据え置いたもの**: `localStorage` の `kataribe.` 接頭辞 (**替えると `packagePaths` 等ユーザーの設定が全部初期値に戻る** — 見えない文字列に改名コストを払う理由がなく、第三者の目にも触れない)/リポジトリ名と書庫サブドメイン (外部の実体が要るのでユーザー作業)/knock の GHCR イメージ名 (稼働中の compose を壊す)。**後方互換は 2 箇所で読む側だけ**: env は `LOREKEEL_x` → 無ければ旧 `KATARIBE_x` (`harness::env_var`。手元の `.env` に旧名が書かれているので、認識をやめると設定してあるのに効かない状態へ静かに落ちる)/出所メタは新旧とも読み、書くのは新のみ・フォークでは両方消す・`tree_hash` の除外にも両方残す (**旧名を除外から外すと既存の取得物が一斉に「編集されています」に化ける**)。**移行と告知** (`app/src-tauri/src/rename.rs`): **移行先は 2 つある**。①`app_data`: identifier が変わると `app_data_dir` が別の場所を指すので、**旧フォルダが在り新側が空のときだけ**再帰コピー (卓の一時キャッシュ `packages/table` は中継から取り直せるので運ばない)。**コピーであって移動ではない** (問題が出れば旧版へ戻れる)・失敗しても起動は止めず告知に理由と旧フォルダのパスを載せる。告知は**旧インストールの痕跡が在るときだけ**一度きり (通知済みフラグだけで判定すると新規ユーザーにも「改名しました」が出て、知らない旧名を知らせることになる)。②**WebView の保存領域** (`migrate_webview_storage`、2026-08-28 実機で踏んで追加): WebView2 のユーザーデータは `%LOCALAPPDATA%/{identifier}/EBWebView` と **identifier 別**なので、改名すると**空のプロファイルが作られ localStorage が丸ごと失われる** — 登録モデル一覧・`packagePaths`(登録したパッケージ)・画像生成設定・テーマ、つまりユーザーから見た「設定」そのもの。**`.env` は運べていたので接続だけ生き、症状が「AI モデルの設定が消えた」に見えた** (実機の報告)。**呼ぶのは `main` の Builder より前** — WebView 起動後は LevelDB を掴んでおり上書きするとプロファイルを壊す。条件は「新側にまだ Local Storage が無い」(一度でも起動していれば触らない)。**保存場所は OS ごとに違うので、実機で確認できた OS だけを扱う**: Windows = `%LOCALAPPDATA%/{id}/EBWebView/Default/Local Storage` / **macOS = `~/Library/WebKit/{id}` をコンテナごと** (2026-08-28 に MacInCloud の実機で確定) / Linux = 未確認ゆえ何もしない。**macOS は「場所」と「コピーで戻るか」を分けて実測した** — 場所は `find ~/Library` で確定し、**コンテナを退避 → 削除 → 起動 (設定が初期化されるのを対照群として確認) → 書き戻して起動 (設定が復元)** の 3 手で、丸ごとコピーが効くことを新しいビルドを待たずに実証した (内部構造 = WebsiteData の割り方は当てにいかず、丸ごと運ぶ形にした)。**✅実際の経路でも実測 (v0.5.18)**: 手で確かめたのは「コピーすれば戻る」だけで「新版が旧プロファイルを見つけて自分でコピーする」は別の主張なので、v0.5.18 を出して実機で測った — `mv` で旧プロファイルだけが在る状態を作り、新版を起動すると `~/Library/WebKit/jp.lorekeel.app` が生まれ設定 (英語表示・フォント特大) が復元された。**一つの機構でも「部品が効くこと」と「呼び出し側が呼ぶこと」は別々に測る**。副産物: 通知済みフラグは localStorage に在るのでプロファイルを消すとお知らせが再び出る (プロファイル単位 = 実際の移行経路では旧プロファイルにフラグが無いので一度だけ出る、が正しい挙動)。identifier は `tauri.conf.json` と二重に持つ (AppHandle の前に走るので Tauri の path API が使えない) ので、綴りの一致を PoC で固定した (ずれると移行が黙って空振りし、設定が初期値に戻るだけなので誰も気づかない)。PoC 4 本 (移行の一度きり性と旧フォルダの残存・新規は無告知 / 旧名メタの認識と tree_hash 不動 / identifier と設定ファイルの一致)。**✅周辺も同日に着地 (2026-08-28)**: GitHub リポジトリ改名 (`betyourluck/Lorekeel`) / Homebrew は新 cask `lorekeel` + **`tap_migrations.json`** で旧 `kataribe` を移行 (旧 cask は削除。`brew upgrade` は tap の rename 移行機構で追随する — 当初「追随しない」と書いたのは誤りで一次情報 (Homebrew/brew #15176・#16648) で訂正した) / 書庫は **`lorekeel.outcasts.jp` を追加し旧ホストは退役させない** (当日は `DEFAULT_SITE_URL` を据え置き — 先に替えると `meta_matches_site` の完全一致が外れ、既存パッケージの更新照会が**沈黙で**止まる。移す順序は「旧を配り続ける → `meta_matches_site` に旧ホストの別名を通す → 既定を替える」と凍結した。**✅2026-09-01 に後半 2 段を実施 (ユーザー指示「新規インストールの人は lorekeel にして欲しい」)**: `ARCHIVE_ALIASES`(2 ホスト) を **backend (更新照会) と frontend (サイトタブの『取得済み』判定) の両方**へ入れ、同時に既定を `lorekeel.outcasts.jp` へ。**既定は一度も設定していない人にしか効かない**ので既存ユーザーは無影響 (`kataribe.siteUrl` は localStorage にあり設定ミラーの射程内)。**別名を同時に入れないと壊れるのはその『一度も設定していない人』** — 新既定に載った瞬間に旧ホスト由来の出所メタと食い違い、更新照会が沈黙で止まるうえ取得済み判定が外れて**同じ配布物を `_2` で二重に据える**。畳むのは**完全一致の表引きで置換ではない** (`kataribe.outcasts.jp.evil.example` は畳まれず従来どおり弾かれる)。**SSRF 姿勢は不変** — 別名が効くのは「照会するかどうか」の判断だけで、接続先は常にユーザー設定の側 (`check_package_updates`/`update_site_package` はどちらも `base` へ出る)。表が 2 箇所に割れるのは deployment の事実で型から導けないため = 単一真実源にできない → 両方の doc comment に相互参照を書いた。着手前に**両ホストが同一の `/api/packages` を返すことを実測**した (空の書庫を新規ユーザーに見せない確認)。台帳追従: specs/05・17 / data_contract `package_updates.archive_aliases`。PoC は既存 `only_meta_from_the_configured_site_is_queried` を拡張) / outcast 側の文言とサイトのバイナリ差し替え。**据え置きは knock の GHCR イメージ名のみ** (稼働中の compose を壊す)。**共有ワイヤ `kataribe_version` も据え置き** — 書庫の一覧 JSON をクライアントが直接読む欄で、サーバが先に替えると**バッジだけが黙って消える** (serde は欠けた `Option` を `None` で受けるので decode は通る = 実測で確認)。動かす順序は「クライアントを両受けにする → サーバが併記 → 旧を落とす」。**←取りこぼし 1 件を 2026-08-31 に回収**: build.yml の `releaseName` が旧名のままで、v0.5.19 の draft が「Kataribe v0.5.19」で作られて発覚 (成果物名は替えたのに release 名の欄が漏れた = 同じファイル内の別の欄)。draft は gh で修正・次のタグから CI が正しい名前で作る。
- ⬜ UI 磨き込み（複数キャラ表示・narration ストリーミング）。〔神経 embedding は語彙乖離 paraphrase が要る時のみ＝現設計では不要。配布アイコン差し替えは ✅2026-07-02 済（app bullet 参照）／会話ログ保存は ✅2026-07-09 済（上記）〕
- ✅ **人狼盤面（グノーシア型）/ spec 06 (2026-07-03 起草 → rev2 査読反映 → 2026-07-04 Phase A〜E 完了 = spec Done)**: **✅Phase A `role_assignment`**: `{key, pool: {役職→人数}, among: [player, …]}` を `initial_state(seed)` が **seed ^ "ROLE_RNG" の専用ストリーム**で Fisher–Yates shuffle して attributes に配る（本流 `state.rng` 非消費＝配役の有無でダイス列不変、同 seed 同配役＝再現可能、LLM 関与不可）。bookkeeping stat 自動生成（各自 `生存`=1・player に `生存{役職}数`、更新は Phase C ResolveVote の専権）。validate 3 種（人数不整合/幻キャラ/重複配布）。PoC: `role_assignment_deals_roles_deterministically_without_touching_main_rng` / `validate_rejects_role_assignment_mismatches`（Red→Green）。**✅Phase B `secret_attributes`**: 宛先別秘匿——GM=`state_brief` が**〔秘匿〕注記**付きで全員分（ゲームを回すのに必要）/ プレイヤー UI=`state_view` が **NPC 分を DTO 段階で落とす**（本人分のみ、プロフィールカードも自動秘匿）/ 登場人物どうし=不可（GM_SYSTEM が「互いに知らない・地の文で明かすな・役職能力の結果は当人だけの知識」を刷り込み）。validate `SecretAttributeUndeclared`（キーは initial_attributes/CharacterDef.attributes/role_assignment.key で宣言必須）。PoC: `secret_attributes_parse_and_validate_declaration` / `state_brief_marks_secret_attributes_and_gm_system_grounds_secrecy`。**✅Phase C `CastVote`+`ResolveVote`**: `vote_rules: [{when: Gate, voter_attribute: {key,value}?}]`＝投票権の宣言（**デフォルト拒否**・幻キーは `VoteRuleAttributeUndeclared`・パラメトリック主語は Gate 本体に入れない）。`CastVote{voter,target}` は LLM 提案可（検証=既知+両者生存 `EntityNotAlive`+rules 合致 `VoteNotAllowed`）、票の器 `GameState.votes: BTreeMap<voter,target>`（**一人一票を型で保証**・再投票上書き・セーブ対象・transition 非持ち越し）。`ResolveVote`＝authored 専権効果 op 第5例（AUTHORED_ONLY_OPS 追加＝schema 除外+`VoteResolveNotAllowed` 却下）: 開票→同数は `seed^"VOTE_RNG"^turn` 専用ストリームで抽選（決定論・本流非消費）→死亡＝`生存`=0（正本）+presence false（投影）→bookkeeping 再計算（`生存{役職}数`/`生存者数`/**`{役職}優位`=2×生存{役職}数−生存者数**＝パリティ勝利を単体比較 Gate で書く差分 stat、人狼勝利=`stat_at_least 人狼優位 0`）→票リセット。PoC 4本（夜に村人の票を弾く/開票の原子適用一式/同数抽選の同 seed 同結果/LLM 開票却下）。**✅Phase D content+surfacing**: ①投票の prompt surfacing——`scenario_brief` に「## 投票」（vote_rules を平易な日本語で列挙）+ GM_SYSTEM「投票の局面では生存 NPC 全員分の票を cast_vote で並べよ（誰に入れるかは性格・疑念・秘匿役職で GM が決める＝推理劇の演出）／開票・処刑の帰結を先取りして語るな」（PoC: `scenario_brief_surfaces_vote_rules_and_gm_system_grounds_voting`）。②ドッグフード盤面 `packages/gnosia_village/`「霧の村」——6人（player+ミラ/ゲン/サヨ/トキオ/ユウレン）、人狼2/占い師1/村人3、フェーズ進行は**既存プリミティブのみ**（`議論T/投票T/夜T` タイマー + repeatable トリガー4本 opening/vote_opens/execution/dawn で議論3T→投票→処刑→夜の狩り→夜明けを無限周回）、タイマー・生存・カウンタ・優位は hidden_stats で一括秘匿（カウンタは死者の役職を漏らす）、goal 3本（あなたの死/村の勝利/人狼の勝利）。動線は使い捨てテスト（削除済）で load_package→配役→フルサイクル2周→`village_win` 到達を一発 green で実証。**✅Phase E 実測 (2026-07-04, gemini-flash-latest=中位モデル, seed 42, 2 周)**: CLI に **`play --package <dir>` モード新設**（`load_package`＝player/globals/world 注入込み＝GUI と同経路。従来 CLI は素の scenario 読みで package 注入が抜けていた）で霧の村を台本 14 行で通しプレイ。**1 周目 (修正前 14T)**: 誤占い 0/2（占い結果は secret と完全一致。「魔法はない」世界観に合わせ占いを洞察として語り直す適応も）／死者の発言 0（presence 接地が完全機能・処刑/襲撃の 3 名は以後不登場）／昼投票 2 回は生存者全員分の cast_vote が演技と一致／時期尚早の投票はエンジン却下→self-repair 1 回で収束。**穴 2 つ**: 初夜の狩り不発（権利の提示だけでは票が出ない、`#32`）＋襲撃シーンの実行者開示「仮面を脱ぎ捨てたミラ」（属性言及の禁止は行動描写を縛らない、`#33`）。**修正 (prompt 層のみ・engine 不変)**: 投票規律に義務「絞られた局面 (夜の狩り等) でも投票できる者が生きているなら必ず cast_vote／出さなければ何も起きない」＋秘匿規律に「隠密行動は実行者を伏せ、結果だけ (翌朝の発見・痕跡) を描け」。**2 周目 (修正後 10T)**: 漏洩 0（襲撃は「別の獣が動き出す気配」と実行者を伏せた描写に）・誤占い 0/1・初夜の狩り発火 (tokio→gen)・却下 0 で **village_win 完走**。判断: 中位モデルで完走ゆえ**役職公開バリアントは当面不要**（n=2 の暫定、ローカル弱モデルは配布テストで再計測）。PoC: 既存 2 テスト（vote 接地/秘匿接地）に義務/隠密の assert 追加（Red→Green）。**#37 (2026-07-05, ユーザー自作 vampire 盤面で夜の狩り不発が再発)**: 静的義務文のみの夜狩り成功率は実測 1/3 → 接地の第三層として `state_brief` が**条件が真の vote_rule を毎ターン動的 surface**（「⚠ いま投票が開いている。票を出せる者: 役職=X の生存者 → 名前 (id) 列挙。必ず並べよ」— 生存 #35 分岐+voter_attribute でエンジンが絞る、該当者ゼロ/条件偽なら節なし）+ GM_SYSTEM「その行が合図」。再実測で lone wolf の夜襲が発火し player 死亡=you_died 到達。**一般化: 接地の強度は 静的規則 < 一般義務 < 現在形の事実+固有名**（条件評価は LLM でなくエンジンが行い、真になったターンに「いま・誰が」を突きつける）。 秘匿役職とランダム割り当て。設計は `specs/06_hidden_roles.md`（Done）: 新機構3つ＝①`role_assignment`（**seed 派生の専用ストリーム `role_rng`** で shuffle・本流ダイス列非干渉、役職 attributes + bookkeeping stat（生存/生存人狼数/村人優位=差分）を自動生成、player 配布込みで確定）②`secret_attributes`（宛先別秘匿: **GM=全員・秘匿注記 / player UI=本人のみ / NPC 間=不可（演じ分け）**、hidden_* とは別軸）③`CastVote`(LLM 提案可・票の意図のみ) + `ResolveVote`(authored トリガー専権の効果 op 第5例、開票・同数 seeded 抽選・死亡=生存 stat（正本）+presence（表示投影）+カウンタ/差分 stat 再計算・票リセットを**一箇所で原子適用**。set_presence 専権は「authored または ResolveVote」に拡張・LLM 直接提案不可は不変)。勝敗 Gate は式を評価せず差分 stat に畳む（stat_compare 新 Gate は導入しない）。フェーズ進行は既存プリミティブ。**核心的未知 = Phase E 実測 3 点**: 役職漏洩率 / 誤占い率（占いは op 化せず語り処理＝#23 非対称を自覚的に抱える）/ 死者の発言（presence 接地が検出基準）。査読で決定済み↔未決の衝突 7 件を解消（rev2）。
- 🟡 **複数同時プレイ / spec 23 (2026-07-23 ユーザー発案→同日起草→査読 12 件反映 rev2→Phase 0 凍結→A→B→C→package_relay→**Phase D 実装 + デプロイ完了 + 2 台実機で運用整備**。**残 = Phase E (3 人 live 実測) のみ**)**: 2〜3 人が同じ盤面を同時にプレイ（ターンごとに各自が制限時間内に入力→締切で束ねて 1 回の LLM 呼び出し）+ WebRTC 音声で卓の空気。**決定 4 点（設計対話で確定）**: ①**ホスト権威** — ホストだけが完全な正本 (`GameSession`) を持ち、他はフィルタ済み view DTO を受け取る。レプリケーションは捨てる（正本 2 つ = split-brain = 「矛盾する GM」。友人同士ならホストを信じる）。既存の「backend が正本・frontend は DTO を描くだけ」がそのまま伸びる = **リモート参加者とは Tauri IPC の代わりにネット越しに同じ DTO を受け取る frontend**。②**AI 提供者はホスト**（持ち回り案は棄却 — prompt の state_brief は〔秘匿〕込みで全員分を載せるので AI を回す人は GM の秘密を全部見る。ホスト固定なら秘密を知る人間がちょうど 1 人。コスト公平性は「今日はお前がホスト」のセッション単位持ち回りで社会的に解決 = セーブ受け渡しで成立、機構化しない）。③**多 PC は作らない** — プレイヤー 2/3 は仲間 entity (`CharacterDef`) を操作（per-entity 数値/属性は既にある・location 単一 = パーティ一体移動）。④入力窓 + 締切 + 発話者名つき合成 prompt（3 人分を 1 往復に束ねる = spec 09 の束ね経済）。**唯一の本質的改修 = `state_view(viewer)` の宛先別化**（spec 06 の宛先別秘匿の「本人」を引数化・ホスト側 DTO 段階でフィルタ）。ノックサーバー（さくら VPS・WebSocket シグナリングのみ・ゲーム通信と音声は P2P/DTLS）。アセットはゲストも同じパッケージ所持前提（ID をワイヤに載せ各自 resolve・版ズレは spec 17 content hash 照合）。開帳はセッション状態へ昇格しホストが順序づけ（全員同時に出目を見る）。ホスト断はセーブ引き継ぎで再開（rng cursor まで完全 = 同じ運命の続き）。v1 スコープ外 = TURN リレー/観戦者/中途参加/真の多 PC/人狼型秘匿盤面（ホスト全知ゆえ「秘匿盤面はホスト=非プレイヤー GM 役」の約束で回避）。Phase 0 契約凍結→A transport seam (`GameTransport`、invoke は game.ts 29 箇所に集中)→B 多人数ターンループ（fake transport でネット無し検証)→C WebRTC 結線→D 音声+開帳共有→E 3 人 live 実測（核心的未知 = GM が行動を各人に正しく帰属させるか）。**rev2 査読 12 件反映 (同日)**: `GameTransport` は request+onEvent の**双方向**（synopsis-compacting 等の emit push は Req/Res で表現できない）／卓の生死はホスト frontend (WebView) が握る旨を仕様化（wire 形式を backend 非依存に凍結し将来の webrtc-rs 移行パスを残す）／**アセット ID 化は Phase A で Local 含め DTO 一本化**（Remote だけ ID にすると DTO が二形態に分裂）／**TURN (coturn) をスコープ外→v1 必須へ昇格**（日本のモバイル CGNAT=対称 NAT は TURN 必中、direct 成功 75-80% の実測。turns:443?transport=tcp + 一時クレデンシャル）／部屋 TTL+再 knock で再接続を v1 に含む（中途参加とは区別）／仲間の拾得は per-entity 化せず**「主人公が拾い give_item で渡す」2 op 束ね**（spec 09 逐次射影が同一ターン受理を保証=エンジン改修ゼロ維持、査読の二択への逆提案）／締切三系統（全員提出/タイマー/ホスト強制）+ 未提出は「黙っている」で prompt に残す（#37 系接地+AFK 透明性）／`RevealState` 昇格は Phase B へ前倒し／部屋コード base62 22 桁 + ノックサーバーが信頼点（SDP 改竄=MITM 可）の明記 + `sig?` 予約／卓言語=ホスト言語／セーブ形式不変で resume に `path_override`／**`participants: [{peer_id, entity_id, display_name}]`** を Phase 0 契約に（帰属マッピングの欠落を査読が指摘）。WebRTC は frontend (RTCPeerConnection) で確定（WebView2=Chromium で動く実例あり・webrtc-rs は音声自前で重い）。**✅Phase 0 凍結 (同日)**: data_contract `Multiplayer` 節 11 項（protocol_version=1・join 時交換で不一致拒否 / participants / room_code base62 22 桁+TTL 10 分 / messages（sig? 予約込み）/ package_match / input_window 三系統+「黙っている」 / transport 64KB+coturn 一時クレデンシャル / asset_wire=ID 一本化（ID 形式 = scenario YAML の image/bgm/sound/icon 欄の値そのもの・kind は欄が含意 = 独自語彙を発明しない）/ **item_idiom=拾得・譲渡・消費とも主人公経由 2 手**（消費は give_item(仲間→主人公)→remove_item の逆向き、査読側の補強で追補）/ language / resume_handoff=path_override）。**✅Phase A (同日実装→ユーザー目視回帰 Green→main へ ff-merge)**: `GameTransport`（request+onEvent 双方向・購読解除つき、`app/src/transport.ts`）+ `LocalTransport`（invoke/listen の薄い包み=挙動不変）。seam を通るのは play_turn / resolve_dice_decision / play_contest_round / facts_* + push 3 イベント（App.vue の listen 直呼びを onEvent へ移設）。**全 DTO のアセット欄を絶対パス→ID へ一本化**（背景/BGM/顔アイコン/ビート CG・SE/結末 SE/マップ CG。app backend の resolve_asset 呼び 9 箇所を pass-through 化・map_view から root 引数を撤去）。解決は新 command `resolve_asset_path`（seam の外=各クライアントが自分のコピーで解決）→ frontend `prefetchAssets`（DTO 到着時に一括解決→以後は同期 `assetUrl(kind,id)`＝revealNext 等の同期経路を async 化しない。キャッシュは applyGameView でクリア=ID はパッケージ内でのみ一意）。旧セーブ無傷（アセットはセーブに入らず毎回 scenario から導出）。app backend 22 green・clippy clean・vue-tsc/vite build green。**✅Phase B (同日実装 = 多人数ターンループをネット無しで固定。GUI からはまだ届かない logic-only、卓 UI は Phase C の join フローと同時)**: ①`state_view(..., viewer)` 宛先別化 — spec 06 の「本人」を引数化、secret 属性は viewer 本人分だけ DTO に通す（フィルタはネットに乗る前・hidden は誰にも出ない）。ホスト画面の viewer は `viewer_entity()`（host_peer→entity。**ホストが仲間を操作する卓ではホスト画面も本人の秘密だけ**＝正本とセーブでは全知だが画面はプレイヤー視界）。②`GameSession.participants` + `set_participants`（門番 `validate_participants`＝空/peer 重複/entity 二重操作/幻 entity/主人公スロット≠1/ホスト不在を拒否。**セーブ非対象**＝卓は揮発、再開時は join で張り直す）+ ゲスト fan-out 原料 `state_view_for(peer_id)`（TurnView で差があるのは state だけ）。③入力窓 — `submit_turn_input`（上書き可・空は拒否）/`turn_input_status`（宣言順 submitted/waiting）/`play_party_turn`（合成→do_play_turn→**受理で窓クリア・却下は提出物を残す**。全員未提出の締切は拒否）。締切三系統の判断はホスト frontend の専権（backend は「いま締める」の提出物で合成）。④合成 `harness::compose_party_action`＝「名前 (entity): 行動」行 + 未提出者は `SILENT_ACTION`「（黙って様子を見ている）」で必ず載せる（#37 系接地 + AFK 透明性）。play_turn は `do_play_turn` へ切り出し単騎と同一実体。⑤GM_SYSTEM 多人数接地 `prompt::party_note`（party≥2 のみ、system ブロック末尾）＝参加者列挙 + 帰属規律（ops は書いた本人の操作 entity へ）+ 「黙っている≠不在」+ item idiom（主人公経由 2 手）。`run_turn` に `party` 引数（CLI は `&[]`）。**単騎の prompt は 1 バイトも変わらない**（PoC で byte 一致固定＝キャッシュ無風）。⑥`RevealState` 昇格 — `GameSession.reveal: RevealView{revealed,total}`、伏せ直し 3 点（受理ターン/プッシュ 1/対決ラウンド 1）、`reveal_next`（飽和インクリメント＝先着勝ちは lock 順・過剰クリック無害）/`reveal_all`。frontend は演出ローカル即時のままカウンタを transport 通知（単騎でも同経路・演出オフ 3 経路は reveal_all で追認）＝Phase D は reveal_order 配信にするだけ。PoC 7 本 Red→Green（harness 3＝合成の黙っている/party_note 接地+単騎 byte 不変/**2 クライアント統合**＝2 人入力(1 人未提出)→合成→run_turn 1 回→仲間 entity 帰属 op 一発受理・正本は仲間側だけ動く ＋ app 4＝宛先別 secret/participants 門番/開帳カウンタ単調飽和/入力窓宣言順）。workspace 312 green・app backend 26 green・clippy clean・build green。**✅Phase C (同日実装完了・live 検証は Phase E)**: ①契約改訂 (ユーザー決定) = knock 置き場決着 (**コードは Kataribe リポ `knock/` public** — プロトコルは契約+クライアントで既に公開・エンドポイントは CT ログで公開・守りはソース非依存の運用防御。信頼点の自前ホスト可がユーザーの安全装置。デプロイ = outcast compose + GHCR、**VPS 借り増し不要**) + turns:443→**5349 主**へ (443 は Caddy が占有・TURN 非 HTTP で同居不可・さくら VPS 追加 IP 不可) + **v1 実配備 ICE は turn:3478 udp/tcp** (turns は coturn への証明書配管後。TURN 認証は HMAC・中身は元々 DTLS-SRTP = 平文でも秘匿不変)。契約 `knock_hosting` 新設 + 運用防御 3 点凍結 (TTL 分単位/部屋作成 IP rate limit/coturn クォータ)。②ノックサーバー `knock/` (workspace 外・gm_core 非依存): WS シグナリング create/join/signal/leave・部屋 base62 22 桁・TTL 10 分+接続イベント延長・再 knock (identity 維持+peer_rejoined)・protocol_version 門番・TURN REST (HMAC-SHA1、RFC 2202 ベクタ接地)・IP rate limit・XFF 右端。**PoC 9 本 green・clippy clean** (テストは knock/ で cargo test = workspace 外)。Dockerfile + `.github/workflows/knock-image.yml` (ghcr.io/betyourluck/kataribe-knock、テスト green が焼く門番。**✅2026-07-23 に初回ビルド成功・public 到達を実測** — 匿名トークンで manifest 200/存在しない名前は 403 で判別、**visibility の手動変更は不要だった**＝GITHUB_TOKEN で public リポから push したパッケージはリポジトリの可視性を継承する。VPS 側の docker login も要らない。タグは `latest` + commit sha)。outcast 側 compose (knock+coturn host network)+Caddyfile (knock.{DOMAIN})+.env.prod.example (TURN_SECRET) は**編集のみ・コミット/デプロイはユーザー管理**。DNS turn.{DOMAIN} A レコード + さくらパケットフィルタ 3478+49160-49200/udp が要る。③配送層 `app/src/rtc.ts`: Signaling/HostTable (game_request を **whitelist+peer 束縛**で backend へ — submit_turn_input/turn_input_status/reveal_next/reveal_all/state_view_for のみ・peer_id 強制上書き=他人の秘密 view 不可。push 中継+reveal_order/input_status/timer_sync/party_turn 配布)/GuestLink (GameTransport 実装。hello で role=host を配送先確定・content_hash 照合・reconnect)。offer は入る側。64KB 超過警告 (未決2 計測点)。④卓 UI+store: TableDialog (TitleBar Users アイコン・参加中 ember) — ホスト=卓を開く→席 entity 割り当て→set_participants→table_start 配布→入力窓 (三系統締切完備: 自動 all_submitted/タイマー+timer_sync/手動)／ゲスト=join (begin_guest_session=アセット root 登録・hash 警告・再接続)。store は `MultiState`+`ingestTurn` 抽出 (単騎とゲスト受信が同一描画実体)+`submitPartyInput` (卓中の playTurn は提出へ委譲)+`applyRevealOrder` (revealApplied 追従=エコー二重開帳なし)。transport は SwitchableTransport (swap 越しに購読生存)+tableHooks (循環なしの逆呼び出し)。backend 追加 command 4: `current_game_view(peer_id?)` (途中 join 初期同期)/`begin_guest_session`+GuestAssetRoot (resolve_asset_path のゲスト root フォールバック)/`resume_from_file` (契約 resume_handoff=path_override)/`package_content_hash`。v1 制限=決断/対決操作はホストのみ。**PASS (2026-07-23 ユーザーFB)**: 「意図して何もしない」を未提出と**別物**として持つ (`harness::PASS_ACTION`、`submit_turn_input(pass: true)` で engine 側の定型文へ寄せ提示層が語りの文言を持たない)。未提出=「分からない」(離席・考え中) / PASS=「決めた」で、**PASS は提出済みに数える**ので全員提出の自動締切が発火する。**動機は表示でなく機構** — 遅延イベント (`turns_since`) は受理ターンでしか進まないので、全員 PASS で番を送れないと「N ターン待つ」が盤面から書けない。全員が動かなかった番には `NOBODY_ACTED_NOTE` を合成行に添え、GM に世界の側を動かすよう促しつつ**プレイヤーが取っていない行動の捏造を禁じる**(そのままだと「何も起きない」で終わらせがち)。全員未提出のまま締めるのは従来どおり拒否 = 番を送るには PASS という明示が要る。**✅package_relay (2026-07-23 ユーザー決定→同日サーバ・クライアント両側実装完了)**: パッケージ入手は手動選択→**ホストの一時アップロード中継**へ。正はホストの実ファイル=改変版で遊ぶ前提。room_code=bearer capability・削除は卓開始時+TTL sweep 二層・受信検証は site.rs 再利用・hash キャッシュで再 DL なし。契約 `package_relay`。サーバ = outcast Spec 30 / C-019 (`PUT`→`{sha256,size,expires_at}` / `GET`→zip+`ETag` / `DELETE`→204 冪等、統合テスト 10 本 green)。クライアント = `app/src-tauri/src/relay.rs` (純関数部) + command 3 つ + `TableHello.relay_sha256` + 卓 UI 簡素化 (ゲストは**コードと名前だけで join**・手動選択は中継を使わない卓の fallback として残す)。**要点 4 つ**: ①**zip の決定論がキャッシュ鍵の前提** (Wrapped + 相対パス辞書順 + zip crate 既定の固定タイムスタンプ = 同じ内容なら同じバイト列 → 「翌週の同卓は再 DL なし」)。②**遠隔由来の sha256 はキャッシュのパス成分になる**ので小文字 hex 64 の形式検査を通してから使う (`..` を差し込ませない)。展開先は `app_data/packages/table/{sha256}` = 書庫の取得物と置き場ごと分離 (卓限りの一時物をパッケージ一覧に混ぜない)。③**ゲストの卓メッセージ処理を直列の鎖に**した — hello はパッケージ取得 (DL・展開) を待つので素朴な `void` 投げだと `table_start` が追い越して「まだパッケージが無いのに盤面を描く」(tts.ts の投入鎖と同型)。④卓開始の DELETE 後も hello の sha256 は announce し続ける = **既に持っている人はキャッシュで繋がる** (落とせないのは開始後に初めて来た人だけ)。PoC 4 本 (Wrapped・決定論・**受領側の展開器がそのまま受理する**結合の表明 / ホスト固有の出所メタを渡さない / 拒否拡張子を送る前に名指し / 部屋コードと sha256 の形式検査)。app backend 30 green。outcast 側の伝文 `docs/kataribe_multiplayer_handoff.md` + Spec 30 実装は**未コミット** (outcast 側で確認してコミット) / **✅Phase D (2026-07-23 実装・live 検証は E)**: `app/src/voice.ts` + rtc.ts 結線 + マイクトグル + 席色リング脈動 + ホスト終了確認。**中枢 = トランシーバを先に置く** — `newPeer` が offer/answer の**前**に audio `sendrecv` トランシーバを張るので ON/OFF は `replaceTrack` だけ＝**再ネゴシエーションゼロ**。これがあるから「OFF=`track.stop()` の完全解放 (OS インジケータが消える)」を接続を張り直さずに実現できる。**mesh** = GuestLink が他ゲストの offer に応えるようになった (guest↔guest の PC は**音だけ**運び DataChannel を張らない・ゲーム配送は依然 guest→host)。**既定 OFF** = 一度も `getUserMedia` を呼ばない (音声なし参加で権限ダイアログも出ない＝契約どおりであると同時にリスク低減)。レベルは AnalyserNode の RMS を 12Hz 配布・`voice` は peer_id しか知らず entity への写像は table.ts (配送層にゲームの語彙を入れない)。仕様からの逸脱 1 点 = CSS 変数直更新でなく 12Hz reactive (懸念は 60fps churn で 12Hz×5 要素は無視可)。**✅Windows の WebView2 でマイク権限は降りた (2026-07-23 ユーザー実測・2 台)** — 着手時は Tauri の未解決報告 (#12547/#10898/#5042) から「ダイアログすら出ずに失敗しうる」と見積もっていたが、リリースビルドで問題なく取得できた＝Rust 側の権限ハンドラは不要。既定 OFF の設計は残す (音声なし参加が正当な使い方ゆえ)。**入力デバイス選択 (ユーザーFB)**: 設定→サウンド→「卓のマイク」。`deviceId: {exact}` 指名で、**消えていたら既定へ落ちて掴み直す** (USB マイクを抜いた状態で会話不能になるより既定で繋がる方がよい)。ON 中の変更はその場で掴み直し (トランシーバは張ったままゆえ再ネゴシエーションなし)。機器名は権限前は空 = ブラウザ共通の仕様なので案内を出す。macOS は `src-tauri/Info.plist` に `NSMicrophoneUsageDescription` (macOS 実機未確認)。v1 外 = 相手別受信音量 / Phase E (3 人 live 実測: 実 WebRTC 接続・TURN 経由・再接続・GM の行動帰属=核心的未知)。デプロイ前提作業: ~~knock イメージ push~~ ~~VPS デプロイ~~ ✅**2026-07-23 完了** (v0.5.6 の push で CI 発火・GHCR は public 継承ゆえ設定変更なし → ユーザーが VPS へデプロイ)。**実測**: `wss://knock.outcasts.jp/ws` が **101 Switching Protocols** (Caddy 経由の upgrade 素通しが成立) / `turn.outcasts.jp` = 49.212.209.180 (CNAME) / **TCP 3478 open・5349 closed** (v1 は平文 TURN のみ = 設計どおり・turns は証明書配管後)。**未実測**: UDP 3478 と relay 帯 49160-49200/udp・TURN 一時クレデンシャルの発行 (TURN_SECRET 欠落は「直接繋がる相手とは成立し、モバイル回線の 1 人だけが入れない」という切り分けにくい形で出る)**。UI 追補: 割り当て可視化 (席色リング+「プレイヤー: ○○」chip) / 部屋コード既定伏せ字 (配信映り込み対策)。**✅2 台実機での運用整備 (2026-07-23、ユーザーがリリースビルドで実測)**: 実装バグ 3 件はどれも**開発機では構造的に再現しない層**だった — **#73 CSP** (`connect-src` に WebSocket の scheme が無くリリースだけ繋がらない。`devUrl` の間は Vite が画面を配るので **CSP は本番ビルドにしか乗らない**) / **#74 既定パッケージ** (同梱していない `packages/escape` を初回一覧の既定にしていた＝相対パスが開発機の cwd でだけ解決。既定を空へ) / **#75 `table_start` の取りこぼし** (ゲストの受信 switch が卓メッセージを allowlist で列挙し卓開始を `default` で捨てていた → **既定で転送**へ反転＝送り側に種別が増えても落ちない形)。#73 と #74 は**向きが逆の同型** (配布時にだけ在るもの / 開発時にだけ在るもの)。卓 UI は「**運用の状態と操作が同じ場所に見えている**」へ寄せた: 卓バー常設 (提出数・待ち・タイマー・PASS・退出・締切・マイク。ダイアログはモーダルなので操作がそこにしか無いと「提出する」と「締める」が同時に見えない=実測でホストが 0/2 のまま締切を押して弾かれた) / **PASS** (契約 `input_window`) / **自動再接続** (指数バックオフ・成功判定は hello 受信・20 回で諦める) / **解散と切断の区別** (`table_closed` を送ってから 200ms 待って畳む。DataChannel は close で未送信分を捨てうる。区別が無いと解散後も再接続を回す) / **二重接続の封鎖** (`guestJoin` が前の link を閉じていなかった=根本 + ホスト側で同名の古い席を畳む=安全網。**表示名は必須**・同名は許さない規則) / ゲストのローカル操作ロック / バッジをモデル名→接続状態 (AI を回すのはホストゆえ自分のモデル名は嘘) / **会話ペインの配色をテーマから独立** (既定 dark＝舞台と UI クロームの分離)。**残る無防備**: ホストのロード/新規ゲームは押すと participants が失われ卓が黙って壊れる (ゲストの退出にすら確認を付けた以上釣り合っていない=確認を挟むのが次の一手)。**将来の課題 3 件を spec へ起票**: 卓の多数決 (engine の投票=虚構の中の投票とは別物・提示層+配送層に置く) / 死んだキャラクターの扱い (足りないのは卓の編成を途中で変える経路=「中途参加」と同じ機構) / **仲間の所持品と `has_item` gate の食い違い** (gate は既定で主人公だけを見るので、仲間が鍵を持つと**却下すら起きずただ真にならない**。`HasSkill` も同型＝「per-entity の状態を party 単位で問いたい」一類。制約: `Gate::eval` は GameState しか見ないので「パーティ」「この場に居る者」は gate から解決できない)。**✅2026-07-24 に 3 件のうち「仲間の所持品」を解決 + マルチの穴 2 件を補修（ユーザーとの設計対話 → 実装 → PoC → 単独コミット ×3）**: ①**ホスト＝player 固定（決定 3 を改訂）** — `validate_participants` に「host_peer の entity==player」を追加 + GUI 席割りでホスト席を固定（select を出さない・候補から player を除外）。動機は UX でなく**故障モード**: ゲストが主人公を執って落ちると、中心 entity（`AddItem`→player / gate 既定 / 所持表示の既定）が「黙っている」彫像になる degraded 状態が生まれる。ホストは正本・AI・決断を握る最安定 peer なので、主人公をそこに結ぶと「卓が生きている＝主人公は操作されている / ホスト断＝卓ごと消える」の二択に畳める。②**パーティ単位 gate（spec 23 (b)）** — `PARTY` sentinel + 作者宣言 `Scenario.party`（companion ロスター・`characters` の部分集合）を `initial_state`/`transition` で `GameState.party` へ seed し、`has_item`/`has_skill` の `entity: "party"` が **{主人公 ∪ ロスター}** を走査。**設計の核は「集めるな、問い方を変えろ」** — 所持を物理的にプールする袋モデル（共有インベントリ）は「〇〇がない→私が持ってる→共有へ渡して→再挑戦」という**持ち替え作業**を生み冒険を物流管理に変える（ユーザー却下、2026-07-24）ので、**所持は各自の手元のまま問いだけを集団化**した。`Gate::eval` が GameState しか見ない上記制約は「ロスターを GameState に置く」ことで signature 改修なしに回避（新 variant も足さず既存 gate の sentinel で解決）。**埋まらない席は GM が声を当てる NPC 仲間として party に残る**（人間の頭数と無関係＝ロスター固定・participants とは別物）。validate は `party ⊆ characters`（`PartyMemberUnknown`）と予約語 `"party"`（`ReservedPartyEntity`）。app `validate_participants` のゲスト割り当ても `characters` 全体→`party` へ締め直し、**villain/merchant のような GM 専用 NPC を割り当てられる穴も同時に閉じた**。③**状態タブ上段の所持品を viewer 本人へ** — `PLAYER` 固定だったため仲間を操作するゲストにもホストの所持品が並んでいた（単騎前提の名残＝spec 06 の宛先別秘匿と同じ viewer 正しさの一族で、ここだけ取り残されていた。監査の結果 viewer 化すべきはこの 1 箇所だけと確定）。PoC 3 本 Red→Green（party gate / 幻 party メンバー+予約語 / 上段所持品の viewer 追従）。**残る将来課題は 2 件**（卓の多数決 / 死んだキャラクターの扱い）。設計は `specs/23_multiplayer.md`。
- 🟡 **シナリオ配布サイト「Kataribe 書庫」/ spec 05 (2026-06-29 着想 → 2026-07-02 起草 → 2026-07-08 改訂: サーバ側着地済・GUI 統合残)**: パッケージ zip の配布サイト。**サーバ側は outcast リポジトリの Spec 23 として実装完了**（デプロイのみ残・エンジン公開待ち）: `kataribe.outcasts.jp`＝独立 Docker コンテナ（Axum+Postgres 別 DB+Vue3 羊皮紙テーマ）、認証は Cookie を Outcasts `/api/me` へ内部転送する**委譲**（独自認証ゼロ、C-017）、閲覧/DL は無認証。受入検証＝前置きフィルタ（zip slip/zip bomb/暗号化拒否/拒否拡張子 **denylist**（js・wasm 含む）/package.yaml と scenarios/ の存在）で **manifest は parse しない**（title 等はフォーム自己申告、正本検証は受領側 `load_package` の二層のまま）。**配布物は常にフォルダ包み形（Wrapped）に正規化**（直下形は raw copy リネームで統一＝受領側は Wrapped 前提で書ける）。レビュー★/DL ランキング/6 カテゴリ/Mature フラグ/制作ガイド `/guide`+`package_spec.md`（LLM にあらすじ+仕様 md を渡してパッケージ生成、実効性検証済）も着地。**✅Phase C（GUI 統合、2026-07-08 実装済）**: PackageDialog 2 タブ化（ローカル/配布サイト）＝一覧 fetch（検索/カテゴリ/ソート/ページ/Mature バッジ）→「取得」で zip DL→`app_data_dir/packages/` へ展開→`packagePaths` 自動登録。HTTP は backend（`fetch_site_packages`/`install_site_package` command、CORS 回避・DL 上限 110MB）、クライアント側 zip 検証 `app/src-tauri/src/site.rs`（zip slip 遮断/Wrapped 以外拒否/拒否拡張子/bomb 上限＝サーバの鏡、二層）、衝突は `名前_2` 回避、manifest 不読は撤去。サイト URL は `localStorage["kataribe.siteUrl"]`（既定=公式、自前サーバ可）。**説明文のクランプ + 詳細リンク (✅2026-07-18, ユーザーFB)**: 一覧カードの description 全文表示で長文がカードを縦長にしていた → CSS 2 行クランプ（文字数切りは表示幅で行数が揺れるため行数で切る、ローカル/サイト両タブ）+「書庫で見る ↗」リンク（`open_external_url` で `{siteUrl}/packages/{id}` を既定ブラウザへ。開くのは常にユーザー登録 siteUrl 起点・id は encodeURIComponent でパス成分固定）。PoC: site.rs unit 7 本 + opt-in 統合 2 本（`--ignored`、実 dev 一覧 DTO + モック書庫 e2e）green、frontend build green。実機残: GUI 目視 + dev 書庫への実パッケージ upload（残骸行は実ファイル無しで 500）。契約の正本は `specs/05_distribution_site.md`、実装詳細は outcast `specs/23`。
- 配布仕様の写しがoutcastのリポジトリにあります。outcast\kataribe\frontend\public\package_spec.md 仕様を変えたときはこちらの修正をしなければならないので報告してください。

## ルーツ

LocalAI（`D:/Github/LocalAI`、廃棄）から摘出: `llm_client` / SSE ストリーミング骨格 / HybridMemory / Tauri UI 殻。
中核（正本・シナリオ脊椎）は新規。詳細な判断経緯は LocalAI のコードと本台帳を参照。
