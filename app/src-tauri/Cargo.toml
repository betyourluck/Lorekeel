[package]
name = "kataribe-desktop"
version = "0.1.0"
description = "Kataribe — 忘れない・矛盾しない GM のデスクトップ殻"
edition = "2021"

# 親 workspace (crates/*) に属さない独立 project として扱わせる。
# 重い tauri 依存を core workspace の `cargo test --workspace` ループから隔離する
# (LocalAI prompt/src-tauri と同流儀)。harness/gm_core/llm_client は path 依存。
[workspace]

[lib]
# Windows でビルド衝突を避けるため _lib サフィックス (Tauri 慣習)。
name = "kataribe_desktop_lib"
crate-type = ["staticlib", "cdylib", "rlib"]

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
tauri = { version = "2", features = ["protocol-asset"] }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
# spec 28 Phase B: エディタ診断の parse エラー位置 (serde_yaml::Location)。workspace と同版。
serde_yaml = "0.9"
tokio = { version = "1", features = ["sync", "rt-multi-thread", "macros"] }
dotenvy = "0.15"
# spec 05 Phase C: 配布サイトからの一覧 fetch + zip DL (workspace と同版・同 TLS)。
reqwest = { version = "0.12", default-features = false, features = ["json", "rustls-tls", "multipart"] }
# spec 05 Phase C: 配布 zip の検証 + 展開 (outcast 側 zip_check と同じ crate)。
zip = "2"
# spec 17: 出所メタの content_hash/tree_hash (zip 経由で lock に既在 = 実質追加ゼロ)。
sha2 = "0.10"
# パッケージ一覧の「参照」: OS ネイティブのフォルダ選択ダイアログ。tauri-plugin-dialog を
# 足さず custom command で完結させる (open_log_folder が tauri-plugin-shell を避けたのと
# 同じ「追加権限ゼロ」方針)。Linux 既定は xdg-portal (libdbus 依存は Tauri 側で既に在る)。
rfd = "0.17"
gm_core = { path = "../../crates/gm_core" }
llm_client = { path = "../../crates/llm_client" }
harness = { path = "../../crates/harness" }
